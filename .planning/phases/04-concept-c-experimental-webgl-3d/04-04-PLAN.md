---
phase: 04-concept-c-experimental-webgl-3d
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - concept-c/assets/js/scene.js
  - qa/camera-framing-check.mjs
  - .planning/phases/04-concept-c-experimental-webgl-3d/captures/
autonomous: true
gap_closure: true
requirements: [CONC-01, CONC-03]

must_haves:
  truths:
    - "At each of the 6 topic waypoints (#t-problems … #t-contact), the settled camera frames that topic's object fully and legibly — camera-to-object distance >= 2.5x that object's bounding radius — with the object's DOM label visible in frame"
    - "At every scroll position in the runway (not just waypoints), at least one object is meaningfully in frame: the look direction is within 30 degrees of some nearby object's bearing — no empty frames anywhere in the transit"
    - "The camera never sits at or inside any object's bounding sphere at any scroll position"
    - "Camera position and look target are a single parametrization (per-topic {cameraPos, lookAt} keyframe pairs interpolated together) — not two independently-shaped curves"
    - "Damped scrollY drive (lerp 0.08), native scroll, deep links, topic-index highlight, idle rotation, parallax, Flarepop one-at-a-time accent, static/frozen and no-webgl modes all behave exactly as before"
    - "Copy fidelity (282/282 chunks) and the full 8-grep brand suite still pass"
  artifacts:
    - path: "concept-c/assets/js/scene.js"
      provides: "Dolly keyframe pairs derived FROM object positions + bounding radii; exported pure buildDollyRig() consumed by both initScene and the QA gate"
      contains: "buildDollyRig"
    - path: "qa/camera-framing-check.mjs"
      provides: "Headless math gate: waypoint standoff/alignment invariants + continuous-sweep no-empty-frame/no-intrusion invariants"
      contains: "buildDollyRig"
    - path: ".planning/phases/04-concept-c-experimental-webgl-3d/captures/settle2-work.png"
      provides: "Settled re-capture at the worst prior waypoint (t=0.4, was camera-inside-mesh) proving legible framing"
  key_links:
    - from: "concept-c/assets/js/scene.js buildDollyRig"
      to: "OBJECT_DEFS positions + per-object geometry bounding radii"
      via: "computeBoundingSphere-derived standoff (>= 2.5x radius) per topic"
      pattern: "computeBoundingSphere"
    - from: "qa/camera-framing-check.mjs"
      to: "concept-c/assets/js/scene.js"
      via: "imports the SAME exported derivation the scene runs (never a reimplementation)"
      pattern: "import.*buildDollyRig.*scene\\.js"
    - from: "updateCameraFromScroll"
      to: "cameraPosCurve + lookTargetCurve"
      via: "both sampled with getPoint(t) (uniform segment parametrization) at the same t"
      pattern: "getPoint\\("
---

<objective>
Close the single verified Phase 4 gap: the scroll-driven dolly's camera path (8 hand-tuned points) and look-at path (6 points) are independently shaped and desynchronize — at t=0.4 (Work) the camera sits INSIDE the object's bounding sphere (1.70 vs radius 1.82), at t=0.6 (Labs) it grazes the surface (1.37 vs 1.02), at t=0.2 (InterceptOS) framing is cropped, and between waypoints (t≈0.45) the look direction points 79–131 degrees away from every object, producing an empty frame across ~40–50% of the scroll journey.

Fix (locked direction): derive the camera path FROM the object positions. At each of the 6 waypoint t-values the camera sits at a computed standoff (>= 2.5x that object's bounding radius, tuned per object) looking at that object; between waypoints, camera position and look target interpolate TOGETHER as {cameraPos, lookAt} keyframe pairs under one parametrization — never two unrelated curves. Everything else about the scene is verified-good and must not change.

Purpose: restores the phase's headline "topic field spatial navigation" metaphor (CONC-01) and makes the scroll-driven camera movement legible end-to-end (CONC-03).
Output: fixed scene.js, a permanent headless camera-framing math gate in qa/, and reviewed settle2-* captures proving every waypoint and the transit zones frame correctly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concept-c-experimental-webgl-3d/04-VERIFICATION.md
@.planning/phases/04-concept-c-experimental-webgl-3d/04-CONTEXT.md
@concept-c/assets/js/scene.js
@concept-c/index.html

<interfaces>
<!-- Current contracts in scene.js the fix replaces/preserves. Use directly — no exploration needed. -->

The broken pair (REPLACE — root cause per 04-VERIFICATION.md):
```js
// 8 hand-tuned camera points vs 6-point look curve = two unrelated shapes
const CAMERA_PATH_POINTS = [ [0,2.4,6], [-0.6,1.5,2], [3.0,1.9,-6], [-2.8,1.3,-14],
  [3.2,1.1,-22], [-2.8,1.5,-30], [2.8,1.7,-38], [0.6,2.1,-44] ];
// ...inside initScene animated path:
const cameraCurve = new THREE.CatmullRomCurve3(CAMERA_PATH_POINTS.map(...), false, 'catmullrom', 0.5);
const lookCurve   = new THREE.CatmullRomCurve3(OBJECT_DEFS.map(d => new THREE.Vector3(...d.pos)), false, 'catmullrom', 0.5);
// ...sampled with arc-length getPointAt(t) — does NOT land on control points at t=i/5
```

Object field (KEEP positions/shapes — the camera derives from these):
```js
const OBJECT_DEFS = [
  { topic: 'problems',    kind: 'box',   size: [1.1, 3.2, 1.1], pos: [-2.4,  0.2,   0], tone: '--surface-2' },
  { topic: 'interceptos', kind: 'prism', size: [2.4, 3.0, 1.6], pos: [ 2.2,  0.6,  -8], tone: '--surface-3', leanDeg: 6 },
  { topic: 'work',        kind: 'box',   size: [2.1, 2.1, 2.1], pos: [-2.0,  0.2, -16], tone: '--surface-2' },
  { topic: 'labs',        kind: 'prism', size: [1.1, 1.4, 1.0], pos: [ 2.4, -0.1, -24], tone: '--surface-3', leanDeg: -9 },
  { topic: 'insights',    kind: 'box',   size: [3.6, 1.1, 1.1], pos: [-2.2,  0.3, -32], tone: '--surface-2' },
  { topic: 'contact',     kind: 'prism', size: [1.8, 2.2, 1.4], pos: [ 1.8,  0.2, -40], tone: '--surface-3', leanDeg: 8 },
];
```

Scroll drive (KEEP structure — only the curve sampling calls change):
```js
function updateCameraFromScroll(scrollY) {
  const t = THREE.MathUtils.clamp(scrollY / denom, 0, 1);   // denom = runway.offsetHeight - innerHeight
  cameraCurve.getPointAt(t, targetPos);  smoothPos.lerp(targetPos, 0.08);
  lookCurve.getPointAt(t, targetLook);   smoothLook.lerp(targetLook, 0.08);
  camera.position.copy(smoothPos);  camera.lookAt(smoothLook);
  updateTopicIndex(t);                                       // Math.round(t * 5) — aligns exactly with keyframes at i/5
}
```

Verified-good exact-t waypoint anchors in index.html: `#t-problems` t=0, `#t-interceptos` t=0.2, `#t-work` t=0.4, `#t-labs` t=0.6, `#t-insights` t=0.8, `#t-contact` t=1.0. Camera FOV 50 (vertical), so half-FOV ≈ 25° vertical / ≈ 38.7° horizontal at 1440x900.

Verifier's ground-truth radii (computeBoundingSphere on the actual geometry reproduces these): problems 1.78, interceptos 2.08, work 1.82, labs 1.02, insights 1.96, contact 1.58. Framing that already reads well: Problems at 3.8x radius, Contact at 2.9x.

Headless-import guard (KEEP — the module parse gate and the new QA gate depend on it): all DOM/WebGL side effects live behind `if (typeof document !== 'undefined') boot();`. `readToken()` uses getComputedStyle — buildDollyRig must NOT call it (geometry math only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the dolly from the object field — keyframe pairs, single parametrization</name>
  <files>concept-c/assets/js/scene.js</files>
  <action>
Replace the hand-tuned CAMERA_PATH_POINTS + independent lookCurve with a derived rig:

1. **Delete `CAMERA_PATH_POINTS` entirely.** Add per-topic framing fields to OBJECT_DEFS (or a parallel const beside it): `standoff` (multiplier on bounding radius, hard floor 2.5 — start near 3.2, tune per object within ~2.8–3.8; Problems already reads well at 3.8x and Contact at 2.9x per 04-VERIFICATION.md) and an offset direction hint. Offset direction constraints: positive z-component (camera in FRONT of the object relative to the -Z travel direction — the dolly always flies forward, never looks back up the tunnel), non-zero x-component (diagonal view so the prisms' apex-up triangular profile stays readable — never pure down-the-tunnel, which flattens them; this preserves the locked apex-up profile framing), small positive y-component (slightly above center — calm). A concrete starting recipe: `offsetDir = normalize([sideSign * 1.0, 0.45, 1.5])` with sideSign alternating to keep the existing side-to-side weave feel; executor discretion on exact values, constraints above are hard.

2. **Add an exported pure function `export function buildDollyRig()`** at module scope, headless-safe (geometry math only — no DOM, no getComputedStyle/readToken, no renderer). It must:
   - For each OBJECT_DEFS entry, construct its geometry (BoxGeometry or makePrismGeometry — both are pure math, safe in Node), call `geometry.computeBoundingSphere()`, and take `radius = geometry.boundingSphere.radius`. (Lean rotation is radius-invariant. Box [2.1,2.1,2.1] must yield ≈1.82, matching the verifier's Work figure — sanity anchor.)
   - Build 6 keyframe PAIRS: `{ topic, lookAt: objectPos, cameraPos: objectPos + offsetDir * (standoff * radius), radius, standoff }`.
   - Build TWO CatmullRomCurve3 curves with EXACTLY 6 points each and identical closed/type/tension arguments: `cameraPosCurve` through the 6 cameraPos values, `lookTargetCurve` through the 6 lookAt values. Same point count + same construction = the pair interpolates the same segment at the same t. If the continuous-sweep gate in Task 2 catches catmullrom-0.5 overshoot dipping inside a bounding sphere, switch BOTH curves to 'centripetal' (they must always stay identical).
   - Return `{ keyframes, cameraPosCurve, lookTargetCurve }`.

3. **In initScene's animated path**, replace the old cameraCurve/lookCurve construction with `const { cameraPosCurve, lookTargetCurve } = buildDollyRig();` and — CRITICAL — sample both with **`getPoint(t, target)`, NOT `getPointAt(t, target)`**. getPointAt is arc-length reparametrized: two curves with different arc profiles sit at different segment positions at the same t (the exact desync mechanism that broke the old version) and don't land on control points at t=i/5. getPoint is uniform across segments: with 6 points, t=i/5 lands EXACTLY on keyframe i for BOTH curves, so every `#t-*` deep link settles at its computed standoff looking dead-on at its object, and between waypoints the pair moves through the same segment together. Initialize smoothPos/smoothLook from `getPoint(0)` likewise.

4. **Change nothing else.** Keep: smoothPos/smoothLook lerp factor 0.08 (damped scrollY drive), updateTopicIndex (Math.round(t*5) now aligns exactly with keyframe indices), idle sine rotation, cursor parallax, Flarepop setAccent/clearAccent, render-loop gating, resize handling, the static-scene (animated=false) hardcoded overview camera at (26,6,6), the no-webgl boot branch, device tiering, and the typeof-document guard. Mesh creation in initScene stays as-is (it may optionally reuse rig geometry/radii, but must not move readToken calls out of initScene).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && node --input-type=module -e "import('./concept-c/assets/js/scene.js').then(m=>{if(typeof m.buildDollyRig!=='function')throw new Error('buildDollyRig not exported');const r=m.buildDollyRig();if(!r.keyframes||r.keyframes.length!==6)throw new Error('need 6 keyframes');console.log('ok')}).catch(e=>{console.error(e);process.exit(1)})" && ! grep -q 'CAMERA_PATH_POINTS' concept-c/assets/js/scene.js && ! grep -q 'getPointAt' concept-c/assets/js/scene.js && grep -q 'computeBoundingSphere' concept-c/assets/js/scene.js</automated>
  </verify>
  <done>CAMERA_PATH_POINTS and all getPointAt sampling are gone; buildDollyRig() is exported, headless-importable, returns 6 derived {cameraPos, lookAt} keyframe pairs plus two structurally-identical 6-point curves; the animated path samples both curves with getPoint at the same t; every other verified-good behavior (damping, modes, accent, tiering) is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Permanent headless camera-framing math gate</name>
  <files>qa/camera-framing-check.mjs</files>
  <action>
Create `qa/camera-framing-check.mjs` — a Node ESM script run from the project root as `node qa/camera-framing-check.mjs`. It imports `buildDollyRig` and (for Vector3 math) the vendored three module from scene.js's own vendor path: `import { buildDollyRig } from '../concept-c/assets/js/scene.js'` resolves relative to the script file, and scene.js's own `../vendor/three.module.js` import resolves relative to scene.js — no copies, no reimplementation. The gate validates the SAME derivation production runs.

**Thresholds are fixed constants taken from 04-VERIFICATION.md's gap spec — if the gate fails, fix scene.js (tune standoff/offsetDir), never loosen a threshold:**

A. **Waypoint invariants** — for i in 0..5 at t = i/5:
   1. `cameraPosCurve.getPoint(t)` equals `keyframes[i].cameraPos` and `lookTargetCurve.getPoint(t)` equals `keyframes[i].lookAt`, each within 1e-6 (proves the exact-hit uniform parametrization).
   2. `distance(cameraPos, lookAt) >= 2.5 * keyframes[i].radius` (the locked standoff floor).
   3. Angle between the look direction (lookAt − cameraPos) and the bearing to object i is < 1 degree (dead-on framing at every waypoint).

B. **Continuous sweep** — for t from 0 to 1 in steps of 0.01 (101 samples), with `camPos = cameraPosCurve.getPoint(t)`, `lookDir = normalize(lookTargetCurve.getPoint(t) − camPos)`:
   1. `distance(camPos, objectPos_j) > 1.15 * radius_j` for EVERY object j (camera never inside or grazing any bounding sphere — catches CatmullRom overshoot between keyframes).
   2. At least one object j with `distance(camPos, objectPos_j) <= 30` has `angle(lookDir, bearing_j) <= 30 degrees` (something is always meaningfully in frame; 30° sits inside the ~38.7° horizontal half-FOV and matches the VERIFICATION missing-item threshold — the old code failed this at t≈0.45 with 79.5° off every object).

Output: print a waypoint table (topic, t, distance, radius, achieved multiplier, look-angle) plus the worst sweep sample (min clearance multiplier, max min-angle) for the SUMMARY; on any failure print every failing row with its numbers and exit 1; exit 0 only when all invariants hold.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && node qa/camera-framing-check.mjs && grep -q 'buildDollyRig' qa/camera-framing-check.mjs</automated>
  </verify>
  <done>qa/camera-framing-check.mjs exits 0 against the fixed scene.js: all 6 waypoints hit their keyframes exactly at t=i/5 with >= 2.5x-radius standoff and <1° look error; the 101-sample sweep never enters a 1.15x bounding sphere and always has at least one object within 30° of the look direction; thresholds match 04-VERIFICATION.md, untouched by tuning.</done>
</task>

<task type="auto">
  <name>Task 3: Settle-methodology re-capture + full regression suite</name>
  <files>.planning/phases/04-concept-c-experimental-webgl-3d/captures/</files>
  <action>
Re-run the exact settle methodology 04-VERIFICATION.md used, against the fixed scene. Start `./serve.sh` (:4340) if not already up (kill after only if this task started it). Puppeteer rig lives in the scratchpad, NEVER the repo (04-03 precedent): `executablePath` = installed Google Chrome (`/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` — the bundled Chrome-for-Testing hangs on captureScreenshot in this sandbox), `page.setViewport({ width: 1440, height: 900 })`, plus any headless-GL flags 04-01 recorded.

**Waypoint captures (6):** for each topic in problems, interceptos, work, labs, insights, contact: goto `http://localhost:4340/concept-c/index.html#t-<topic>`, wait 2.8s (>= the required 2.5s settle — past the 0.08-lerp convergence window), screenshot to `.planning/phases/04-concept-c-experimental-webgl-3d/captures/settle2-<topic>.png`.

**Intermediate captures (3):** goto the base URL, then `page.evaluate` a `window.scrollTo(0, Math.round(t * (document.querySelector('.scroll-runway').offsetHeight - window.innerHeight)))` for t = 0.1, 0.45, 0.8 (0.45 is the original empty-frame reproduction point), wait 2.8s each, screenshot to `settle2-mid-010.png`, `settle2-mid-045.png`, `settle2-mid-080.png`.

**Mode-regression captures (2):** `settle2-reduced-1440.png` — emulate `prefers-reduced-motion: reduce`, top of page: the frozen overview must still show all 6 objects + labels exactly as the untouched static path always has. `settle2-top-390.png` — viewport 390x844 at t=0: the new t=0 camera keyframe changes the Problems label projection, so confirm the 04-03 surface-chip fix still prevents the label/hero-headline collision.

**READ every capture (all 11) with the Read tool and judge — this is the gap's acceptance evidence:**
- Each of the 6 waypoint captures: the focused topic's object clearly in frame — full silhouette visible (not clipped by viewport edges, not an unrecognizable single-flat-face fill, camera obviously outside the mesh), hard-stepped toon shading readable, and that topic's label visible. Work and Labs (the prior camera-inside-mesh failures) get the harshest read.
- Each of the 3 intermediates: at least one object meaningfully in frame — recognizable geometry, not empty background with only topbar + topic index (the original bug's signature).
- The 2 mode captures: reduced-motion overview unchanged in character; 390 label chip legible over the hero.

If any capture fails the read: tune the per-topic `standoff` multipliers / offset directions in scene.js (floor stays 2.5x), re-run `node qa/camera-framing-check.mjs` (must still pass), re-capture, re-read. Iterate until all 11 pass.

**Mechanical regression suite (all must pass after any tuning):** `python3 qa/copy-diff.py --all concept-c` (282/282), the full 8-grep vendor-excluded brand chain, the headless module-parse gate on scene.js, and `node qa/camera-framing-check.mjs`. Record the framing gate's waypoint table + the capture-review verdicts for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && for f in settle2-problems settle2-interceptos settle2-work settle2-labs settle2-insights settle2-contact settle2-mid-010 settle2-mid-045 settle2-mid-080 settle2-reduced-1440 settle2-top-390; do [ -f ".planning/phases/04-concept-c-experimental-webgl-3d/captures/$f.png" ] || { echo "missing $f"; exit 1; }; done && ! find .planning/phases/04-concept-c-experimental-webgl-3d/captures -name 'settle2-*.png' -size -8k | grep -q . && node qa/camera-framing-check.mjs && python3 qa/copy-diff.py --all concept-c && node --input-type=module -e "import('./concept-c/assets/js/scene.js').then(()=>console.log('ok'),e=>{console.error(e);process.exit(1)})" && ! grep -rIiE --exclude-dir=vendor 'fresh thinking starts here' concept-c && ! grep -rIiE --exclude-dir=vendor 'a855f7|6366f1|22d3ee' concept-c && ! grep -rIi --exclude-dir=vendor '<hr' concept-c && ! grep -rIE --exclude-dir=vendor 'border-(top|bottom)[[:space:]]*:' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*#' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])linear-gradient\(|radial-gradient|conic-gradient' concept-c && ! grep -rI --exclude-dir=vendor 'view-transition-name' concept-c</automated>
  </verify>
  <done>All 11 settle2-* captures exist (non-trivial size), were each Read and pass judgment: every waypoint shows its focused object fully framed with its label (Work and Labs no longer inside/grazing the mesh), all 3 intermediates show at least one recognizable object (t=0.45 no longer empty), reduced-motion and 390px modes regressed clean; camera-framing gate, copy-diff 282/282, module parse, and all 8 brand greps pass.</done>
</task>

</tasks>

<verification>
- `node qa/camera-framing-check.mjs` exits 0: exact keyframe hits at t=i/5, >= 2.5x-radius standoff and <1° look error at all 6 waypoints, 101-sample sweep with no bounding-sphere intrusion (1.15x margin) and always >= 1 object within 30° of the look direction — the three failure modes 04-VERIFICATION.md measured (1.70 vs 1.82 at Work, 1.37 vs 1.02 at Labs, 79.5° off everything at t=0.45) are each directly gated.
- All 11 settle2-* captures reviewed by reading them: 6 waypoints legible with labels, 3 intermediates never empty, 2 mode regressions clean.
- `python3 qa/copy-diff.py --all concept-c` exits 0 (282/282); full 8-grep brand suite clean; headless module-parse gate passes.
- No changes to: static-scene camera, no-webgl branch, device tiering, accent handling, scroll/deep-link mechanics, label projection.
</verification>

<success_criteria>
The gap's failed truth flips to verified: scrolling the runway dollies the camera such that each topic's waypoint frames its 3D object legibly (camera at a computed >= 2.5x-bounding-radius standoff, looking dead-on) and transitions between waypoints never present empty or clipped frames — proven mechanically by the new qa/camera-framing-check.mjs gate and visually by 11 read-and-judged settle2 captures, with every previously-passing Phase 4 gate (copy 282/282, brand greps, module parse, modes) still green. CONC-01 and CONC-03 move from PARTIAL to SATISFIED.
</success_criteria>

<output>
After completion, create `.planning/phases/04-concept-c-experimental-webgl-3d/04-04-SUMMARY.md` including the framing gate's waypoint table (topic, distance, radius, achieved multiplier), the worst sweep sample, and the capture-review verdicts.
</output>
