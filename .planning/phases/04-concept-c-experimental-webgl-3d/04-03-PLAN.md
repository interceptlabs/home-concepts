---
phase: 04-concept-c-experimental-webgl-3d
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - concept-c/assets/js/scene.js
  - concept-c/assets/css/concept-c.css
  - concept-c/index.html
  - .planning/phases/04-concept-c-experimental-webgl-3d/captures/
autonomous: true
requirements: [CONC-04, CONC-05]

must_haves:
  truths:
    - "With prefers-reduced-motion set on a WebGL2 device: a FROZEN composed 3D scene renders once at an overview framing showing all 6 objects with labels correctly projected — no camera dolly, no parallax, DPR 1, no rAF loop; anchor jumps are instant; resize re-projects and re-renders once"
    - "Keyboard tabbing through topbar CTA, all 6 topic labels, the topic index, and every sub-page link shows a clear :focus-visible treatment; a label whose object leaves the frustum hides via visibility (never display:none) and focus is never silently ejected"
    - "On a low-tier device (hardwareConcurrency <= 4, or deviceMemory <= 4 when present, or narrow viewport as the Safari/Firefox fallback): the scene constructs with antialias off, DPR 1, 3-step toon map, and no parallax — tier decided BEFORE renderer construction"
    - "copy-diff --all concept-c exits 0; all 8 brand greps clean; every internal href and #anchor across the 4 pages resolves; >=16 non-trivial captures reviewed covering 390/768/1440, mid-dolly, forced no-webgl, reduced-motion frozen scene, and JS-off"
  artifacts:
    - path: "concept-c/assets/js/scene.js"
      provides: "deviceTier() heuristic + static-scene overview framing + tier-aware renderer construction"
      contains: "hardwareConcurrency"
    - path: "concept-c/assets/css/concept-c.css"
      provides: ":focus-visible treatment + prefers-reduced-motion overrides (instant scroll, no CSS motion)"
      contains: "focus-visible"
    - path: ".planning/phases/04-concept-c-experimental-webgl-3d/captures"
      provides: ">=16 reviewed QA captures (12 JS-off responsive + scene/mid-dolly/no-webgl/reduced-motion JS-on)"
  key_links:
    - from: "concept-c/assets/js/scene.js deviceTier()"
      to: "renderer/material construction"
      via: "tier decided before new THREE.WebGLRenderer (antialias) and before makeStepGradientMap (3 vs 4 steps)"
      pattern: "deviceTier"
    - from: "static-scene mode"
      to: "one-shot render + resize re-render"
      via: "initScene({ animated: false }) — no rAF scheduling path"
      pattern: "animated"
    - from: "qa sweep"
      to: "all 4 concept-c pages"
      via: "python3 qa/copy-diff.py --all concept-c + link-integrity script + capture review"
      pattern: "copy-diff"
---

<objective>
Finish Concept C: refine the reduced-motion frozen-scene mode to the locked spec, add the focus-visible treatment, implement device-tier degradation, then run the phase-closing mechanical QA sweep — copy-diff on all 4 pages, the full 8-grep brand suite, link integrity, and reviewed responsive captures including mid-dolly, forced no-webgl, reduced-motion, and JS-off states.

Purpose: CONC-04 (reduced-motion + no-WebGL paths verified end to end) and CONC-05 (device tiering complete), plus the mechanical proof the phase is done.
Output: polished scene.js + concept-c.css, QA captures in the phase directory, QA results table for the SUMMARY.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concept-c-experimental-webgl-3d/04-CONTEXT.md
@.planning/phases/04-concept-c-experimental-webgl-3d/04-RESEARCH.md (Pitfall 3: deviceMemory is Chromium-only — hardwareConcurrency primary, screen-size fallback; Open Question 1: frozen 3D scene is the locked reduced-motion call)
@concept-c/assets/js/scene.js
@concept-c/assets/css/concept-c.css

<interfaces>
<!-- Contracts inherited from 04-01/04-02 — extend, do not rename -->
- Mode classes: webgl / webgl static-scene / no-webgl; static-scene already exists as a basic render-once path from 04-01 — this plan brings it to the locked spec
- scene.js structure: boot() decision tree behind typeof-document guard; initScene({ animated }); readToken(); makeStepGradientMap(steps); sceneReady dataset hook; label bindings on a.topic-label[data-topic]
- .is-hidden = visibility:hidden (never display:none) — keep
- Runway collapses to 100svh under static-scene and no-webgl (04-01 CSS contract)
- Capture rig (02-03/03-03 proven): puppeteer in scratchpad only, executablePath real installed Google Chrome, page.setViewport(), page.setJavaScriptEnabled(false) for JS-off, fullPage for page captures; headless-GL flags recorded in 04-01's SUMMARY if SwiftShader was needed; page.emulateMediaFeatures([{name:'prefers-reduced-motion', value:'reduce'}]) for the frozen-scene capture; waitForFunction on dataset.sceneReady for JS-on scene captures
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frozen-scene refinement + focus-visible + device-tier degradation</name>
  <files>concept-c/assets/js/scene.js, concept-c/assets/css/concept-c.css, concept-c/index.html</files>
  <action>
**Static-scene mode to the locked spec (reduced-motion, WebGL2 available):** camera at a composed OVERVIEW framing — a dedicated keyframe (not curve t=0) chosen so all 6 objects and their labels are visible and legible at once; labels projected once after the render (and re-projected + re-rendered on resize/orientationchange only); `renderer.setPixelRatio(1)` regardless of tier; no rAF ever scheduled; no parallax listeners registered; no idle rotation; topic-index stays hidden (04-01 CSS contract — the label constellation is the nav here). Verify no scroll listener registers camera work in this mode. The scene renders the SAME topic field — reduced-motion visitors see the actual concept, frozen (locked call).

**CSS reduced-motion overrides:** `@media (prefers-reduced-motion: reduce)` block forcing `animation: none; transition: none` on any animated affordance added across 04-01/02 (label marker scale, CTA hovers) — belt-and-braces alongside the JS gate. Smooth scroll already collapses to instant via the no-preference wrapper from 04-02 — confirm, don't duplicate.

**Focus-visible treatment (whole concept):** consistent `:focus-visible` on topic labels, topic index items, topbar CTA, footer/site links, and sub-page links: 2px solid outline in var(--fg), outline-offset 2-3px, plus the label's marker dot swelling on focus exactly as on hover (focus parity with hover — CONC-02's "hover AND keyboard-focus labels"). No color-based-only focus signal; no borders (outline only — the border-top/bottom grep must stay clean).

**Device tiering (RESEARCH Pitfall 3):** `deviceTier()` returning 'low' | 'full', computed BEFORE renderer construction: low when `navigator.hardwareConcurrency <= 4`, OR `navigator.deviceMemory <= 4` (Chromium-only bonus signal, only when present), OR `matchMedia('(max-width: 768px)').matches` as the Safari/Firefox fallback — never gated on deviceMemory alone. Low tier: `antialias: false` in the WebGLRenderer options, `setPixelRatio(1)`, `makeStepGradientMap(3)` (still within the locked 3-5 hard-step range), parallax never registered. Full tier: current 04-01 behavior. Log the chosen tier via the dataset hook (`dataset.sceneTier = tier`) for the capture rig and SUMMARY.

Keep the boot decision tree order: no-webgl check FIRST, then reduced-motion, then tier — tier applies inside both animated and static paths. All changes stay behind the typeof-document guard so the Node module-parse gate keeps passing. Touch index.html only if a hook/class is genuinely needed; copy must not change (copy-diff re-verifies).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && node --input-type=module -e "import('./concept-c/assets/js/scene.js').then(()=>console.log('ok'),e=>{console.error(e);process.exit(1)})" && grep -q 'deviceTier' concept-c/assets/js/scene.js && grep -q 'hardwareConcurrency' concept-c/assets/js/scene.js && grep -q 'deviceMemory' concept-c/assets/js/scene.js && grep -q 'focus-visible' concept-c/assets/css/concept-c.css && grep -q 'prefers-reduced-motion: reduce' concept-c/assets/css/concept-c.css && python3 qa/copy-diff.py --all concept-c && ! grep -rIiE --exclude-dir=vendor 'fresh thinking starts here' concept-c && ! grep -rIiE --exclude-dir=vendor 'a855f7|6366f1|22d3ee' concept-c && ! grep -rIi --exclude-dir=vendor '<hr' concept-c && ! grep -rIE --exclude-dir=vendor 'border-(top|bottom)[[:space:]]*:' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*#' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])linear-gradient\(|radial-gradient|conic-gradient' concept-c && ! grep -rI --exclude-dir=vendor 'view-transition-name' concept-c</automated>
  </verify>
  <done>Static-scene renders the composed overview once at DPR 1 with no loop/parallax/scroll work; every interactive element has a visible outline-based focus-visible state with hover parity on labels; deviceTier() degrades antialias/DPR/step-count/parallax before construction and never relies on deviceMemory alone; copy-diff --all and all greps clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mechanical QA — copy gate, brand greps, link integrity, full capture sweep</name>
  <files>.planning/phases/04-concept-c-experimental-webgl-3d/captures/</files>
  <action>
Start `./serve.sh` (:4340) if not already up; kill it after only if this task started it. Puppeteer stays in the scratchpad (never the repo); use the proven rig and any headless-GL flags recorded by 04-01.

**1. Copy gate:** `python3 qa/copy-diff.py --all concept-c` — exit 0 (index + 3 sub-pages, annotated mode).

**2. Brand grep suite:** the 8-grep vendor-excluded chain in the verify block — banned tagline, deprecated 12-path hexes, `<hr>`, border-top/bottom hairlines, raw-hex colored text, non-Flarepop colored text vars, smooth/radial/conic gradients (repeating-linear-gradient hard steps are the sole licensed exception — additionally audit `grep -n 'gradient(' concept-c/assets/css/concept-c.css` and confirm every hit is the duplicated-stop stepped utility), view-transition-name.

**3. Link integrity (scratchpad script):** extract every href/src from the 4 concept-c HTML pages; for local paths assert the target file exists on disk; for #anchors assert an element with that id exists in the target page; for the runway waypoints assert all 6 t-* ids match the topic-index hrefs. External hrefs (Spotify/Apple/YouTube episode links) are checked for well-formedness only, not fetched. Zero failures.

**4. JS-off responsive captures (12):** index.html + pages/interceptos.html + pages/work.html + pages/insights.html at 390/768/1440, `setJavaScriptEnabled(false)`, fullPage — into `.planning/phases/04-concept-c-experimental-webgl-3d/captures/{page}-{width}.png`. These simultaneously prove the no-JS guarantee: index captures must show the stepped backdrop, the label constellation, hero copy, and the full below-fold content with no canvas and no 400vh dead runway.

**5. JS-on scene captures (6):** with sceneReady wait — `index-scene-390.png`, `index-scene-1440.png` (top framing); `index-middolly-390.png`, `index-middolly-1440.png` (scrollTo ~45% of runway, settle ~1.5s); `index-nowebgl-1440.png` (?nowebgl=1); `index-reduced-1440.png` (emulateMediaFeatures prefers-reduced-motion:reduce — must show the frozen overview: all 6 objects + labels, and dataset.sceneTier/sceneReady set with no dolly on subsequent scroll — verify via page.evaluate that scrollTo() then a 1s wait leaves the canvas pixels unchanged or simply that no rAF is pending via a scene-exposed flag if simpler; record method used).

**6. Review:** Read ALL 18 PNGs. Judge: hard-stepped toon bands (not smooth); apex-up geometry; labels legible + non-colliding at 390; topic index not overlapping labels; below-fold sections opaque with no canvas bleed-through; sub-pages clean at all widths; no horizontal overflow anywhere. Fix any issue found, re-run copy-diff + greps on touched files, re-capture.

**7. Functional spot checks in a real browser (record observations in SUMMARY):** (a) scroll with keyboard PageDown through the dolly — native physics, no hijack; (b) deep-link to /concept-c/#t-work — camera lands framing Work; (c) navigate to a sub-page and back — return renders promptly and the dolly resumes at the restored scroll position (pageshow/bfcache path); (d) Tab through the page — focus order logical, focus visible everywhere, no focus loss while scrolling with a label focused.

**8. Record the QA results table** (gate results, each grep, link-integrity counts, 18 captures, tier observed, functional checks) for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 qa/copy-diff.py --all concept-c && [ "$(ls .planning/phases/04-concept-c-experimental-webgl-3d/captures/*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 16 ] && ! find .planning/phases/04-concept-c-experimental-webgl-3d/captures -name '*.png' -size -8k | grep -q . && ! grep -rIiE --exclude-dir=vendor 'fresh thinking starts here' concept-c && ! grep -rIiE --exclude-dir=vendor 'a855f7|6366f1|22d3ee' concept-c && ! grep -rIi --exclude-dir=vendor '<hr' concept-c && ! grep -rIE --exclude-dir=vendor 'border-(top|bottom)[[:space:]]*:' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*#' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])linear-gradient\(|radial-gradient|conic-gradient' concept-c && ! grep -rI --exclude-dir=vendor 'view-transition-name' concept-c && node --input-type=module -e "import('./concept-c/assets/js/scene.js').then(()=>console.log('ok'),e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>copy-diff --all concept-c exits 0; every brand grep clean with the gradient audit confirming only the stepped utility; link integrity passes across all 4 pages; >=16 non-trivial captures exist and were visually reviewed (responsive, mid-dolly, no-webgl, reduced-motion frozen, JS-off) with all issues fixed; functional scroll/deep-link/back/focus checks recorded.</done>
</task>

</tasks>

<verification>
- python3 qa/copy-diff.py --all concept-c exits 0
- Full 8-grep brand suite clean (vendor-excluded); gradient audit shows only the duplicated-stop stepped utility
- Reduced-motion (emulated): frozen composed 3D overview at DPR 1, no dolly/parallax, instant anchor jumps
- Low-tier heuristic verified in code review: hardwareConcurrency primary, deviceMemory bonus-only, viewport fallback; antialias/DPR/steps/parallax all degrade
- >=16 reviewed captures in the phase captures/ directory; link integrity zero failures
</verification>

<success_criteria>
- CONC-04 complete: the DOM mirror stands alone (JS-off captures prove it), reduced-motion gets the frozen 3D scene, no-WebGL gets the stepped constellation — all verified by capture
- CONC-05 complete: device-tiered construction (antialias, DPR, step count, parallax) with a heuristic that works outside Chromium; render loop provably idle past the hero zone and on hidden tabs
- Concept C is ready for Phase 5's cross-concept QA and gallery packaging
</success_criteria>

<output>
After completion, create `.planning/phases/04-concept-c-experimental-webgl-3d/04-03-SUMMARY.md`
</output>
