---
phase: 04-concept-c-experimental-webgl-3d
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - concept-c/index.html
  - concept-c/assets/css/concept-c.css
  - concept-c/pages/interceptos.html
  - concept-c/pages/work.html
  - concept-c/pages/insights.html
autonomous: true
requirements: [CONC-01, CONC-02]

must_haves:
  truths:
    - "Below the 3D zone the homepage continues into standard opaque DOM: #problems and #labs verbatim teasers, the #convert block, and the footer — every rendered text node data-copy annotated, copy-diff exit 0"
    - "Activating the Problems/Labs/Contact labels (and the topbar CTA) smooth-scrolls to the matching below-fold section — instant when prefers-reduced-motion is set; InterceptOS/Work/Insights labels navigate to three real sub-pages"
    - "interceptos.html renders all 4 flows x 4 stages + the 13-agent roster; work.html renders all 3 cases consolidated; insights.html renders all 3 episodes with per-episode links — all verbatim, all with a persistent way back, none loading any WebGL/three.js"
    - "Cross-document navigations get the @view-transition cross-fade where supported (no view-transition-name anywhere) and plain navigation elsewhere"
  artifacts:
    - path: "concept-c/index.html"
      provides: "Filled #problems/#labs/#convert sections + footer, all data-copy one-element-per-line"
      contains: "data-copy=\"labs.h2_html\""
    - path: "concept-c/pages/interceptos.html"
      provides: "Full InterceptOS sub-page (flows, stages, agents roster), inline lockup, back link"
      min_lines: 200
    - path: "concept-c/pages/work.html"
      provides: "Consolidated 3-case work sub-page"
      min_lines: 100
    - path: "concept-c/pages/insights.html"
      provides: "NEW layout-only episodes index (3 episodes, per-episode Spotify/Apple/YouTube links)"
      min_lines: 80
  key_links:
    - from: "a.topic-label[data-topic] hrefs"
      to: "concept-c/pages/*.html + #problems/#labs/#convert"
      via: "locked routing map from 04-01"
      pattern: "pages/interceptos.html"
    - from: "labs teaser CTA"
      to: "#convert"
      via: "labs.cta.label rendered with href rewritten to #convert (NEVER the literal #pitchLabs — routing hook, not copy)"
      pattern: "href=\"#convert\""
    - from: "concept-c/assets/css/concept-c.css"
      to: "cross-document View Transitions"
      via: "@view-transition { navigation: auto } + scroll-behavior smooth inside prefers-reduced-motion: no-preference"
      pattern: "@view-transition"
---

<objective>
Complete Concept C's content layer: fill the three below-fold homepage sections (problems teaser, labs teaser, convert block) plus the footer, and build the three derived sub-pages (interceptos.html, consolidated work.html, NEW insights.html) — all verbatim from content/homepage.json, wired with @view-transition cross-fades and reduced-motion-safe smooth scrolling.

Purpose: completes CONC-01 (standard performant DOM below the fold, 3D stays in the hero zone) and CONC-02 (every label's destination is real — no dead ends, no invented routes).
Output: filled index.html below-fold, 3 sub-pages, extended concept-c.css.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-concept-c-experimental-webgl-3d/04-CONTEXT.md
@.planning/phases/04-concept-c-experimental-webgl-3d/04-RESEARCH.md (Content Data Shapes & Routing Map section is the authoritative ref list + trap map for every section/page in this plan)
@content/homepage.json
@shared/README.md

Idiom references (READ for patterns, never reference at runtime):
@concept-b/pages/work.html (the working consolidated-3-case annotation idiom — cases loop, metric object expansion, variable-length results lists, singular .agents string)
@concept-b/pages/interceptos.html (flows/stages/agents annotation idiom incl. the absent stage-3 .agents guard)
@concept-a/index.html (footer idiom: data-copy tagline + link-label lists)

<interfaces>
<!-- Contracts inherited from 04-01 — extend, do not rename -->
- Stub sections exist: `<section id="problems">`, `<section id="labs">`, `<section id="convert">` inside `main.below-fold` (opaque var(--page), z-index above canvas) — fill them in place, keep the ids
- Topbar CTA already points at #convert; label-field hrefs already follow the locked routing map — this plan makes every destination real
- Mode classes / runway / label / index CSS from 04-01 must remain untouched in behavior; add new rules, don't rewrite existing ones
- Sub-pages carry NO scene.js, NO three.js, NO canvas — the "3D as nav, DOM as content" split is the point (CONC-01)

Data traps (verified against content/homepage.json — from 03/04-RESEARCH):
- problems teaser refs: problems.eyebrow/h2/lead + items.{0-3}.tabEyebrow + items.{0-3}.tabName ONLY (no quote/attrib/tells/signalNum/bridge — there is no full Problems page in this concept)
- labs: label, h2_html (HTML fragment — data-copy on the wrapper, keep the inner <span class="hl">), body, cta OBJECT (.label rendered; .href REWRITTEN to #convert), stat OBJECT (.num/.label)
- convert: eyebrow (already in topbar), h2, lead, cta OBJECT {heading, sub, href} — render .heading/.sub as a non-link block (href "#convoDrawer" has no real target in this prototype; 03-02 precedent)
- footer: tagline + site_links[5] + trust_links[3] + social_links[3] (all string arrays, each item data-copy'd by index); site_links map to real concept-c routes (InterceptOS->pages/interceptos.html, Intercept Labs->#labs, Work->pages/work.html, Insights->pages/insights.html, Contact->#convert); trust/social labels render as plain text items — never invented hrefs
- os.flows[i].stages[3] ("Outcome") has NO .agents key — guard the loop
- work.cases[i].agents is a SINGLE STRING despite the plural name — one text node, never iterate
- work.cases[i].results is variable length (4, 4, 5) — per-index expansion
- insights.episodes[i]: episode/show/title/guest.name/guest.role/summary + links ARRAY[3] of {label, href} (label data-copy'd, href used verbatim as the real external link) + tile_href routing hook; full_refs is [] by design — insights.html is a LAYOUT-ONLY upgrade, never new copy
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Below-fold homepage sections + footer + routing/transition CSS</name>
  <files>concept-c/index.html, concept-c/assets/css/concept-c.css</files>
  <action>
Generate the section markup with a one-off uncommitted python script that reads content/homepage.json and writes the finished HTML into the stub sections — rendered text and its data-copy dot-path derived together (02-02/03-02 technique: 430/430 and 426/426 first-run passes). Emit ONE data-copy element per line (03-02 lesson: multi-attribute lines break numeric grep verification).

**#problems** — eyebrow (mono kicker), h2, lead, then the 4 items as a quiet grid: items.{i}.tabEyebrow (mono) + items.{i}.tabName. Teaser only — no deeper refs exist for this concept.

**#labs** — label (mono kicker), h2_html (wrapper-level data-copy, preserve the inner `<span class="hl">`; style .hl in var(--flarepop) — permitted colored text), body, CTA as `<a href="#convert" data-copy="labs.cta.label">Build with Labs</a>` (href rewritten per trap map), stat as num (mono data size) + label.

**#convert** — h2, lead, then the conversion tile as a NON-LINK block: cta.heading + cta.sub (03-02 precedent — no dead link, no invented route). The topbar CTA and the Contact label land here via smooth scroll.

**footer** — inline lockup NOT needed here (topbar persists); render footer.tagline (data-copy), site_links as a nav with the 5 real hrefs from the interfaces map, trust_links + social_links as plain-text mono lists (each item data-copy'd `footer.trust_links.0` etc., no hrefs). No border-top separator — space/weight only.

**CSS additions:** `@view-transition { navigation: auto }` at the top of concept-c.css (cross-fade only — view-transition-name stays banned). `@media (prefers-reduced-motion: no-preference) { html { scroll-behavior: smooth; } }` — anchor jumps are instant under reduced motion with zero JS. Below-fold typography: dark, quiet, editorial — generous space, mono kickers, oversized h2s, max-width prose; sections keep `background: var(--page)` opaque (the render loop stops behind them). No borders, no hairlines, no new gradients.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 qa/copy-diff.py concept-c/index.html && [ "$(grep -o 'data-copy=' concept-c/index.html | wc -l | tr -d ' ')" -ge 30 ] && grep -q '@view-transition' concept-c/assets/css/concept-c.css && grep -q 'scroll-behavior' concept-c/assets/css/concept-c.css && grep -q 'data-copy="labs.cta.label"' concept-c/index.html && ! grep -q 'pitchLabs' concept-c/index.html && ! grep -q 'convoDrawer' concept-c/index.html && ! grep -rIiE --exclude-dir=vendor 'fresh thinking starts here' concept-c && ! grep -rIiE --exclude-dir=vendor 'a855f7|6366f1|22d3ee' concept-c && ! grep -rIi --exclude-dir=vendor '<hr' concept-c && ! grep -rIE --exclude-dir=vendor 'border-(top|bottom)[[:space:]]*:' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*#' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])linear-gradient\(|radial-gradient|conic-gradient' concept-c && ! grep -rI --exclude-dir=vendor 'view-transition-name' concept-c && node --input-type=module -e "import('./concept-c/assets/js/scene.js').then(()=>console.log('ok'),e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>All three below-fold sections + footer carry verbatim, per-line-annotated copy (>=30 chunks on the homepage); copy-diff exits 0; labs CTA routes to #convert with no literal #pitchLabs/#convoDrawer anywhere; view-transition + reduced-motion-safe smooth scroll live in CSS; brand greps clean; scene.js untouched and still parsing.</done>
</task>

<task type="auto">
  <name>Task 2: Three sub-pages — interceptos.html, work.html (consolidated), insights.html (new)</name>
  <files>concept-c/pages/interceptos.html, concept-c/pages/work.html, concept-c/pages/insights.html, concept-c/assets/css/concept-c.css</files>
  <action>
Same generator technique: one-off uncommitted python script(s) looping directly over homepage.json's arrays so text + dot-path derive together, one data-copy element per line. Each page: quiet, typographic, dark (locked) — shared head idiom (has-js line optional but harmless, tokens/fonts/motion + concept-c.css), a slim topbar with the INLINE canonical lockup SVG (copy the markup from concept-c/index.html — currentColor lesson) linking back to /concept-c/ plus a mono "← Back" affordance, and NO script tags, NO canvas, NO three.js — zero WebGL on sub-pages (CONC-01 split).

**interceptos.html** — read concept-b/pages/interceptos.html for the annotation idiom only. Render os framing + all 4 flows: tabLabel/job/layer + the 4-stage grid per flow (tag/name/desc/agents — GUARD stage 3 "Outcome" which has no .agents key). Then the agents roster: agents.categories[4] + all 13 agents.items (name/type/role/desc/solves[1-2 per-index]/sample). The unannotated structural labels (Challenge/Approach-style field names, stage column headings) get the single HTML-comment note marking them as content-model field names, not brand copy.

**work.html** — consolidated, all 3 cases stacked (concept-b/pages/work.html is the proven idiom): client/tag/name/summary + metric.num (mono data size, the page's stat moments) + metric.label, challenge, approach, results as <li> per-index (4/4/5 items), agents as ONE text node. Case status/metric numerals in mono; no colored text beyond any Flarepop accents already licensed.

**insights.html** — NEW page, layout-only depth upgrade (full_refs is [] by design — zero new copy). Render insights.eyebrow/h2/lead, then one full-width section per episode: episode + show (mono kickers), title (oversized h3), guest.name + guest.role, summary, and the 3 links as real external `<a href>` using links[i].href verbatim with data-copy'd labels (these are the live Spotify/Apple/YouTube hrefs — locked Phase-01 decision).

**CSS:** extend concept-c.css with a `.subpage` layer (shared page scaffold, prose measures, episode/case/flow layouts). Reuse the existing token vocabulary; no borders, no hairline dividers, no gradients; view-transition cross-fade comes free from Task 1's at-rule since all pages share the stylesheet.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 qa/copy-diff.py concept-c/index.html concept-c/pages/interceptos.html concept-c/pages/work.html concept-c/pages/insights.html && [ "$(grep -o 'data-copy=' concept-c/pages/interceptos.html | wc -l | tr -d ' ')" -ge 150 ] && [ "$(grep -o 'data-copy=' concept-c/pages/work.html | wc -l | tr -d ' ')" -ge 40 ] && [ "$(grep -o 'data-copy=' concept-c/pages/insights.html | wc -l | tr -d ' ')" -ge 25 ] && ! grep -rI 'vendor/three' concept-c/pages && ! grep -rIi '<canvas' concept-c/pages && [ "$(grep -l 'lockup-svg' concept-c/pages/*.html | wc -l | tr -d ' ')" -eq 3 ] && ! grep -rIiE --exclude-dir=vendor 'fresh thinking starts here' concept-c && ! grep -rIiE --exclude-dir=vendor 'a855f7|6366f1|22d3ee' concept-c && ! grep -rIi --exclude-dir=vendor '<hr' concept-c && ! grep -rIE --exclude-dir=vendor 'border-(top|bottom)[[:space:]]*:' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*#' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])linear-gradient\(|radial-gradient|conic-gradient' concept-c && ! grep -rI --exclude-dir=vendor 'view-transition-name' concept-c && node --input-type=module -e "import('./concept-c/assets/js/scene.js').then(()=>console.log('ok'),e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <done>All 3 sub-pages exist with verbatim annotated copy at the expected densities (>=150/>=40/>=25 chunks), inline lockups, persistent back navigation, and zero WebGL; copy-diff exits 0 across all 4 concept-c pages; the full grep suite stays clean.</done>
</task>

</tasks>

<verification>
- python3 qa/copy-diff.py --all concept-c exits 0 (index + 3 sub-pages, annotated mode)
- Every label-field destination resolves: 3 real files, 3 real in-page anchors; labs CTA and topbar CTA land on #convert
- Sub-pages load with no three.js request and no canvas; back navigation present on all 3
- @view-transition cross-fade wired (Firefox degrades to plain navigation — expected, no code needed)
</verification>

<success_criteria>
- CONC-01 complete: 3D stays in the hero zone; everything below the fold and on sub-pages is standard, opaque, performant DOM
- CONC-02 complete: every hotspot label routes somewhere real — sub-page navigations and in-page smooth scrolls, no dead ends, no invented routes
</success_criteria>

<output>
After completion, create `.planning/phases/04-concept-c-experimental-webgl-3d/04-02-SUMMARY.md`
</output>
