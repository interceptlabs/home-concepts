---
phase: 08-concept-d-iteration-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - concept-d/pages/explore/problems.html
  - concept-d/pages/explore/interceptos.html
  - concept-d/pages/explore/agents.html
  - concept-d/pages/explore/labs.html
  - concept-d/pages/explore/insights.html
  - concept-d/pages/explore/case-hp-abx.html
  - concept-d/pages/explore/case-intel-abm.html
  - concept-d/pages/explore/case-sap-video.html
  - concept-d/index.html
  - concept-d/assets/css/concept-d.css
  - concept-d/assets/js/quiet-modules.js
  - concept-d/assets/js/cards.js
autonomous: true
requirements: [IT3-02]

must_haves:
  truths:
    - "Clicking any of the 8 homepage cards (5 sections + 3 cases) navigates to a standalone quiet explore page — with JS enabled AND with JS disabled (cards are plain anchors)"
    - "Every explore page shows a prominent mono '← Back to home' affordance as the first focusable element inside <main>, and it returns to the homepage"
    - "No module <dialog> remains in concept-d/index.html; the Labs pitch drawer and Agents detail-contact drawer still work ON the explore pages that need them"
    - "The quiet module content renders on each explore page from deployed.js's untouched data objects — both copy gates stay green"
  artifacts:
    - path: "concept-d/pages/explore/problems.html"
      provides: "Quiet Problems page (q-tabs + #quietSolve render)"
      min_lines: 60
    - path: "concept-d/pages/explore/interceptos.html"
      provides: "Quiet InterceptOS page (q-tabs + #quietFlow stepper)"
      min_lines: 60
    - path: "concept-d/pages/explore/agents.html"
      provides: "Agent roster page (deployed renderAgents grid + detail overlay + glyph defs)"
      min_lines: 80
    - path: "concept-d/pages/explore/labs.html"
      provides: "Quiet Labs page with working pitchLabs drawer CTA"
      min_lines: 60
    - path: "concept-d/pages/explore/insights.html"
      provides: "Quiet Insights page (3 ep-tiles + q-more details)"
      min_lines: 80
    - path: "concept-d/pages/explore/case-hp-abx.html"
      provides: "HP case page (.q-case-detail render)"
      min_lines: 60
    - path: "concept-d/pages/explore/case-intel-abm.html"
      provides: "Intel case page (.q-case-detail render)"
      min_lines: 60
    - path: "concept-d/pages/explore/case-sap-video.html"
      provides: "SAP case page (.q-case-detail render)"
      min_lines: 60
    - path: "concept-d/index.html"
      provides: "Homepage with 8 <a class=card> navigations, zero <dialog>, no cards.js/quiet-modules.js script tags"
    - path: "concept-d/assets/css/concept-d.css"
      provides: ".explore-page-scoped quiet styles (dialog.module-modal scope retired), @view-transition navigation:auto, .explore-back chrome"
  key_links:
    - from: "concept-d/index.html"
      to: "concept-d/pages/explore/*.html"
      via: "8 card anchors"
      pattern: "href=\"pages/explore/"
    - from: "concept-d/pages/explore/*.html"
      to: "concept-d/assets/js/deployed.js + quiet-modules.js"
      via: "classic script tags, deployed.js FIRST (data objects), quiet-modules.js second"
      pattern: "\\.\\./\\.\\./assets/js/deployed\\.js"
    - from: "concept-d/pages/explore/*.html"
      to: "concept-d/index.html"
      via: "explore-back anchor, first focusable in main"
      pattern: "explore-back"
    - from: "concept-d/assets/js/quiet-modules.js"
      to: "concept-d/pages/explore/agents.html"
      via: "InterceptOS bridge link retargeted from #agents to agents.html"
      pattern: "agents\\.html"
---

<objective>
Convert Concept D's homepage from 8 module dialogs to 8 real page navigations (IT3-02): build the 8 standalone quiet explore pages under `concept-d/pages/explore/`, turn every homepage card into a plain `<a href>`, retire the dialogs / morph JS / no-JS fallback anchors, and give the navigation a cross-document View Transition cross-fade.

Purpose: Jon's iteration-3 direction — "The cards should open up a new page not a modal window, but with a clear way to get back to the homepage."
Output: 8 new explore pages, a dialog-free index.html, re-scoped quiet CSS, retargeted quiet-modules.js, cards.js deleted.
</objective>

<checker_note>Task 1 MUST also rewrite the prose doc-comment (~line 490-493 of concept-d.css) that mentions `dialog.module-modal` — otherwise Task 1's own `grep -c 'dialog\.module-modal' = 0` gate can never pass. Task 1's per-page verify should also assert `quiet-modules.js` script presence on each of the 5 section explore pages.</checker_note>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-concept-d-iteration-3/08-CONTEXT.md
@.planning/phases/07-concept-d-iteration-2/07-02-SUMMARY.md
@concept-d/index.html
@concept-d/assets/js/quiet-modules.js
@concept-d/assets/js/cards.js
@concept-d/assets/css/concept-d.css
@concept-d/pages/labs.html

<interfaces>
<!-- Everything the executor needs — verified against the live codebase. No exploration required. -->

**deployed.js has UNGUARDED top-level DOM references — every page that loads it MUST carry the full drawer scaffold or the script throws mid-execution:**
```js
// deployed.js line 267:  const scrim = document.getElementById('scrim');  → line 311: scrim.addEventListener(...)
// deployed.js line 400:  document.getElementById('pitchForm').addEventListener('submit', ...)
// deployed.js line 416:  document.getElementById('convoForm').addEventListener('submit', ...)
```
→ Transplant index.html's own scaffold block (lines ~650–711: `#scrim` div + `#convoDrawer` aside with `#convoForm`/`#convoSuccess`/`#convoType`/`#convoContext`/`#convoContextField` + `#pitchLabs` aside with `#pitchForm`/`#pitchSuccess`) verbatim into EVERY explore page, before the script tags. This is the proven-sufficient set (index.html loads deployed.js with exactly this scaffold).

**deployed.js load-time renders (05-03 null guards make missing hooks silent no-ops):**
- `renderAgents('strategy')` → needs `#agentsGrid` — present only on agents.html (deployed markup)
- `buildSolve()`/`renderFlow()` → `#solveDetail`/`#probFlow` absent everywhere in explore pages — guarded no-ops
- `themeToggle` IIFE is guarded (`if(!b)return`) — include the toggle button in the header like other pages
- `agentDetailContact` click → `closeAgentDetail()` + `openConvo(...)` → opens `#convoDrawer` (guarded via if(agentContactBtn))

**quiet-modules.js hooks (all null-guarded — each page carries only its own):**
- problems.html: `#qProblemsTabs` (4 .q-tab buttons) + `<div id="quietSolve" aria-live="polite">`
- interceptos.html: `#qOsTabs` (4 .q-tab buttons) + `<div id="quietFlow" aria-live="polite">`
- case-*.html: `<div class="q-case-detail" data-key="hp-abx|intel-abm|sap-video">`
- agents/labs/insights: NO quiet-modules hooks (agents = deployed renderAgents; labs/insights = static markup)
- Script order is load-bearing: deployed.js (defines `const PROBLEMS_RR/PROBLEM_FLOWS/CASES`) BEFORE quiet-modules.js (reads them as bare globals from a later classic script)

**Card → page mapping (8 anchors):**
| Homepage card (data-modal today) | New href |
|---|---|
| dlg-problems | pages/explore/problems.html |
| dlg-os | pages/explore/interceptos.html |
| dlg-agents | pages/explore/agents.html |
| dlg-labs | pages/explore/labs.html |
| dlg-insights | pages/explore/insights.html |
| dlg-case-hp-abx | pages/explore/case-hp-abx.html |
| dlg-case-intel-abm | pages/explore/case-intel-abm.html |
| dlg-case-sap-video | pages/explore/case-sap-video.html |

**Relative paths from pages/explore/ (two levels deep):**
- Assets: `../../assets/css/deployed.css`, `../../assets/css/concept-d.css`, `../../assets/js/deployed.js`, `../../assets/js/quiet-modules.js`, case images `../../assets/img/case-*.png`
- Header nav (copy pages/labs.html's topbar, fix depth): lockup → `../../index.html`, InterceptOS → `../os.html`, Intercept Labs → `../labs.html`, Work → `../work.html`, About → `../../about.html`, Insights → `../../insights-hub.html`, Contact → `../contact.html`
- Insights archive link: `../../insights-hub.html#episodes` (#episodes is a verified JS-hash-route — keep it in the link checker's allowlist)

**Known landmines (07-02 lessons — do not re-learn):**
- `#os { background: var(--band-blue) }` in deployed.css is UNCONDITIONAL — the interceptos page's `section id="os"` sits on a dark navy band that redefines `--fg` to white while leaving `--surface*` light-theme white. The quiet q-tab/q-step/q-flow-chip components are border+color-only for exactly this reason. NEVER add a `var(--surface*)` background fill to them.
- The Agents pale-pink wash override needs a `:root[data-theme="light"]`-prefixed selector to win the cascade — when re-scoping, keep the prefix: `:root[data-theme="light"] .explore-page .agent-card-v6 { ... }`
- Labs has TWO separate hairlines to keep neutralized: `.labs` AND `.labs-stats` border-top overrides
- agents.html needs the 13 `glyph-*` SVG symbol defs (the grid renders `<use href="#glyph-...">`) — copy the hidden symbol-defs `<svg>` block from pages/os.html (it carries all 13 glyphs + intercept-lockup symbol)
- Literal-substring verify gates collide with HTML comments (07-01/07-02 precedent) — keep new comments free of gate-matched substrings

**Cross-document View Transitions precedent (concept-a/b/c, proven):**
```css
@view-transition { navigation: auto; }
```
Added to concept-d.css (loaded by index + all explore pages — both sides of a navigation need it). The existing reduced-motion safety net (`::view-transition-group(*) … animation: none !important`) already covers cross-document transitions — KEEP it. The `::view-transition-group(modal-morph)` trio retires with the morph.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS/JS groundwork + 5 quiet section explore pages</name>
  <files>concept-d/assets/css/concept-d.css, concept-d/assets/js/quiet-modules.js, concept-d/pages/explore/problems.html, concept-d/pages/explore/interceptos.html, concept-d/pages/explore/agents.html, concept-d/pages/explore/labs.html, concept-d/pages/explore/insights.html</files>
  <action>
**concept-d.css:**
1. Re-scope every `dialog.module-modal`-prefixed selector to `.explore-page` (the quiet component styles: .q-tabs/.q-tab/.q-problem/.q-flow-*/.q-stepper/.q-step*/.q-case-*/.q-more/ep-tile quieting, the Labs `.labs`+`.labs-stats` hairline neutralizations, the Agents `:root[data-theme="light"] … .agent-card-v6` wash override WITH its prefix, the section-padding override, the reduced-motion transition-none block entries). Also re-scope `dialog.module-modal section {…}` → `.explore-page section {…}`.
2. DELETE modal-shell-only rules: the `dialog.module-modal` box/positioning shell, `dialog.module-modal::backdrop`, `.modal-close` styles, `.modal-body` sizing, and the `::view-transition-group(modal-morph)`/`::view-transition-old(modal-morph)`/`::view-transition-new(modal-morph)` trio. KEEP the `@media (prefers-reduced-motion: reduce)` `::view-transition-*(*) { animation: none !important; }` safety net — it now guards the cross-document cross-fade.
3. ADD `@view-transition { navigation: auto; }` (concept-a/b/c precedent) near the top with a one-line comment.
4. ADD `.explore-back` chrome: mono (`var(--font-mono)`), ~13px, uppercase, letter-spacing ~0.08em, `color: var(--fg)`, no underline, hover `color: var(--flarepop)`, `:focus-visible` outline 2px solid var(--fg) offset 2px, display:inline-block with comfortable margin below the topbar (e.g. `margin: 28px 0 8px`). ADD a slim `.explore-foot` treatment if needed (padding + the existing `.curio` styling from deployed.css may suffice — no rule lines, no new colored text).
NOTE: after this task's commit the homepage dialogs are temporarily unskinned (they still open via cards.js) — Task 2 removes them entirely; this transient is intentional and confined to this plan.

**quiet-modules.js:** retarget the InterceptOS stepper bridge link from `<a href="#agents">` to `<a href="agents.html">` (the file now runs only on explore pages, all siblings in pages/explore/). Keep the link text byte-identical. Update the file-header comment to say it runs on the explore pages (avoid writing any gate-matched literal like a dialog id).

**5 section pages** at concept-d/pages/explore/{problems,interceptos,agents,labs,insights}.html, each built as:
- `<head>`: theme-IIFE script + has-js script + fonts links + `../../assets/css/deployed.css` + `../../assets/css/concept-d.css` (copy pages/labs.html's head, fix depth); title pattern "{Module name} — Intercept" (chrome, not canonical copy).
- `<header class="topbar">` copied from pages/labs.html with hrefs fixed per the interfaces table (lockup → ../../index.html; include the themeToggle button and the Contact cta-nav).
- agents.html ONLY: the hidden SVG symbol-defs block from pages/os.html (13 glyph-* symbols) after the header.
- `<main id="main" class="explore-page">`: FIRST element is `<a class="explore-back" href="../../index.html">&larr; Back to home</a>`, then the module section transplanted from the corresponding index.html dialog's `.modal-body` contents (the `<section id="problems">`, `<section id="os">`, `<section id="agents">`, `<section id="labs">`, `<section id="insights">` blocks) — every text node byte-identical; only fix relative hrefs (insights archive link → `../../insights-hub.html#episodes`; ep-tile external links unchanged).
- Slim `<footer>`: `<div class="wrap"><div class="foot-bot"><span class="curio">Powered by curiosity.</span></div></div>` — deliberately no site-link columns (avoids replicating the mirrored dead-link allowlist).
- Drawer scaffold verbatim from index.html (#scrim + #convoDrawer + #pitchLabs with all form ids) — REQUIRED on every page, deployed.js has unguarded references (see interfaces).
- Scripts: `../../assets/js/deployed.js` then `../../assets/js/quiet-modules.js`.
Labs page: keep the `data-open="pitchLabs"` CTA button (works via deployed.js). Agents page: keep the full agents-stage/detail-overlay markup (deployed.js renders and wires it).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && node --check concept-d/assets/js/quiet-modules.js && grep -q 'agents.html' concept-d/assets/js/quiet-modules.js && [ "$(grep -c 'dialog\.module-modal' concept-d/assets/css/concept-d.css)" = "0" ] && grep -q '@view-transition' concept-d/assets/css/concept-d.css && for f in problems interceptos agents labs insights; do p="concept-d/pages/explore/$f.html"; grep -q 'explore-back' "$p" && grep -q 'id="scrim"' "$p" && grep -q 'id="pitchForm"' "$p" && grep -q 'id="convoForm"' "$p" && grep -q 'assets/js/deployed.js' "$p" || exit 1; done && grep -q 'glyph-watchtower' concept-d/pages/explore/agents.html && python3 qa/copy-diff.py --mode substring concept-d/pages/explore/problems.html concept-d/pages/explore/interceptos.html concept-d/pages/explore/agents.html concept-d/pages/explore/labs.html concept-d/pages/explore/insights.html</automated>
  </verify>
  <done>5 section explore pages exist with deployed header, back link, scaffold, slim footer, and byte-identical module copy (substring copy-diff exits 0); concept-d.css carries zero dialog.module-modal selectors, has @view-transition and .explore-back chrome; quiet-modules.js bridge points at agents.html and parses.</done>
</task>

<task type="auto">
  <name>Task 2: 3 case pages + homepage cards→anchors conversion (dialogs retire)</name>
  <files>concept-d/pages/explore/case-hp-abx.html, concept-d/pages/explore/case-intel-abm.html, concept-d/pages/explore/case-sap-video.html, concept-d/index.html, concept-d/assets/js/cards.js, concept-d/assets/css/concept-d.css</files>
  <action>
**3 case pages** (same shell as Task 1's pages — header, back link, slim footer, scaffold, deployed.js + quiet-modules.js): transplant each `dlg-case-*` dialog's `.modal-body` `<section class="case-window">` verbatim; fix `case-image` src to `../../assets/img/case-*.png`; keep the `.q-case-detail[data-key]` hook div (quiet-modules.js fills it). The three pages may share one template with per-case content (Claude's discretion per CONTEXT).

**concept-d/index.html conversion:**
1. Replace each of the 8 `<button type="button" class="card" data-modal="…" aria-haspopup="dialog">` with `<a class="card" href="pages/explore/….html">` per the interfaces mapping table — inner markup (eyebrow/card-bottom/heading/teaser/CTA) unchanged EXCEPT the CTA label: change `Open&nbsp;+` → `View&nbsp;+` on all 8 cards (discretion exercised per CONTEXT: neutral chrome wording that reads as a visit affordance).
2. DELETE the 8 duplicate no-JS elements (`<div class="card card-static">` and the 7 `<a class="card card-fallback">`) — cards ARE anchors now, no-JS works by construction.
3. DELETE all 8 `<dialog class="module-modal">…</dialog>` blocks (lines ~315–622).
4. Retarget the FAQ answer's `<a href="#labs">Read more.</a>` → `<a href="pages/explore/labs.html">Read more.</a>` (#labs no longer exists on this page; link text untouched).
5. Remove the `<script src="assets/js/cards.js">` and `<script src="assets/js/quiet-modules.js">` tags (deployed.js + hero-video.js + reel-video.js remain). Keep the drawer scaffold (convert section's convo-tile still opens #convoDrawer; deployed.js needs #pitchForm present).
6. Update the stale "DRAWER SCAFFOLDS (reparented by cards.js…)" comment.

**Delete concept-d/assets/js/cards.js** (`git rm`) — nothing needs it: dialogs are gone, drawers on explore pages are top-level exactly like the deployed site (deployed.js's own document-level Escape/scrim handling applies; the 07-02 drawer-first-Esc/reparenting machinery existed only because drawers lived inside dialogs).

**concept-d.css dead-path cleanup:** remove the `html.has-js .card-fallback, html.has-js .card-static { … }` / `html:not(.has-js) .card[data-modal] { … }` no-JS switching rules and any other `[data-modal]`/`.card-fallback`/`.card-static` selectors; keep the no-JS contrast rules that reference still-existing elements (e.g. video-toggle hiding). Do not touch card resting/hover values (08-02 owns hover).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && ! grep -q '<dialog' concept-d/index.html && [ "$(grep -o 'href="pages/explore/' concept-d/index.html | wc -l | tr -d ' ')" = "9" ] && ! grep -q 'data-modal' concept-d/index.html && ! grep -q 'card-fallback' concept-d/index.html && ! grep -q 'card-static' concept-d/index.html && ! grep -q 'cards.js' concept-d/index.html && ! grep -q 'quiet-modules.js' concept-d/index.html && [ ! -f concept-d/assets/js/cards.js ] && ! grep -q 'data-modal' concept-d/assets/css/concept-d.css && for f in case-hp-abx case-intel-abm case-sap-video; do p="concept-d/pages/explore/$f.html"; grep -q 'explore-back' "$p" && grep -q 'q-case-detail' "$p" && grep -q 'id="scrim"' "$p" || exit 1; done && python3 qa/copy-diff.py --mode substring concept-d/index.html concept-d/pages/explore/case-hp-abx.html concept-d/pages/explore/case-intel-abm.html concept-d/pages/explore/case-sap-video.html</automated>
  </verify>
  <done>index.html has 9 pages/explore/ hrefs (8 cards + retargeted FAQ link), zero dialogs, zero data-modal/fallback/static remnants, and no cards.js/quiet-modules.js tags; cards.js is deleted; 3 case pages exist with back links + case-detail hooks; substring copy-diff exits 0 on all 4 changed/new HTML files.</done>
</task>

<task type="auto">
  <name>Task 3: Plan gates — copy, script-diff, link integrity, brand greps</name>
  <files>concept-d/index.html, concept-d/pages/explore/problems.html</files>
  <action>
Run the full mechanical gate set for the navigation restructure; fix any failures in this plan's own files (expected file edits: none — the files listed are the likely fix targets if a gate fails).

1. **Copy gates:** `python3 qa/copy-diff.py --mode substring` across concept-d/index.html + all 8 explore pages (one invocation); `python3 qa/concept-d-script-diff.py` (deployed.js must remain byte-identical on its data objects — this plan never edits deployed.js).
2. **Link integrity:** write a one-off stdlib-Python checker in the scratchpad (05-03/07-03 precedent — not committed): scan every concept-d/*.html + concept-d/pages/*.html + concept-d/pages/explore/*.html for href/src, resolve relative paths against each file's location, verify same-page fragment targets exist, and verify the 8 card hrefs + 8 back links round-trip. Documented allowlist (carry 07-03's entries): `insights-hub.html#episodes` (verified JS-hash-route — now also referenced from pages/explore/insights.html), `about.html` `#main` (pre-existing staging bug, logged in 05's deferred-items), and the mirrored deployed footer links on PRE-EXISTING pages only (ai-policy/privacy-policy/terms-of-service). New explore pages must contribute ZERO new allowlist entries (their slim footer has no site links). Exit 0 required.
3. **Brand greps on new chrome** (the 8 explore pages + this phase's concept-d.css additions; deployed.css/deployed.js + ported module markup exempt): (a) no decorative rule lines — comment-aware Python check (07-03 lesson: literal grep false-positives on comments) asserting no NEW `border-top`/`border-bottom` declaration with a visible width in the `.explore-back`/`.explore-foot`/explore-shell rules; (b) no smooth gradients — `! grep -E 'linear-gradient|radial-gradient' concept-d/pages/explore/*.html` and Python-confirm no new gradient declarations were added to concept-d.css this phase; (c) Flarepop-only colored text — new chrome colors limited to var(--fg)/var(--fg-2/3)/var(--flarepop); (d) banned tagline absent: `! grep -ri 'Fresh thinking starts here' concept-d/pages/explore/`.
4. **Structural sanity:** `@view-transition` present exactly once in concept-d.css; each explore page's first `<a>` inside `<main>` is the back link (Python: parse each file, assert the first anchor after `<main` has class explore-back).
5. If (and only if) any gate fails, fix the offending file from this plan's files_modified set and re-run until all green.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 qa/copy-diff.py --mode substring concept-d/index.html concept-d/pages/explore/problems.html concept-d/pages/explore/interceptos.html concept-d/pages/explore/agents.html concept-d/pages/explore/labs.html concept-d/pages/explore/insights.html concept-d/pages/explore/case-hp-abx.html concept-d/pages/explore/case-intel-abm.html concept-d/pages/explore/case-sap-video.html && python3 qa/concept-d-script-diff.py && [ "$(grep -c '@view-transition' concept-d/assets/css/concept-d.css)" = "1" ] && ! grep -riq 'Fresh thinking starts here' concept-d/pages/explore/ && ! grep -Eq 'linear-gradient|radial-gradient' concept-d/pages/explore/*.html && git diff --quiet concept-d/assets/js/deployed.js concept-d/assets/css/deployed.css</automated>
  </verify>
  <done>Both copy gates green on index + all 8 explore pages; one-off link checker exits 0 with zero new allowlist entries; brand checks pass on all new chrome; deployed.css/deployed.js confirmed byte-untouched.</done>
</task>

</tasks>

<verification>
- `python3 qa/copy-diff.py --mode substring` exits 0 across concept-d/index.html + all 8 pages/explore/ files
- `python3 qa/concept-d-script-diff.py` exits 0 (deployed.js data objects byte-identical)
- `grep -c '<dialog' concept-d/index.html` = 0; `grep -c 'href="pages/explore/' concept-d/index.html` = 9
- cards.js absent from disk and from index.html script tags; quiet-modules.js absent from index.html, present on all 8 explore pages after deployed.js
- Every explore page carries: topbar header, `.explore-back` as first in-main focusable, drawer scaffold (#scrim/#convoDrawer/#pitchLabs), slim footer
- Link-integrity one-off passes with only the documented pre-existing allowlist entries
</verification>

<success_criteria>
IT3-02 satisfied at the code level: all 8 cards are real anchor navigations to standalone quiet pages, each page has a clear consistent way back (prominent mono back link + lockup), no module dialogs remain on the homepage, drawers still function on the explore pages that need them, and all mechanical gates are green. (Live behavior + captures are proven in 08-02.)
</success_criteria>

<output>
After completion, create `.planning/phases/08-concept-d-iteration-3/08-01-SUMMARY.md`
</output>
