---
phase: 08-concept-d-iteration-3
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - concept-d/assets/css/concept-d.css
  - assets/gallery/concept-d.png
  - REVIEW.md
  - .planning/phases/08-concept-d-iteration-3/captures/
autonomous: true
requirements: [IT3-01, IT3-03, IT3-04]

must_haves:
  truths:
    - "At 1440×900 AND 1280×800 the hero headline sits close under the sticky topbar, is visibly larger (>=44px computed at 1440), and the full 5-card grid still fits inside the first viewport"
    - "Card hover is unmistakable: fill thins to ~40% white, card lifts with a larger shadow and ~1.02 scale, the card CTA flips to Flarepop — sine-eased 300-450ms; keyboard :focus-visible gets the identical state"
    - "The work-reel section plays the v2 reel (weavy.ai SAP brand-film excerpts) when scrolled into view, stays paused under reduced-motion, and stays within budget"
    - "Full behavior pass green: card click navigates to its explore page, back link returns home, drawers work ON the explore pages, reduced-motion and no-JS paths hold"
    - "Gallery thumbnail and REVIEW.md's Concept D paragraph describe iteration 3"
  artifacts:
    - path: "concept-d/assets/css/concept-d.css"
      provides: "Top-anchored .hero-viewport, enlarged .hero-d .hero-h1 clamp, obvious .card hover/focus state at 300-450ms sine"
      contains: "flex-start"
    - path: ".planning/phases/08-concept-d-iteration-3/captures/home-1440x900.png"
      provides: "Fold proof, desktop"
    - path: ".planning/phases/08-concept-d-iteration-3/captures/home-1280x800.png"
      provides: "Fold proof, small desktop"
    - path: ".planning/phases/08-concept-d-iteration-3/captures/card-hover-1440.png"
      provides: "Obvious-hover evidence"
    - path: "assets/gallery/concept-d.png"
      provides: "Refreshed gallery thumbnail (iteration-3 homepage)"
    - path: "REVIEW.md"
      provides: "Concept D paragraph updated for iteration 3 (pages not modals, bigger hero, v2 reel)"
  key_links:
    - from: "concept-d/assets/css/concept-d.css .hero-viewport"
      to: "top-anchored layout"
      via: "justify-content flex-start"
      pattern: "justify-content:\\s*flex-start"
    - from: ".card:hover"
      to: ".card-cta Flarepop flip"
      via: "hover-scoped descendant rule"
      pattern: ":hover \\.card-cta"
    - from: "concept-d/index.html #work-reel"
      to: "assets/video/work-reel-1080.webm (v2)"
      via: "reel-video.js IntersectionObserver play"
      pattern: "work-reel-1080"
---

<objective>
Land the remaining three iteration-3 requirements and close the phase: top-anchor the hero under the sticky nav with a meaningfully bigger headline while both fold proofs still hold (IT3-01), make card hover unmistakably obvious within brand rules (IT3-03), verify the already-committed v2 work reel plays and fits budget (IT3-04), then run the full Puppeteer behavior pass + honest capture review and refresh the gallery thumbnail + REVIEW.md.

Purpose: Jon's direction — "the main headline can move up closer to the sticky [nav]. It can also be bigger… create a more obvious hover state for those cards… for the work section use the weavy.ai work we just made."
Output: tuned concept-d.css, judged captures at both fold sizes + hover + explore pages, green behavior suite, refreshed gallery/REVIEW.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-concept-d-iteration-3/08-CONTEXT.md
@.planning/phases/08-concept-d-iteration-3/08-01-SUMMARY.md
@concept-d/assets/css/concept-d.css
@concept-d/index.html
@concept-d/assets/video/ASSETS.md

<interfaces>
<!-- Current CSS values (post-08-01) the executor will change — no exploration needed. -->

**Hero (concept-d.css):**
```css
.hero-viewport {            /* currently justify-content: center */
  position: relative; z-index: 1; min-height: 100dvh;
  display: flex; flex-direction: column; justify-content: center;
  gap: 24px; padding: 32px 0 28px;
}
.hero-d .hero-h1 { font-size: clamp(26px, 3.2vw, 40px); max-width: 32ch; line-height: 1.12; }
.hero-d .hero-sub { margin-top: 12px; font-size: clamp(14px, 1vw, 15px); max-width: 63ch; }
```
Topbar: `position:sticky; top:0` with `padding:15px 28px` rows (~63px tall, deployed.css — never edited).

**Card resting/hover (concept-d.css):**
```css
.card { background: rgba(255,255,255,0.75); box-shadow: 0 12px 32px rgba(10,10,15,0.12);
        transition: transform 1.1s cubic-bezier(0.37,0,0.63,1), box-shadow 1.1s …, background-color 1.1s …; }
.card:hover, .card:focus-visible { transform: translateY(-3px); box-shadow: 0 18px 40px rgba(10,10,15,0.16); }
@media (hover: hover) and (pointer: fine) { .card:hover { background-color: rgba(255,255,255,0.5); } }
.card:focus-visible, .card:active { background-color: rgba(255,255,255,0.5); }
.card-cta { … color: var(--fg); }   /* mono uppercase chrome, "View +" after 08-01 */
```
A `@media (prefers-reduced-motion: reduce)` block already sets `.card { transition: none }` — keep it covering the new values.

**Reel v2 (already committed, verify only):** `concept-d/assets/video/work-reel-1080.webm` (2,819,131 B) / `.mp4` (4,160,815 B) / `work-reel-poster.jpg` (~144 KB). `reel-video.js` plays via IntersectionObserver when #work-reel enters view, never under reduced-motion, `preload="none"`.

**Puppeteer rig (established patterns — reuse, do not rediscover):**
- Import by absolute ESM path: `/Users/jontoewsinterceptgroup.com/node_modules/puppeteer/lib/esm/puppeteer/puppeteer.js` (no local node_modules)
- `executablePath` must point at real installed Google Chrome (bundled Chrome-for-Testing hangs on captureScreenshot in this sandbox)
- Use `page.setViewport()` for viewport sizing (CLI --window-size clamps below ~500px)
- Serve on :4340 via `./serve.sh` (check if already running first: `curl -sf http://localhost:4340/ >/dev/null`)
- `page.setJavaScriptEnabled(false)` for no-JS assertions
- `page.emulateMediaFeatures([{name:'prefers-reduced-motion', value:'reduce'}])` for reduced-motion assertions

**Gallery thumbnail precedent (06-01):** capture homepage at 1440 wide, crop top 900px to exact 16:10 server-side with PIL, write to `assets/gallery/concept-d.png`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero up/bigger + obvious hover (CSS) with judged fold + hover captures</name>
  <files>concept-d/assets/css/concept-d.css</files>
  <action>
**Hero top-anchor + scale (IT3-01)** — starting recipe, tune ONLY via the capture review below:
- `.hero-viewport`: `justify-content: center` → `flex-start`; keep `min-height: 100dvh` and the no-scroll-jacking overflow behavior; padding-top small so the kicker starts close under the sticky topbar (e.g. `padding: 20px 0 28px`). To keep "cards fill the remainder of the viewport", add `margin-top: auto` to the homepage card grid inside the viewport (`.hero-viewport .card-field`) so the hero hugs the top and the 5-card row sits in the lower band — judge the balance in captures and drop the auto-margin if natural flow reads better.
- `.hero-d .hero-h1`: `clamp(26px, 3.2vw, 40px)` → `clamp(34px, 3.7vw, 54px)` (≈53px at 1440, ≈47px at 1280 — inside Jon's 44–56 target band; `max-width: 32ch` keeps it 2 lines at any size since ch scales with font-size). Kicker + blurb stay compact; `.hero-sub` measure (63ch) unchanged.

**Obvious hover (IT3-03)** — replace the subtle state; identical end-state for :focus-visible; touch untouched:
```css
.card { transition: transform 400ms cubic-bezier(0.37,0,0.63,1),
                    box-shadow 400ms cubic-bezier(0.37,0,0.63,1),
                    background-color 400ms cubic-bezier(0.37,0,0.63,1); }
.card-cta { transition: color 400ms cubic-bezier(0.37,0,0.63,1); }
@media (hover: hover) and (pointer: fine) {
  .card:hover { background-color: rgba(255,255,255,0.4);
                transform: translateY(-6px) scale(1.02);
                box-shadow: 0 26px 56px rgba(10,10,15,0.24); }
  .card:hover .card-cta { color: var(--flarepop); }
}
.card:focus-visible { background-color: rgba(255,255,255,0.4);
                      transform: translateY(-6px) scale(1.02);
                      box-shadow: 0 26px 56px rgba(10,10,15,0.24); }
.card:focus-visible .card-cta { color: var(--flarepop); }
```
Remove/adjust the old `.card:hover, .card:focus-visible { translateY(-3px) … }` and 0.5-alpha rules so only one coherent state exists; keep `.card:active` at the fill-drop only (touch gets no lift/scale). Flarepop CTA flip is the ONLY colored text this adds (brand-legal); no gradients, no rule lines, sine easing only. Confirm the existing reduced-motion `transition: none` block still covers `.card` and add `.card-cta` to it.

**Judged captures (honest review — mechanical gates can't see composition):** with serve.sh on :4340, capture via the Puppeteer rig into `.planning/phases/08-concept-d-iteration-3/captures/`: `home-1440x900.png`, `home-1280x800.png` (JS on, poster state fine), and `card-hover-1440.png` (page.hover on the first card, wait ≥500ms for the transition to land). READ all three with the Read tool and judge: headline close under the topbar and clearly bigger; hero + all 5 cards fully inside the fold at BOTH sizes; hover state unmistakable at a glance (fill visibly thinner, real lift, Flarepop CTA) yet calm. Tune clamp/padding/shadow and re-capture until all three read right.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && grep -q 'justify-content: flex-start' concept-d/assets/css/concept-d.css && grep -q 'scale(1.02)' concept-d/assets/css/concept-d.css && grep -q 'rgba(255,255,255,0.4)' concept-d/assets/css/concept-d.css && ! grep -q 'transform 1.1s' concept-d/assets/css/concept-d.css && ls .planning/phases/08-concept-d-iteration-3/captures/home-1440x900.png .planning/phases/08-concept-d-iteration-3/captures/home-1280x800.png .planning/phases/08-concept-d-iteration-3/captures/card-hover-1440.png</automated>
  </verify>
  <done>Hero viewport top-anchored with enlarged clamp; obvious hover state in place with focus parity and reduced-motion coverage; all three captures exist and have been READ and judged (fold holds at both sizes, hover unmistakable).</done>
</task>

<task type="auto">
  <name>Task 2: Puppeteer behavior suite + reel v2 verification + explore-page captures</name>
  <files>.planning/phases/08-concept-d-iteration-3/captures/</files>
  <action>
Write a one-off Puppeteer suite in the scratchpad (established rig per interfaces; not committed) against http://localhost:4340/concept-d/ asserting, in order:

1. **Fold, mechanically:** at 1440×900 and 1280×800 — `.hero-d .hero-h1` computed font-size ≥ 44px (1440) / ≥ 40px (1280); `getBoundingClientRect().bottom` of `#modules` ≤ viewport height at both sizes; h1 top edge within ~180px of viewport top.
2. **Hover state, mechanically:** `page.hover()` the first card, wait 600ms → computed `background-color` is `rgba(255, 255, 255, 0.4)`, transform matrix non-none, `.card-cta` color is `rgb(255, 0, 229)`. Then Tab-focus a card (keyboard, so :focus-visible applies) → same end state.
3. **Card navigation:** click the InterceptOS card → navigation lands on `pages/explore/interceptos.html`; `#quietFlow` has rendered children; click `.explore-back` → back on the concept-d homepage. Repeat for one case card (`case-sap-video.html`, `.q-case-detail` populated).
4. **Drawers on explore pages:** on `pages/explore/labs.html` click `[data-open="pitchLabs"]` → `#pitchLabs` gains `.open`; Esc closes it (deployed.js document-level handler). On `pages/explore/agents.html` click an agent card → detail overlay `.is-open`; click `#agentDetailContact` → `#convoDrawer` gains `.open`.
5. **Reel v2 (IT3-04):** scroll `#work-reel` into view, wait ≤3s → `!video.paused && video.currentTime > 0` and `currentSrc` contains `work-reel-1080` (IO-gated playback of the committed v2 asset).
6. **Reduced-motion:** with `prefers-reduced-motion: reduce` emulated, reload home → hero video paused AND reel stays paused after scrolling it into view.
7. **No-JS:** `setJavaScriptEnabled(false)`, load home → click a card anchor navigates to its explore page; back link returns (plain anchors, no handlers).

**Budget assertions (shell, not Puppeteer):** `work-reel-1080.webm` ≤ 4,500,000 B; `work-reel-1080.mp4` ≤ 6,000,000 B; `work-reel-poster.jpg` ≤ 300,000 B (`stat -f%z`).

**Explore-page captures (judged):** capture all 8 explore pages at 1440×900 into the phase captures dir (`explore-problems-1440.png`, `explore-interceptos-1440.png`, `explore-agents-1440.png`, `explore-labs-1440.png`, `explore-insights-1440.png`, `explore-case-hp-abx-1440.png`, `explore-case-intel-abm-1440.png`, `explore-case-sap-video-1440.png`), plus `explore-labs-drawer-1440.png` with the pitch drawer open. READ every capture and judge honestly: deployed header intact, back link prominent, quiet module renders correctly as a page (not boxed like a dialog), no white-on-white or collision regressions (watch the interceptos navy band). Fix any finding in concept-d.css or the explore page files (report as deviation, 08-01's files) and re-capture.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && [ "$(stat -f%z concept-d/assets/video/work-reel-1080.webm)" -le 4500000 ] && [ "$(stat -f%z concept-d/assets/video/work-reel-1080.mp4)" -le 6000000 ] && [ "$(stat -f%z concept-d/assets/video/work-reel-poster.jpg)" -le 300000 ] && ls .planning/phases/08-concept-d-iteration-3/captures/explore-problems-1440.png .planning/phases/08-concept-d-iteration-3/captures/explore-interceptos-1440.png .planning/phases/08-concept-d-iteration-3/captures/explore-agents-1440.png .planning/phases/08-concept-d-iteration-3/captures/explore-labs-1440.png .planning/phases/08-concept-d-iteration-3/captures/explore-insights-1440.png .planning/phases/08-concept-d-iteration-3/captures/explore-case-hp-abx-1440.png .planning/phases/08-concept-d-iteration-3/captures/explore-case-intel-abm-1440.png .planning/phases/08-concept-d-iteration-3/captures/explore-case-sap-video-1440.png .planning/phases/08-concept-d-iteration-3/captures/explore-labs-drawer-1440.png</automated>
  </verify>
  <done>All behavior assertions (fold ×2, hover + focus parity, nav round-trips, explore-page drawers, reel v2 playback, reduced-motion, no-JS) pass; budgets green; all 9 explore captures exist and have been READ with any findings fixed.</done>
</task>

<task type="auto">
  <name>Task 3: Gallery thumbnail + REVIEW.md refresh + final gate re-run</name>
  <files>assets/gallery/concept-d.png, REVIEW.md</files>
  <action>
1. **Gallery thumbnail:** re-capture the concept-d homepage at 1440 wide (JS on, hero settled), crop the top 900px to exact 16:10 with PIL (06-01 precedent) and overwrite `assets/gallery/concept-d.png`. READ the result — the thumbnail must show the new top-anchored bigger hero + 5-card grid.
2. **REVIEW.md:** update ONLY the Concept D material: the "Concept D — Home Variant" paragraph now describes iteration 3 (headline up under the sticky nav and larger; cards navigate to standalone quiet explore pages with a prominent Back-to-home affordance — no module modals; unmistakable card hover with the Flarepop CTA flip; work reel now the v2 weavy.ai SAP brand-film montage), the Concept D what-to-try hints (try: click a card → quiet page → back link; hover a card; let the work reel play), and the captures pointer line (add `.planning/phases/08-concept-d-iteration-3/captures/`). Touch nothing else in REVIEW.md — other concepts' sections and the Fritz-QA note stay byte-identical.
3. **Final gate re-run (phase-closing):** `python3 qa/copy-diff.py --mode substring` on concept-d/index.html + all 8 explore pages; `python3 qa/concept-d-script-diff.py`; confirm `git diff --quiet concept-d/assets/js/deployed.js concept-d/assets/css/deployed.css`; banned tagline absent from all changed files; comment-aware Python re-check that this phase's concept-d.css additions introduced no rule-line declarations and no gradients (07-03's value-aware precedent, not literal grep).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 qa/copy-diff.py --mode substring concept-d/index.html concept-d/pages/explore/problems.html concept-d/pages/explore/interceptos.html concept-d/pages/explore/agents.html concept-d/pages/explore/labs.html concept-d/pages/explore/insights.html concept-d/pages/explore/case-hp-abx.html concept-d/pages/explore/case-intel-abm.html concept-d/pages/explore/case-sap-video.html && python3 qa/concept-d-script-diff.py && git diff --quiet concept-d/assets/js/deployed.js concept-d/assets/css/deployed.css && grep -qi 'iteration' REVIEW.md && python3 -c "from PIL import Image; im=Image.open('assets/gallery/concept-d.png'); w,h=im.size; assert abs(w/h - 1.6) < 0.01, (w,h); print('16:10 ok', w, h)" && ! grep -ri 'Fresh thinking starts here' concept-d/ --include='*.html' --include='*.css' -l</automated>
  </verify>
  <done>Gallery thumbnail is a fresh 16:10 iteration-3 capture (READ and judged); REVIEW.md's Concept D paragraph/hints/captures pointer describe iteration 3 with everything else untouched; all copy/script/brand gates green on the final state.</done>
</task>

</tasks>

<verification>
- Fold proofs: hero + all 5 cards inside the first viewport at 1440×900 AND 1280×800, h1 ≥ 44px computed at 1440, headline close under the topbar — proven by Puppeteer assertions AND judged captures
- Hover: mechanical end-state assertions (0.4 fill, transform, Flarepop CTA) + focus parity + judged hover capture
- Reel v2 plays via IO when in view, never under reduced-motion; webm/mp4/poster within budget
- Behavior suite green end-to-end: nav round-trips, explore-page drawers, reduced-motion, no-JS
- Both copy gates + script-diff green on the final phase state; deployed.css/deployed.js byte-untouched
- assets/gallery/concept-d.png and REVIEW.md reflect iteration 3
</verification>

<success_criteria>
IT3-01, IT3-03, IT3-04 all observably true (with IT3-02 proven live by this plan's behavior suite), evidenced by green mechanical gates, a green 15+-assertion behavior pass, and honestly-reviewed captures at both fold sizes plus all 8 explore pages. Phase 8 is ready for Jon's local review on :4340 — nothing deployed anywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/08-concept-d-iteration-3/08-02-SUMMARY.md`
</output>
