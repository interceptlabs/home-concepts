---
phase: 05-concept-d-home-variant
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - concept-d/index.html
  - concept-d/assets/css/concept-d.css
  - concept-d/assets/js/cards.js
  - concept-d/assets/js/hero-video.js
autonomous: true
requirements: [COND-01, COND-02, COND-03, COND-04, COND-06, COND-07]
must_haves:
  truths:
    - "The homepage shows the deployed sticky nav + logo (verbatim, incl. theme toggle and hover-glitch) over a full-screen light motion-graphics video in light mode"
    - "The deployed hero copy (kick/h1/sub/CTA text) sits over the video, verbatim"
    - "8 small real-<button> cards (Problems, InterceptOS, Agents, Work, Labs, Insights, FAQs, Contact) each open an accessible modal containing that COMPLETE deployed module, fully functional — problems tabs, agents detail pane, case drawer, labs pitch drawer, FAQ accordion, convert form all work inside their modals"
    - "The clients logo strip renders inline as a quiet trust strip (not behind a card)"
    - "Esc closes a modal and focus returns to the invoking card; without JS, cards degrade to anchors to the section pages"
    - "Reduced-motion visitors get a paused video showing the poster; the pause control and visibility-pause work"
  artifacts:
    - path: "concept-d/index.html"
      provides: "Hero video layer + verbatim hero copy + 8 cards + clients strip + 8 DOM-resident module <dialog>s + footer + global drawer scaffold + SVG defs"
      min_lines: 400
    - path: "concept-d/assets/js/cards.js"
      provides: "Card→dialog wiring, drawer-scaffold reparenting, synthetic resize dispatch, focus-return, os→agents bridge"
      min_lines: 40
    - path: "concept-d/assets/js/hero-video.js"
      provides: "Concept B video idioms adapted: reduced-motion pause, play-rejection fallback, visibility pause, toggle"
      min_lines: 30
    - path: "concept-d/assets/css/concept-d.css"
      provides: "NEW chrome only: video layer, card field, modal shell, modal section-padding neutralization, no-JS toggles"
      min_lines: 60
  key_links:
    - from: "concept-d/index.html"
      to: "concept-d/assets/js/deployed.js"
      via: "script tag BEFORE cards.js (cards.js calls deployed.js globals closeAll/openDrawer)"
      pattern: "assets/js/deployed\\.js"
    - from: "concept-d/assets/js/cards.js"
      to: "fritz-bg canvases inside dialogs"
      via: "showModal() then window.dispatchEvent(new Event('resize'))"
      pattern: "dispatchEvent\\(new Event\\('resize'\\)\\)"
    - from: "concept-d/assets/js/cards.js"
      to: "#scrim/#casePanel/#convoDrawer/#pitchLabs"
      via: "reparent the 4 scaffold nodes INTO the opening dialog (top-layer fix), back to body on close"
      pattern: "append"
    - from: "concept-d/index.html cards"
      to: "pages/*.html"
      via: "no-JS fallback anchors (html.has-js toggling)"
      pattern: "pages/(os|work|labs|contact)\\.html"
---

<objective>
Build the Concept D variant homepage: deployed sticky nav/logo and hero copy ported verbatim over concept-d's own full-screen light video, a field of 8 small enticing button-cards that expand into `<dialog>` modals each carrying its COMPLETE deployed section module (with the two research-verified landmine fixes: drawer top-layer stacking and hidden-canvas re-measure), the clients strip inline, and the no-JS/keyboard/reduced-motion paths.

Purpose: this is the phase's core deliverable — Jon's reveal model over the deployed design, modules intact. The ported modules are SACROSANCT: only their container changes.
Output: concept-d/index.html + the 3 new-chrome files (concept-d.css, cards.js, hero-video.js).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-concept-d-home-variant/05-CONTEXT.md
@.planning/phases/05-concept-d-home-variant/05-RESEARCH.md
@shared/README.md
@concept-b/assets/js/panels.js
@concept-b/assets/js/video.js
@concept-d/assets/video/ASSETS.md

Staging source (read by line range): `/Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-website-staging/home.html`

<interfaces>
Markup blocks to port VERBATIM from staging home.html (build index.html with a one-off extraction script per the proven 02-01/03-02 pattern — never hand-retype):

- Head title + meta description: lines ~20–21. Theme-init script: line 19, inline in <head> verbatim. Google Fonts preconnect + css2 link: lines ~15–17 verbatim. NEVER port head lines 9–18 (mobile redirect).
- Header: 2304–2327 (incl. theme-toggle button verbatim — locked call #3).
- SVG defs: the two hidden `<svg>` blocks from ~2152–2303 (symbol defs incl. #intercept-lockup + agent glyphs, then #fritz-glitch-source). EXCLUDE the separate `<svg>` at 2139–2151 containing `<filter id="hero-glitch">` (belongs to the replaced hero system).
- Hero copy (NOT the hero video markup): from 2332–2346 port ONLY the `.wrap` contents — `<p class="kick">`, `<h1 class="hero-h1">` (with its <em>/<span class="dot"> markup), `<p class="hero-sub">`, `<div class="hero-ctas">`. EXCLUDE `.hero__videos`/`.hero__video`/`.hero__scrim` (2333–2337). The hero CTA is `<a class="btn btn-primary" href="#problems">Explore more</a>` — rewrite href to `#modules` (the card field container; plain in-page anchor, works without JS). CTA text stays verbatim.
- Clients strip: 2349–2378 verbatim.
- Module sections (each goes inside its own dialog, markup untouched, section ids preserved — deployed.js's one-time init queries them at load, and closed-dialog content is still in the DOM for getElementById):
  problems 2380–2410 · os 2412–2430 · agents 2432–2477 · work 2479–2553 · labs 2555–2573 · insights 2575–2647 · faqs 2649–2666 · convert 2668–2681
- Footer: 2685–2707.
- Global drawer scaffold: 2709–2781 (#scrim, #casePanel, #convoDrawer, #pitchLabs) verbatim, as direct children of <body> (cards.js reparents them on modal open).
- JSON-LD FAQ schema: 2784–2798 verbatim (harmless, mirrors deployed).

HREF REWRITES on ported header/footer (root context — same rule set as 05-01):
`#os`→`pages/os.html` · `#labs`→`pages/labs.html` · `#work`→`pages/work.html` · `#convert`→`pages/contact.html` (nav CTA keeps id="navContact", footer keeps id="footContact" — the JS hijack was deleted from deployed.js in 05-01) · `about.html`/`insights-hub.html`/`chatb2b.html` unchanged · logo href stays `#main`.

DEPLOYED.JS GLOBALS available to cards.js (top-level script scope — load deployed.js FIRST):
`openDrawer(id)`, `closeAll()`, `_openModal(el)`, `_closeModal()`, plus one-time init at load (buildSolve, renderFlow('intelligence'), renderAgents('strategy'), [data-case]/[data-open] wiring, fritz-bg IIFE with a debounced window-resize listener, logo glitch auto-fire 500ms after load).

TWO LANDMINE FIXES (research Pitfalls 4 & 5 — both implemented in cards.js, ZERO deployed.js edits):
1. Top-layer stacking: `<dialog>.showModal()` renders in the browser top layer; the position:fixed drawer scaffold would render UNDERNEATH an open dialog (z-index has no effect against the top layer). Fix by REPARENTING: on modal open, move the 4 scaffold nodes (#scrim, #casePanel, #convoDrawer, #pitchLabs) into the opening dialog via `dialog.append(...)`; on dialog close, call `closeAll()` then move them back to document.body. Single instances, ids stay unique, deployed.js drawer logic untouched, event listeners survive reparenting. CONSTRAINT: `dialog.module-modal` must NOT use transform/filter/backdrop-filter/perspective/contain — any of these creates a fixed-position containing block that would trap the drawers inside the dialog's box.
2. Hidden-canvas zero-dimension: fritz-bg canvases inside closed dialogs measure 0×0 at load and bail forever. Fix: after every `dialog.showModal()`, dispatch `window.dispatchEvent(new Event('resize'))` — the ported IIFE's existing debounced resize listener (staging ~3288) re-measures.

CARD → DIALOG → NO-JS FALLBACK MAP (8 cards, in this order):
| Card | dialog id | module section id | no-JS fallback href |
|---|---|---|---|
| Problems    | dlg-problems | #problems | (none — no-JS renders a non-interactive card; the module's panel copy is JS-rendered even on standalone pages, and Problems is modal-only per locked call #1) |
| InterceptOS | dlg-os       | #os       | pages/os.html |
| Agents      | dlg-agents   | #agents   | pages/os.html#agents |
| Work        | dlg-work     | #work     | pages/work.html |
| Labs        | dlg-labs     | #labs     | pages/labs.html |
| Insights    | dlg-insights | #insights | insights-hub.html |
| FAQs        | dlg-faqs     | #faqs     | pages/contact.html#faqs |
| Contact     | dlg-convert  | #convert  | pages/contact.html |

(The homepage Insights MODULE — 3 ChatB2B episode teasers — and the nav's Insights destination — the mirrored insights-hub.html — are two different things; do not conflate. The card opens the module modal; its no-JS fallback goes to the hub.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble index.html — ported chrome + hero video layer + cards + 8 module dialogs</name>
  <files>concept-d/index.html</files>
  <action>
Build concept-d/index.html with a one-off extraction/assembly script reading the staging line ranges in the interfaces block (never hand-retype ported markup). Structure, in order:

1. **Head**: charset/viewport; staging title + meta description verbatim; theme-init script (staging line 19) inline verbatim; a 1-line `has-js` bootstrap inline script (`document.documentElement.classList.add('has-js')` — concept-b precedent); Google Fonts links verbatim; `<link rel="stylesheet" href="assets/css/deployed.css">` then `href="assets/css/concept-d.css"`. Do NOT link shared/tokens.css — deployed.css is concept-d's token source (shared/README binds new-chrome RULES, not its files). NO mobile-redirect script.

2. **Header** ported verbatim with root-context href rewrites (interfaces table). Theme toggle kept verbatim.

3. **NEW hero video layer** (concept-d's own class names — never `.hero__video`, that CSS wasn't ported): `<div class="hero-video-layer" aria-hidden="true">` containing `<video class="hero-video" autoplay muted playsinline loop preload="metadata" poster="assets/video/hero-light-poster.jpg">` with the webm `<source>` FIRST, then the mp4. Plus a visible, labeled pause/play control button (`class="video-toggle"`, aria-pressed), positioned unobtrusively.

4. **main#main**: a hero section (new transparent container over the video) holding the VERBATIM ported hero copy (kick/h1/sub/CTA per interfaces; CTA href rewritten to `#modules`). Then `<section id="modules" class="card-field">` with the 8 cards per the card map: each card exists twice — `<button class="card" data-modal="dlg-…">` (shown with JS) and `<a class="card card-fallback" href="…">` (shown without JS; Problems' no-JS variant is a non-interactive `<div class="card card-static">`). Card copy rule: eyebrow = that module's own kicker/label text verbatim from its ported section header; heading = the module's h2 verbatim; teaser = ONE verbatim line from the module (its lede/sub — a complete verbatim string, never reworded, never cut mid-word). Every string on a card must exist verbatim somewhere in staging home.html — no invented copy, ever. Then the **clients strip** ported verbatim as a quiet inline trust strip below the card field.

5. **8 dialogs**: `<dialog class="module-modal" id="dlg-…" aria-label="…">` each containing a new close button (`<button class="modal-close" data-modal-close aria-label="Close">`) and the module's complete ported section verbatim (section ids intact). Module markup is untouched — only the container is new.

6. **Footer** ported verbatim with href rewrites; **global drawer scaffold** (2709–2781) verbatim as body-level siblings; **SVG defs** blocks (~2152–2303, excluding the #hero-glitch filter); **JSON-LD** verbatim.

7. **Scripts at end of body, in this order**: `assets/js/deployed.js`, `assets/js/hero-video.js`, `assets/js/cards.js`.

Do NOT add `data-copy` attributes anywhere in concept-d — the substring gate must stay in auto-detected fallback mode, matching the deployed source's own structure.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && test -s concept-d/index.html && [ "$(grep -c '<dialog' concept-d/index.html)" = 8 ] && [ "$(grep -c 'data-modal="dlg-' concept-d/index.html)" = 8 ] && [ "$(grep -c 'card-fallback' concept-d/index.html)" -ge 7 ] && for id in problems os agents work labs insights faqs convert modules main scrim casePanel convoDrawer pitchLabs; do grep -q "id=\"$id\"" concept-d/index.html || { echo "missing #$id"; exit 1; }; done && ! grep -q 'mobile.html' concept-d/index.html && ! grep -q 'hero__video' concept-d/index.html && ! grep -q 'hero-glitch' concept-d/index.html && ! grep -q 'data-copy' concept-d/index.html && grep -q 'poster="assets/video/hero-light-poster.jpg"' concept-d/index.html && python3 -c "
h=open('concept-d/index.html').read()
assert h.index('hero-light-loop.webm') < h.index('hero-light-loop.mp4'), 'webm must be listed before mp4'
assert h.index('assets/js/deployed.js') < h.index('assets/js/cards.js'), 'deployed.js must load before cards.js'
assert 'href=\"#modules\"' in h, 'hero CTA must anchor to #modules'
assert 'ig_theme' in h, 'theme-init script must be inline in head'
print('structure ok')" && python3 -c "
import re
from html.parser import HTMLParser
stg=open('/Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-website-staging/home.html').read()
src=open('concept-d/index.html').read()
m=re.search(r'<section id=\"modules\".*?</section>', src, re.S)
assert m, 'card field section#modules not found'
class T(HTMLParser):
    def __init__(self):
        super().__init__(); self.parts=[]; self.skip=0
    def handle_starttag(self,t,a):
        if t in ('script','style','svg'): self.skip+=1
    def handle_endtag(self,t):
        if t in ('script','style','svg') and self.skip: self.skip-=1
    def handle_data(self,d):
        if not self.skip: self.parts.append(d)
def visible_chunks(s):
    p=T(); p.feed(s)
    return [re.sub(r'\s+',' ',c).strip() for c in p.parts if re.sub(r'\s+',' ',c).strip()]
stgtext=' '.join(visible_chunks(stg))
bad=[c for c in visible_chunks(m.group(0)) if len(c)>=10 and c not in stgtext]
for c in bad: print('INVENTED CARD COPY:', c[:90])
assert not bad, '%d card text chunk(s) not found verbatim in staging' % len(bad)
print('card copy verbatim ok:', len(visible_chunks(m.group(0))), 'chunks checked')"</automated>
  </verify>
  <done>index.html contains all 8 dialogs with intact module sections, 8 card buttons + fallback variants, verbatim header/footer/hero-copy/clients/scaffold/defs with correct href rewrites, webm-first video with poster, correct script order, no mobile redirect / hero-queue remnants / data-copy attributes; every ≥10-char card text chunk exists verbatim in the staging source.</done>
</task>

<task type="auto">
  <name>Task 2: New chrome — concept-d.css, hero-video.js, cards.js</name>
  <files>concept-d/assets/css/concept-d.css, concept-d/assets/js/hero-video.js, concept-d/assets/js/cards.js</files>
  <action>
**concept-d.css** (NEW chrome only — consumes deployed.css custom properties like var(--page)/var(--surface)/var(--fg)/var(--line); shared/README rules bind new chrome: no hex text colors, no colored text at all in new chrome, no gradients, no rule-line dividers, no view-transition-name, no invented decorative marks):
- `.hero-video-layer`: position:fixed; inset:0; z-index:0; video object-fit:cover, 100% width/height. Page content stacks above it.
- Hero viewport: hero copy + card field compose within ~100dvh (min-height, NOT fixed height — if the card field overflows small viewports it scrolls natively per locked decision; no scroll-jacking).
- `.card-field`: responsive grid (e.g., auto-fit minmax) of SMALL cards. Cards: near-opaque light surface (rgba white ≥ .9, or backdrop-filter blur ON THE CARD — never on the dialog), soft shadow, legible over moving light footage (research: the deployed flat-white-on-white pattern doesn't work over video — this elevation is functional legibility chrome, same category as Concept B's hotspot labels). Hover/entrance transitions (if any) use sine ease-in-out `cubic-bezier(0.37,0,0.63,1)`, long durations, no jarring landings. `:focus-visible{outline:2px solid var(--fg); outline-offset:2px}` (04-03 convention).
- Below-hero flow (clients strip container + footer): `position:relative; background:var(--page)` so they opaquely occlude the fixed video layer (04-02 lesson — fixed layers bleed through transparent sections).
- `dialog.module-modal`: width min(1100px, 94vw); max-height ~90vh; overflow:auto; opaque background var(--page); `::backdrop` a FLAT translucent ink rgba (no gradient). MUST NOT set transform/filter/backdrop-filter/perspective/contain on the dialog itself — leave a CSS comment stating this invariant (it would trap the reparented fixed-position drawers). Additive container-level neutralization of the ported page-flow section rule (research-enumerated; NEVER edit deployed.css): `dialog.module-modal section{padding:32px 0 8px; border-bottom:0;}`.
- `.modal-close` styling; no-JS toggles: `html.has-js .card-fallback, html.has-js .card-static{display:none}` and `html:not(.has-js) .card[data-modal]{display:none}`.
- `@media (prefers-reduced-motion: reduce)`: suppress new-chrome transitions.

**hero-video.js** — adapt concept-b/assets/js/video.js idioms (read it; rename selectors to `.hero-video`/`.video-toggle`/`.hero-video-layer`): `attemptPlay(userInitiated)` gating ambient autoplay behind prefers-reduced-motion but always allowing explicit user play; iOS play()-rejection fallback (poster stays, toggle shows play state); toggle state driven off the video element's own play/pause events (not manually tracked); **the 03-03 lesson**: native `autoplay` plays regardless of JS gating — for reduced-motion visitors explicitly call `video.pause()` + sync toggle state BEFORE wiring the play/pause listeners; `visibilitychange` pause/resume that never auto-resumes over an explicit user pause. IntersectionObserver hero-exit pause is unnecessary here — the layer is position:fixed and never leaves the viewport; note this deviation from concept-b in a comment.

**cards.js** — concept-b panels.js idiom + the two landmine fixes (see interfaces):
- `openModal(dialog, invoker)`: remember invoker; reparent the 4 scaffold nodes (#scrim, #casePanel, #convoDrawer, #pitchLabs) into the dialog via `dialog.append(...)`; `dialog.showModal()`; `window.dispatchEvent(new Event('resize'))`.
- Wire every `button.card[data-modal]` click to its dialog. Wire `[data-modal-close]` to `dialog.close()`.
- On each dialog's native `close` event (fires for Esc AND close button): call `closeAll()` if defined (closes any open drawer, restores body overflow), reparent the 4 scaffold nodes back to document.body, focus-return to the invoker.
- os→agents bridge (research port-map note): delegated click listener on #dlg-os for `a[href="#agents"]` (renderFlow generates this link) → preventDefault, close dlg-os, open dlg-agents.
- Nothing else. deployed.js is never edited.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && node --check concept-d/assets/js/cards.js && node --check concept-d/assets/js/hero-video.js && grep -q 'showModal' concept-d/assets/js/cards.js && grep -q "dispatchEvent(new Event('resize'))" concept-d/assets/js/cards.js && grep -q 'closeAll' concept-d/assets/js/cards.js && grep -q 'append' concept-d/assets/js/cards.js && grep -q 'prefers-reduced-motion' concept-d/assets/js/hero-video.js && grep -q 'visibilitychange' concept-d/assets/js/hero-video.js && grep -q 'module-modal' concept-d/assets/css/concept-d.css && grep -q 'has-js' concept-d/assets/css/concept-d.css && ! grep -iIE 'fresh thinking starts here' concept-d/assets/css/concept-d.css concept-d/assets/js/cards.js concept-d/assets/js/hero-video.js && ! grep -iIE 'a855f7|6366f1|22d3ee' concept-d/assets/css/concept-d.css concept-d/assets/js/cards.js concept-d/assets/js/hero-video.js && ! grep -IE '(^|[^-])color:[^;}]*#' concept-d/assets/css/concept-d.css && ! grep -IE 'linear-gradient|radial-gradient|conic-gradient' concept-d/assets/css/concept-d.css && ! grep -IE 'border-(top|bottom)[[:space:]]*:[[:space:]]*[^0n]' concept-d/assets/css/concept-d.css && ! grep -I 'view-transition-name' concept-d/assets/css/concept-d.css && ! grep -Ii '<hr' concept-d/index.html</automated>
  </verify>
  <done>Both JS files parse; cards.js implements reparent + resize-dispatch + closeAll + focus-return + os→agents bridge; hero-video.js implements reduced-motion explicit pause, rejection fallback, visibility pause; concept-d.css passes every new-chrome brand grep (the border grep permits only 0/none values — the modal section neutralization removes a rule line, never adds one). Ported deployed.css is exempt from these greps by scope — it is the approved live design.</done>
</task>

<task type="auto">
  <name>Task 3: Mechanical behavior verification — Puppeteer modal suite + copy gates</name>
  <files>(scratchpad only — no repo files; results recorded in SUMMARY)</files>
  <action>
Write a Puppeteer assertion script at `/private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad/concept-d-modal-suite.mjs` (never in the repo). Rig lessons (02-03/03-03/04-03 proven): `executablePath` = the real installed Google Chrome (the bundled Chrome-for-Testing hangs on screenshots in this sandbox), `page.setViewport()` (never bare --window-size), serve via ./serve.sh on :4340 (start only if not already running; kill only if this task started it). Collect `page.on('pageerror')` — zero page errors tolerated across the whole run. Assertions (exit non-zero on any failure, print a named PASS/FAIL line per check):

1. Load http://localhost:4340/concept-d/index.html at 1440×900. For EACH of the 8 dialogs: click its card button → `dialog.open === true`; press Escape → dialog closed AND `document.activeElement` is the invoking card (focus-return).
2. Problems modal: capture `#solveDetail.innerHTML`, click the 2nd `.solve-tab`, assert innerHTML changed (tabs work inside the modal). Assert the `canvas.fritz-bg` inside #dlg-problems has a `width` attribute > 0 after open (hidden-canvas fix proven).
3. Agents modal: click an agent card → detail pane visible; click `#agentDetailContact` → `#convoDrawer` has class `open` AND `document.elementFromPoint()` at the drawer's center resolves to a node inside #convoDrawer (top-layer stacking fix proven — the drawer renders ABOVE the open dialog). Close the drawer, close the dialog; assert #convoDrawer's parentElement is document.body again (scaffold reparented home).
4. Work modal: click a `[data-case]` card → `#casePanel` has class `open`, `#cName` textContent non-empty (openCase ran), `#cHero img` src contains `/concept-d/assets/img/case-` and `naturalWidth > 0` (extracted images load).
5. Labs modal: click `[data-open="pitchLabs"]` → #pitchLabs open + elementFromPoint proof. FAQs modal: click a `summary` → its `details.open === true`.
6. Nav href attributes: header nav links point at pages/os.html, pages/labs.html, pages/work.html, pages/contact.html, about.html, insights-hub.html (pages are built in 05-03 — assert attributes only, do not navigate).
7. Reduced-motion: fresh page with `page.emulateMediaFeatures([{name:'prefers-reduced-motion', value:'reduce'}])`, reload → `video.paused === true`.
8. No-JS: `page.setJavaScriptEnabled(false)`, reload → the 7 `.card-fallback` anchors have non-null bounding boxes and every `button.card[data-modal]` has none (CSS toggle works).

Then run the copy gates: `python3 qa/copy-diff.py --mode substring concept-d/index.html` must exit 0 — expected coverage ≈ the documented 42-of-155 static chunks; record the exact checked/passed counts in the SUMMARY with the explicit note that the JS-object copy is covered by qa/concept-d-script-diff.py (the locked Phase 5 compensation; the Puppeteer-rendered copy-diff variant is deferred to Phase 6). Also re-run `python3 qa/concept-d-script-diff.py` (regression).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 qa/copy-diff.py --mode substring concept-d/index.html && python3 qa/concept-d-script-diff.py && node /private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad/concept-d-modal-suite.mjs</automated>
  </verify>
  <done>All Puppeteer assertions pass with zero page errors: 8/8 modals open, Esc + focus-return work, problems tabs + agents detail + case drawer (with loaded case image) + pitch drawer + FAQ accordion all function INSIDE their modals, drawers render above the dialog top layer, scaffold returns to body on close, reduced-motion pauses the video, no-JS shows fallback anchors. copy-diff substring exits 0 on index.html; script-diff gate exits 0.</done>
</task>

</tasks>

<verification>
- The reveal model works end-to-end: card → modal → complete functional deployed module → Esc → focus back on card.
- Both landmine fixes are functionally proven (elementFromPoint drawer-above-dialog check; canvas width > 0 check).
- Copy discipline: substring gate 0 failures on index; script-diff gate 0 failures; card copy proven verbatim-in-source by the Task 1 parser check; no data-copy attributes introduced.
- New-chrome brand greps all clean (ported deployed.css/deployed.js exempt by scope).
</verification>

<success_criteria>
- COND-01: deployed sticky nav + logo treatment verbatim on index (theme toggle included).
- COND-02: full-screen light video (webm-first, poster, autoplay muted playsinline loop) with light-mode UI, reduced-motion static fallback, pause control, visibility pause.
- COND-03: 8 small enticing real-<button> cards over the video, verbatim eyebrow/heading + one verbatim teaser line each.
- COND-04: every card expands into a modal carrying the COMPLETE deployed module, design and behavior intact.
- COND-06 (homepage half): substring copy gate + script-diff gate + card-copy source check all pass.
- COND-07: dialogs accessible (native focus trap, Esc, focus return), cards real buttons, keyboard path complete, no scroll-jacking, no-JS degrades to anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-concept-d-home-variant/05-02-SUMMARY.md` — include the Puppeteer suite results (per-assertion), the copy-diff checked/passed counts with the documented 42/155 static-coverage note, and any discretion calls made (card field geometry, modal sizing).
</output>
