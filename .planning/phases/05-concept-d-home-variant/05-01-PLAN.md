---
phase: 05-concept-d-home-variant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - concept-d/assets/css/deployed.css
  - concept-d/assets/js/deployed.js
  - concept-d/assets/img/case-hp-abx.png
  - concept-d/assets/img/case-intel-abm.png
  - concept-d/assets/img/case-sap-video.png
  - qa/concept-d-script-diff.py
  - concept-d/about.html
  - concept-d/insights-hub.html
  - concept-d/chatb2b.html
  - concept-d/assets/podcast/chatb2b-trailer.mp4
  - concept-d/assets/podcast/chatb2b-trailer-poster.jpg
autonomous: true
requirements: [COND-05, COND-06]
must_haves:
  truths:
    - "concept-d/assets/css/deployed.css reproduces the staging stylesheet in original cascade order (7 of 8 style blocks; only the .hero__video block excluded) so every Jon-feedback revision override survives"
    - "concept-d/assets/js/deployed.js carries PROBLEMS_RR, PROBLEM_FLOWS, AGENTS, CAT_LABELS, and CASES byte-identical to staging home.html, proven by a committed mechanical gate"
    - "about.html, insights-hub.html, chatb2b.html serve from concept-d/ differing from staging ONLY in href attribute values (the 5-pattern rewrite)"
    - "The 3 case-study images decode byte-identical to the staging base64 originals and deployed.js points at them via /concept-d/assets/img/ paths"
    - "The mobile-redirect script and the deployed hero's own video-queue system are NOT ported anywhere"
  artifacts:
    - path: "concept-d/assets/css/deployed.css"
      provides: "Full staging <style> content, verbatim, original order"
      min_lines: 2000
    - path: "concept-d/assets/js/deployed.js"
      provides: "Staging data objects + render/drawer/canvas/logo-glitch/theme-toggle logic (4 script blocks)"
      min_lines: 450
    - path: "qa/concept-d-script-diff.py"
      provides: "Byte-compare gate for the JS-templated copy the static copy-diff gate cannot see"
      min_lines: 60
    - path: "concept-d/about.html"
      provides: "Mirrored staging page, hrefs rewritten"
    - path: "concept-d/insights-hub.html"
      provides: "Mirrored staging page, hrefs rewritten"
    - path: "concept-d/chatb2b.html"
      provides: "Mirrored staging page, hrefs rewritten"
  key_links:
    - from: "qa/concept-d-script-diff.py"
      to: "concept-d/assets/js/deployed.js"
      via: "brace-counting extraction + byte-compare of the 5 const objects against staging home.html"
      pattern: "PROBLEMS_RR"
    - from: "concept-d/assets/js/deployed.js"
      to: "concept-d/assets/img/case-*.png"
      via: "CASE_IMG values rewritten from data URIs to /concept-d/assets/img/ paths"
      pattern: "/concept-d/assets/img/case-"
    - from: "concept-d/insights-hub.html"
      to: "concept-d/assets/podcast/chatb2b-trailer.mp4"
      via: "existing relative src=\"assets/podcast/…\" resolving because the page sits at concept-d/ root"
      pattern: "assets/podcast/chatb2b-trailer"
---

<objective>
Port the deployed staging bundle's foundation into concept-d: the full stylesheet and script content as shared `deployed.css`/`deployed.js` (verbatim except four enumerated edits), the 3 case-study images extracted from base64, a committed script-diff gate that mechanically proves the JS-object copy is byte-identical to source, and the 3 mirrored pages (about/insights-hub/chatb2b) with their href rewrites and podcast assets.

Purpose: everything Plans 05-02/05-03 build sits on these two shared files. The deployed port is SACROSANCT — it is the approved live design; nothing in it gets "fixed", restyled, reformatted, or linted. The script-diff gate is this phase's compensating control for copy-diff's documented blind spot (only 42 of 155 long canonical chunks live in static markup; the rest live inside the JS data objects).

Output: concept-d/assets/css/deployed.css, concept-d/assets/js/deployed.js, concept-d/assets/img/case-*.png ×3, qa/concept-d-script-diff.py, concept-d/{about,insights-hub,chatb2b}.html, concept-d/assets/podcast/* ×2.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-concept-d-home-variant/05-CONTEXT.md
@.planning/phases/05-concept-d-home-variant/05-RESEARCH.md
@shared/README.md
@qa/README.md

Source of truth (read by line range, do NOT load whole file at once):
`/Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-website-staging/home.html` (3,430 lines — if `wc -l` differs from 3430, STOP relying on line numbers and re-locate every block by its content marker instead).

Sibling sources: `/Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-website-staging/{about.html,insights-hub.html,chatb2b.html}` (701/1097/919 lines) and `/Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-website-staging/assets/podcast/`.

<interfaces>
Verified block map of staging home.html (1-indexed, current as of 2026-07-24):

STYLE BLOCKS (exactly 8 `<style>…</style>` regions):
1. lines 25–1904 (main stylesheet)          → PORT
2. 1905–1918                                → PORT
3. 1919–1926 (Fritz AA contrast fixes)      → PORT
4. 1927–1939 (AA contrast, tinted panels)   → PORT
5. 1940–2112 (r8 revision block)            → PORT
6. 2113–2117 (case-drawer hero image)       → PORT
7. 2118–2122 (fritz-bg canvas config)       → PORT
8. 2128–2138 (contains `.hero__video`)      → EXCLUDE (deployed hero's own video system; Concept D replaces it)

SCRIPT BLOCKS:
- head 9–18: mobile redirect (`location.replace('mobile.html…')`)  → NEVER PORT (mobile.html doesn't exist here; would 404 real phones)
- head 19: theme-init (`localStorage.getItem('ig_theme')` one-liner) → NOT into deployed.js — Plans 05-02/05-03 inline it verbatim in each page's <head> (must run before render to avoid theme flash)
- 2784–2798: JSON-LD FAQ schema (type=application/ld+json)          → NOT into deployed.js — markup-side, 05-02 keeps it inline in index.html
- 2800–3241: MAIN block (marker: `const PROBLEMS_RR`)               → PORT (block 1 of deployed.js)
- 3242–3290: fritz-bg canvas IIFE (marker: `fritz-bg`)              → PORT (block 2)
- 3292–3333: hero 3-clip video queue (marker: `QUEUE = ['assets/video/hero.mp4'`) → EXCLUDE
- 3334: theme-toggle IIFE (marker: `themeToggle`)                   → PORT (block 3) — locked call: theme toggle kept verbatim
- 3335–3428: logo hover-glitch IIFE (marker: `fritz-glitch-source`) → PORT (block 4)

DATA OBJECTS inside the MAIN block (all top-level `const NAME = {…};`):
- `const PROBLEMS_RR = {`  at 2802   (Problems copy)
- `const PROBLEM_FLOWS = {` at 2887  (InterceptOS copy)
- `const AGENTS = {`        at 2964  (Agents copy)
- `const CAT_LABELS = {`    at 3031  (one-liner)
- `const CASE_IMG = {`      at 3145  (3 base64 data URIs, ~1.85M chars — the ONLY object that gets edited)
- `const CASES = {`         at 3151  (Work case copy; references CASE_IMG by key, itself unedited)

THE ONLY 2 PERMITTED JS EDITS (everything else byte-identical — no reformatting, no lint fixes):
(a) CASE_IMG values → `'/concept-d/assets/img/case-hp-abx.png'`, `'/concept-d/assets/img/case-intel-abm.png'`, `'/concept-d/assets/img/case-sap-video.png'` (server-root-absolute so one deployed.js works from both concept-d/ and concept-d/pages/; keys unchanged)
(b) DELETE staging lines 3230–3233 exactly (4 lines): `const navContactBtn…` + its `addEventListener` line, `const footContactBtn…` + its line — the nav/footer Contact click-hijack that opens the convo drawer instead of navigating (COND-05 requires plain navigation). LEAVE `agentContactBtn` and `caseContactBtn` wiring intact.

MIRRORED-PAGE HREF REWRITE (5 exact patterns, longest-first not required since each includes the closing quote). Verified per-file occurrence counts (about / chatb2b / insights-hub):
| Pattern                      | Rewrite to             | counts |
|------------------------------|------------------------|--------|
| `href="home.html"`           | `href="index.html"`    | 1/1/1  |
| `href="home.html#convert"`   | `href="pages/contact.html"` | 4/3/2 |
| `href="home.html#labs"`      | `href="pages/labs.html"`    | 2/2/2 |
| `href="home.html#os"`        | `href="pages/os.html"`      | 2/2/2 |
| `href="home.html#work"`      | `href="pages/work.html"`    | 3/2/2 |
Everything else in the 3 mirrored pages stays byte-identical: the 6 insights-article hrefs (insights-*.html — out of scope, will 404, documented), the 3 legal-page hrefs (ai-policy/privacy-policy/terms-of-service — pre-existing dead links in staging itself), all external links, all visible text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract deployed.css + deployed.js + case images from staging home.html</name>
  <files>concept-d/assets/css/deployed.css, concept-d/assets/js/deployed.js, concept-d/assets/img/case-hp-abx.png, concept-d/assets/img/case-intel-abm.png, concept-d/assets/img/case-sap-video.png</files>
  <action>
Write a one-off extraction script (scratchpad, not committed — the proven 02-01/03-02 pattern) rather than hand-copying thousands of lines. The script reads staging home.html and:

1. **deployed.css**: regex-split all `<style[^>]*>…</style>` regions in document order. Assert exactly 8 found. Drop the ONE whose content contains `.hero__video`. Concatenate the other 7 blocks' inner content in original order, separated by `\n\n/* ── staging style block N ── */\n` comments (comments are the only additions). Cascade order is load-bearing: the same classes (.prob-chapter, .agent-card-v6, .case-v6, .prob-flow-head, .labs*, .ep-tile*) are redefined across revision rounds and the LAST definition wins — never reorder, never dedupe.

2. **deployed.js**: extract inner content of exactly these 4 script blocks, identified by content marker (fall back to markers if line numbers drift): MAIN (`const PROBLEMS_RR`), fritz-bg IIFE (`canvas.fritz-bg` / `fritz-bg`), theme-toggle IIFE (`themeToggle`), logo-glitch IIFE (`fritz-glitch-source`). Concatenate in that order with `/* ── staging script: NAME ── */` separators. Assert the result does NOT contain: `mobile.html`, `assets/video/hero.mp4`, `ig_theme`, `application/ld+json` content.

3. Apply the ONLY two permitted edits (see interfaces block): (a) extract the 3 CASE_IMG base64 data URIs, base64-decode each to concept-d/assets/img/case-{hp-abx,intel-abm,sap-video}.png, and replace the CASE_IMG values with the three `/concept-d/assets/img/…` paths; (b) delete the 4 navContactBtn/footContactBtn lines (staging 3230–3233). Nothing else changes — no prettifying, no whitespace normalization, no lint fixes. The deployed port is sacrosanct.

Create the target directories (concept-d/assets/css, concept-d/assets/js, concept-d/assets/img) as needed.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && test -s concept-d/assets/css/deployed.css && test -s concept-d/assets/js/deployed.js && grep -q 'const PROBLEMS_RR' concept-d/assets/js/deployed.js && grep -q 'const PROBLEM_FLOWS' concept-d/assets/js/deployed.js && grep -q 'const AGENTS' concept-d/assets/js/deployed.js && grep -q 'const CAT_LABELS' concept-d/assets/js/deployed.js && grep -q 'const CASES' concept-d/assets/js/deployed.js && grep -q "getElementById('scrim')" concept-d/assets/js/deployed.js && grep -q 'themeToggle' concept-d/assets/js/deployed.js && grep -q 'fritz-glitch-source' concept-d/assets/js/deployed.js && ! grep -q 'mobile.html' concept-d/assets/js/deployed.js && ! grep -q 'base64' concept-d/assets/js/deployed.js && ! grep -q "assets/video/hero.mp4" concept-d/assets/js/deployed.js && ! grep -q 'ig_theme' concept-d/assets/js/deployed.js && ! grep -q 'navContactBtn' concept-d/assets/js/deployed.js && ! grep -q 'footContactBtn' concept-d/assets/js/deployed.js && grep -q 'agentContactBtn' concept-d/assets/js/deployed.js && grep -c '/concept-d/assets/img/case-' concept-d/assets/js/deployed.js | grep -qx 3 && ! grep -q 'hero__video' concept-d/assets/css/deployed.css && grep -q 'solve-tab' concept-d/assets/css/deployed.css && grep -q 'agent-card-v6' concept-d/assets/css/deployed.css && grep -q 'fritz-bg' concept-d/assets/css/deployed.css && grep -q 'data-theme="light"' concept-d/assets/css/deployed.css && node --check concept-d/assets/js/deployed.js && python3 -c "import sys; [sys.exit('bad PNG: '+p) for p in ['concept-d/assets/img/case-hp-abx.png','concept-d/assets/img/case-intel-abm.png','concept-d/assets/img/case-sap-video.png'] if open(p,'rb').read(8) != b'\x89PNG\r\n\x1a\n']; print('PNGs ok')"</automated>
  </verify>
  <done>deployed.css holds 7 style blocks in original order with the hero-video block absent; deployed.js parses (node --check), holds the 4 script blocks with all 5 data objects and drawer/canvas/glitch/theme logic, contains no mobile-redirect / hero-queue / base64 / nav-footer-hijack; the 3 case PNGs exist with valid PNG magic; CASE_IMG points at them.</done>
</task>

<task type="auto">
  <name>Task 2: Committed script-diff gate proving JS-object copy is byte-identical to source</name>
  <files>qa/concept-d-script-diff.py</files>
  <action>
Create `qa/concept-d-script-diff.py` (stdlib only, following qa/copy-diff.py conventions) — the permanent compensating gate for copy-diff's blind spot on JS-templated copy. Locked call: this mechanical script-diff IS Phase 5's verification for the 113 canonical chunks that live only inside JS; the Puppeteer-rendered copy-diff variant is DEFERRED to Phase 6.

CLI: `python3 qa/concept-d-script-diff.py [--staging PATH] [--js PATH] [--css PATH]` with defaults `/Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-website-staging/home.html`, `concept-d/assets/js/deployed.js`, `concept-d/assets/css/deployed.css` (resolve repo-relative defaults relative to the script's parent dir, like copy-diff.py does).

Checks (print one PASS/FAIL line each; exit 0 only if all pass, 1 on any failure, 2 on I/O error):
1. For each of `PROBLEMS_RR`, `PROBLEM_FLOWS`, `AGENTS`, `CAT_LABELS`, `CASES`: extract the full `const NAME = {…};` region from BOTH the staging file and deployed.js using a small brace-counting scanner that starts at the `const NAME` token and tracks quote state (', ", `) so braces inside string/template literals don't break counting. Byte-compare the two extractions. On mismatch, report the object name and a ±60-char context window around the first differing byte.
2. `CASE_IMG`: extract the 3 base64 data URIs from STAGING, decode each, byte-compare against the corresponding concept-d/assets/img/case-*.png file (key → filename map: hp-abx/intel-abm/sap-video). Also assert deployed.js's CASE_IMG contains exactly the 3 expected `/concept-d/assets/img/` paths.
3. Exclusion asserts: `mobile.html` NOT in deployed.js; `assets/video/hero.mp4` NOT in deployed.js; `.hero__video` NOT in deployed.css; `navContactBtn` NOT in deployed.js.

This gate runs in every subsequent plan's verification and again in Phase 6.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 qa/concept-d-script-diff.py && SCRATCH=/private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad && mkdir -p "$SCRATCH" && sed 's/The Intelligence Problem/The Intelligence Problam/g' concept-d/assets/js/deployed.js > "$SCRATCH/mutated-deployed.js" && ! python3 qa/concept-d-script-diff.py --js "$SCRATCH/mutated-deployed.js" && echo 'mutation test: gate correctly FAILs on a 1-char drift'</automated>
  </verify>
  <done>Gate exits 0 against the real deployed.js and exits non-zero against a 1-character mutation of PROBLEMS_RR (fixture-proven, same philosophy as qa/fixtures/fail-annotated.html). All 5 objects byte-compared, case images byte-verified against decoded staging base64, exclusions asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror about/insights-hub/chatb2b + podcast assets with the 5-pattern href rewrite</name>
  <files>concept-d/about.html, concept-d/insights-hub.html, concept-d/chatb2b.html, concept-d/assets/podcast/chatb2b-trailer.mp4, concept-d/assets/podcast/chatb2b-trailer-poster.jpg</files>
  <action>
1. Copy the 3 staging pages to concept-d/ ROOT (siblings of the future index.html — this keeps their bare-filename cross-links to each other correct with zero edits). Copy `assets/podcast/chatb2b-trailer.mp4` and `chatb2b-trailer-poster.jpg` from the staging bundle into `concept-d/assets/podcast/` (insights-hub.html and chatb2b.html reference them via relative `assets/podcast/…` src, which resolves from concept-d/ root unmodified).

2. Apply EXACTLY the 5-pattern href rewrite from the interfaces table via scripted find/replace (each pattern includes the closing quote, so order doesn't matter). Expected occurrence counts per file are in the table — assert them.

3. Touch NOTHING else: the 6 insights-article hrefs in insights-hub.html stay as-is (out of scope per Deferred Ideas; they will 404 — note this in the SUMMARY), the 3 legal-page hrefs stay (pre-existing dead links in staging itself), no visible text changes anywhere. These pages are EXEMPT from the copy-diff gate — they ARE the source.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && STG=/Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-website-staging && for f in about.html insights-hub.html chatb2b.html; do grep -c 'href="home.html' concept-d/$f | grep -qx 0 || exit 1; diff <(sed 's/href="[^"]*"/href=@/g' "$STG/$f") <(sed 's/href="[^"]*"/href=@/g' "concept-d/$f") > /dev/null || { echo "$f differs beyond hrefs"; exit 1; }; done && [ "$(grep -o 'href="pages/contact.html"' concept-d/about.html | wc -l | tr -d ' ')" = 4 ] && [ "$(grep -o 'href="pages/work.html"' concept-d/about.html | wc -l | tr -d ' ')" = 3 ] && [ "$(grep -o 'href="index.html"' concept-d/insights-hub.html | wc -l | tr -d ' ')" = 1 ] && test -s concept-d/assets/podcast/chatb2b-trailer.mp4 && test -s concept-d/assets/podcast/chatb2b-trailer-poster.jpg && bash -c 'STARTED=0; if ! curl -sf -o /dev/null http://localhost:4340/; then ./serve.sh >/dev/null 2>&1 & STARTED=$!; sleep 1; fi; RC=0; for p in concept-d/about.html concept-d/insights-hub.html concept-d/chatb2b.html concept-d/assets/podcast/chatb2b-trailer.mp4; do [ "$(curl -s -o /dev/null -w "%{http_code}" http://localhost:4340/$p)" = 200 ] || RC=1; done; [ "$STARTED" != 0 ] && kill $STARTED 2>/dev/null; exit $RC'</automated>
  </verify>
  <done>All 3 mirrored pages serve 200 from concept-d/ with zero remaining home.html hrefs; the sed-neutralized diff proves each file is byte-identical to staging except href values; expected rewrite counts hold; podcast video + poster resolve 200.</done>
</task>

</tasks>

<verification>
- `python3 qa/concept-d-script-diff.py` exits 0 (all 5 data objects byte-identical, case images byte-verified, exclusions hold) and fails on mutation.
- Mirrored pages: href-neutralized diff empty vs staging; serve 200 with podcast assets resolving.
- Nothing ported was "fixed": no reformatting, no restyling, no lint changes in deployed.css/deployed.js beyond the four enumerated edits (CASE_IMG paths, hijack deletion, block exclusions, block-separator comments).
</verification>

<success_criteria>
- COND-05 (mirrored-page half): about.html / insights-hub.html / chatb2b.html live in concept-d with correct internal routing into the (upcoming) concept-d pages.
- COND-06 (verbatim-by-construction half): the JS-object copy — the 113 canonical chunks invisible to copy-diff — is mechanically proven byte-identical to the deployed source by a committed, mutation-tested gate.
- Plans 05-02/05-03 can build pages that reference deployed.css/deployed.js without re-deriving anything from staging.
</success_criteria>

<output>
After completion, create `.planning/phases/05-concept-d-home-variant/05-01-SUMMARY.md` — include the script-diff gate results, the mirrored-page rewrite counts, and the documented known-dead links (6 insights articles, 3 legal pages).
</output>
