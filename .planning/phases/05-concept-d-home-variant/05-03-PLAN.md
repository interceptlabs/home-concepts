---
phase: 05-concept-d-home-variant
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - concept-d/pages/os.html
  - concept-d/pages/labs.html
  - concept-d/pages/work.html
  - concept-d/pages/contact.html
  - .planning/phases/05-concept-d-home-variant/captures/
autonomous: true
requirements: [COND-01, COND-05, COND-06, COND-07]
must_haves:
  truths:
    - "Nav items route to standalone pages carrying that content: InterceptOS→os.html (os + agents modules), Intercept Labs→labs.html, Work→work.html, Contact→contact.html (convert + FAQs), About→about.html, Insights→insights-hub.html"
    - "Section pages use the deployed header/footer shell with modules intact, and their drawers (case, convo, pitch) work via the unmodified global scaffold"
    - "Every internal link and in-page anchor across concept-d resolves (with the documented allowlist of pre-existing dead links)"
    - "The homepage and all 4 section pages pass the substring copy gate; the script-diff gate still passes"
    - "Responsive captures at 390/768/1440 (index, one open modal, os.html) plus reduced-motion and no-JS captures were READ and judged honestly, with issues fixed"
  artifacts:
    - path: "concept-d/pages/os.html"
      provides: "InterceptOS + Agents modules in the deployed page shell"
      min_lines: 150
    - path: "concept-d/pages/labs.html"
      provides: "Labs module in the deployed page shell"
    - path: "concept-d/pages/work.html"
      provides: "Work module in the deployed page shell"
    - path: "concept-d/pages/contact.html"
      provides: "Convert + FAQs modules in the deployed page shell"
    - path: ".planning/phases/05-concept-d-home-variant/captures"
      provides: ">=10 reviewed QA captures (responsive, modal-open, reduced-motion, no-JS)"
  key_links:
    - from: "concept-d/pages/*.html"
      to: "concept-d/assets/js/deployed.js"
      via: "script src=\"../assets/js/deployed.js\" — one-time init renders flows/agents, wires drawers against the plain global scaffold (no dialog nesting needed on pages)"
      pattern: "\\.\\./assets/js/deployed\\.js"
    - from: "concept-d/index.html card fallbacks + nav"
      to: "concept-d/pages/*.html"
      via: "hrefs verified by the link-integrity pass"
      pattern: "pages/(os|labs|work|contact)\\.html"
    - from: "concept-d/pages/os.html"
      to: "#agents"
      via: "renderFlow's generated bridge link — a same-page anchor here, works unmodified"
      pattern: "id=\"agents\""
---

<objective>
Compose the 4 standalone section pages from the same ported modules in the deployed page shell, wire the nav end-to-end, then run the phase-closing mechanical QA sweep: link integrity across all of concept-d (including mirrored pages), substring copy-diff on the 4 generated pages, the brand grep suite, responsive/reduced-motion/no-JS captures reviewed honestly, and the video budget check.

Purpose: completes COND-05 (nav → full pages) and closes the phase with the fidelity + accessibility evidence COND-06/COND-07 require.
Output: pages/os.html, pages/labs.html, pages/work.html, pages/contact.html + reviewed captures + QA results table for the SUMMARY.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-concept-d-home-variant/05-CONTEXT.md
@.planning/phases/05-concept-d-home-variant/05-RESEARCH.md
@.planning/phases/05-concept-d-home-variant/05-01-SUMMARY.md
@.planning/phases/05-concept-d-home-variant/05-02-SUMMARY.md
@qa/README.md

Staging source (read by line range): `/Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-website-staging/home.html`

<interfaces>
PAGE COMPOSITION (locked by CONTEXT — "Problems" is intentionally modal-only, no page, matching the deployed nav):
| Page | Modules (staging line ranges, verbatim) | Notes |
|---|---|---|
| pages/os.html      | os 2412–2430 + agents 2432–2477 | renderFlow's generated `href="#agents"` bridge link works unmodified (same page) |
| pages/labs.html    | labs 2555–2573 | "Build with Labs" → #pitchLabs via global scaffold |
| pages/work.html    | work 2479–2553 | case cards → #casePanel via global scaffold |
| pages/contact.html | convert 2668–2681 then faqs 2649–2666 | #faqs anchor target for index's FAQs fallback link; include the JSON-LD FAQ schema (2784–2798) verbatim on this page |

SHELL per page (all ported verbatim from staging with the pages-context rewrites):
- Head: charset/viewport; page title format "{Module label} — Intercept" (page titles are chrome, not canonical copy — do not invent visible body copy anywhere); theme-init script (staging line 19) inline verbatim; Google Fonts links verbatim; `../assets/css/deployed.css` + `../assets/css/concept-d.css`; NO mobile redirect; no has-js bootstrap needed (no cards here).
- Header 2304–2327 + footer 2685–2707 with PAGES-context hrefs:
  `#os`→`os.html` · `#labs`→`labs.html` · `#work`→`work.html` · `#convert`→`contact.html` · `about.html`→`../about.html` · `insights-hub.html`→`../insights-hub.html` · `chatb2b.html`→`../chatb2b.html` · logo `#main`→`../index.html`
- SVG defs blocks (~2152–2303, excluding the #hero-glitch filter) on every page — agent glyphs are required on os.html, the glitch source everywhere the logo animates.
- Global drawer scaffold 2709–2781 verbatim at end of body on EVERY page (uniform, unused drawers harmless; no dialog nesting/reparenting needed here — research confirms the original global-scaffold pattern works unmodified on standalone pages).
- Script: `../assets/js/deployed.js` only (no cards.js, no hero-video.js). deployed.js's nav/footer Contact hijack was deleted in 05-01, so the Contact CTA is a plain navigation link everywhere.
- Modules render on the deployed flat light page — no video layer on section pages.

KNOWN-DEAD LINK ALLOWLIST for the link-integrity pass (pre-existing in the staging source itself, left as-is per locked decisions — never "fix"):
- `ai-policy.html`, `privacy-policy.html`, `terms-of-service.html` (footer legal links, every page)
- the 6 insights-article pages linked from insights-hub.html (`insights-*.html` — mirroring them is explicitly deferred)
- external http(s)/mailto links: skipped, not checked

VIDEO BUDGET (actuals: webm 2.3MB, mp4 5.5MB, poster 95KB): webm ≤ 4,000,000 bytes; poster ≤ 200,000 bytes; `autoplay muted playsinline loop` + `poster=` attributes present; webm source listed before mp4.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the 4 standalone section pages from the ported modules</name>
  <files>concept-d/pages/os.html, concept-d/pages/labs.html, concept-d/pages/work.html, concept-d/pages/contact.html</files>
  <action>
Build the 4 pages with a one-off extraction/assembly script (same technique as 05-02 Task 1 — never hand-retype ported markup), each per the interfaces composition + shell spec. Main content = the modules directly in the deployed shell; do NOT author new page-hero copy or headings (inventing copy is banned; the modules carry their own section headers). Module markup, section ids, and internal wiring stay verbatim — only the shell around them is assembled.

Order sanity: contact.html renders convert first, then faqs (the page's purpose is contact; FAQs support it) — note this discretion call in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && for f in os labs work contact; do test -s concept-d/pages/$f.html || exit 1; grep -q 'mobile.html' concept-d/pages/$f.html && exit 1; grep -q '../assets/js/deployed.js' concept-d/pages/$f.html || exit 1; grep -q '../assets/css/deployed.css' concept-d/pages/$f.html || exit 1; grep -q 'ig_theme' concept-d/pages/$f.html || exit 1; for id in scrim casePanel convoDrawer pitchLabs; do grep -q "id=\"$id\"" concept-d/pages/$f.html || { echo "missing #$id in $f"; exit 1; }; done; grep -q 'href="../index.html"' concept-d/pages/$f.html || exit 1; grep -q 'href="../about.html"' concept-d/pages/$f.html || exit 1; done && grep -q 'id="os"' concept-d/pages/os.html && grep -q 'id="agents"' concept-d/pages/os.html && grep -q 'id="labs"' concept-d/pages/labs.html && grep -q 'id="work"' concept-d/pages/work.html && grep -q 'id="convert"' concept-d/pages/contact.html && grep -q 'id="faqs"' concept-d/pages/contact.html && python3 qa/copy-diff.py --mode substring concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html && python3 qa/concept-d-script-diff.py</automated>
  </verify>
  <done>All 4 pages exist with correct shells (theme-init, fonts, deployed.css+concept-d.css, rewritten header/footer, SVG defs, full drawer scaffold, deployed.js), correct module composition and section ids, no mobile redirect; substring copy gate exits 0 on all 4 pages; script-diff gate still green.</done>
</task>

<task type="auto">
  <name>Task 2: Link integrity + end-to-end nav and page-behavior verification</name>
  <files>(scratchpad only — no repo files; results recorded in SUMMARY)</files>
  <action>
**Link integrity (scratchpad Python script):** walk every concept-d/**/*.html; extract all href/src values; skip external (http/https/mailto/tel) and the allowlist in interfaces; resolve relative paths against each file's location and `/concept-d/...` absolute paths against the repo root; every remaining target must exist on disk. For fragment links (`page.html#x` or bare `#x`), assert the target file contains `id="x"` (checks: `#agents` on os.html, `#faqs` on contact.html, `#modules`/`#main` on index.html, and every dialog/scaffold anchor). Print counts: files scanned, links checked, allowlisted, failures. Zero non-allowlisted failures required.

**End-to-end nav (Puppeteer, scratchpad, proven rig):**
1. index → click nav "InterceptOS" → page.url() ends /concept-d/pages/os.html; on os.html click nav "Work" → work.html; nav "Intercept Labs" → labs.html; nav "Contact" (cta-nav) → contact.html — asserting the 05-01 hijack deletion holds (navigates, no drawer).
2. From a section page: "About" → ../about.html; "Insights" → ../insights-hub.html; logo → ../index.html. From about.html (mirrored): a rewritten link (e.g., its contact CTA) → pages/contact.html.
3. Page-module behavior on the standalone pages (global scaffold, no reparenting): os.html — click a `.prob-tab` → flow content changes; click an agent card → detail pane opens; `#agentDetailContact` → #convoDrawer opens (class `open`), close via its `.x` button. work.html — `[data-case]` → #casePanel opens with `#cHero img` naturalWidth > 0. labs.html — `[data-open="pitchLabs"]` → #pitchLabs opens. contact.html — convert tile CTA opens #convoDrawer; a FAQ `summary` click toggles `details.open`.
4. Zero page errors (`page.on('pageerror')`) across every page visited.

**Brand grep suite (phase scope — ported deployed.css/deployed.js and the 3 mirrored pages are EXEMPT from the new-chrome-only greps; tagline/deprecated-hex/view-transition greps run everywhere since the staging source is verified clean):** run the exact commands in the verify block.

**Gates:** re-run `python3 qa/copy-diff.py --mode substring concept-d/index.html concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html` and `python3 qa/concept-d-script-diff.py`.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 /private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad/concept-d-linkcheck.py && node /private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad/concept-d-nav-suite.mjs && ! grep -rIiE 'fresh thinking starts here' concept-d && ! grep -rIiE 'a855f7|6366f1|22d3ee' concept-d && ! grep -rI 'view-transition-name' concept-d && ! grep -rI 'data-copy' concept-d && ! grep -Ii '<hr' concept-d/index.html concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html && ! grep -IE '(^|[^-])color:[^;}]*#' concept-d/assets/css/concept-d.css && ! grep -IE 'linear-gradient|radial-gradient|conic-gradient' concept-d/assets/css/concept-d.css && ! grep -IE 'border-(top|bottom)[[:space:]]*:[[:space:]]*[^0n]' concept-d/assets/css/concept-d.css && python3 qa/copy-diff.py --mode substring concept-d/index.html concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html && python3 qa/concept-d-script-diff.py</automated>
  </verify>
  <done>Link integrity passes with zero non-allowlisted failures (counts recorded); nav routes end-to-end in both directions including mirrored-page re-entry; every drawer/tab/accordion works on its standalone page with zero page errors; full grep suite clean; both copy gates green across index + 4 pages.</done>
</task>

<task type="auto">
  <name>Task 3: Responsive/reduced-motion/no-JS captures + video budget + honest review</name>
  <files>.planning/phases/05-concept-d-home-variant/captures/</files>
  <action>
**Captures (Puppeteer scratchpad rig: real Chrome executablePath, page.setViewport — never bare --window-size below ~500px, fullPage for page captures)** into `.planning/phases/05-concept-d-home-variant/captures/`:
1. `index-390.png`, `index-768.png`, `index-1440.png` — JS-on, video layer present (wait for load + a settle beat).
2. `index-modal-agents-1440.png` — agents modal open (after clicking its card, resize dispatched).
3. `index-modal-problems-390.png` — problems modal open at mobile width (fritz canvas must be visibly rendered, module usable/scrollable).
4. `os-390.png`, `os-768.png`, `os-1440.png` — full page.
5. `index-reduced-1440.png` — emulateMediaFeatures prefers-reduced-motion: video paused on its poster frame.
6. `index-nojs-1440.png` — setJavaScriptEnabled(false): fallback anchor cards visible, hero copy and clients strip intact.

**Review (mandatory, honest):** READ every capture with the Read tool and judge: cards legible over the moving light footage at all 3 widths; card field small/enticing (not a wall); no horizontal overflow anywhere; modal fits and scrolls at 390 with close button reachable; header/footer render identically to the deployed design; clients strip + footer opaquely occlude the fixed video (no bleed-through); os.html modules read deployed-faithful (this is the COND-06 visual-fidelity check); reduced-motion shows a still poster; no-JS layout coherent. Fix any issue found (respecting the sacrosanct rule — fixes go in new chrome only, never in ported deployed.css/deployed.js markup/styles), re-run the Task 2 gates on touched files, re-capture.

**Video budget:** run the byte/attribute checks in the verify block.

**Record the QA results table** (both gates' counts, each grep, link-integrity counts, capture list with review verdicts, budget numbers) for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && [ "$(ls .planning/phases/05-concept-d-home-variant/captures/*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 10 ] && ! find .planning/phases/05-concept-d-home-variant/captures -name '*.png' -size -8k | grep -q . && [ "$(stat -f%z concept-d/assets/video/hero-light-loop.webm)" -le 4000000 ] && [ "$(stat -f%z concept-d/assets/video/hero-light-poster.jpg)" -le 200000 ] && grep -q 'autoplay muted playsinline loop' concept-d/index.html && grep -q 'poster="assets/video/hero-light-poster.jpg"' concept-d/index.html && python3 -c "h=open('concept-d/index.html').read(); assert h.index('hero-light-loop.webm') < h.index('hero-light-loop.mp4'); print('webm-first ok')" && python3 qa/copy-diff.py --mode substring concept-d/index.html concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html && python3 qa/concept-d-script-diff.py</automated>
  </verify>
  <done>≥10 non-trivial captures exist and every one was READ and judged with issues fixed; video budget holds (webm ≤ 4MB, poster ≤ 200KB, correct attributes, webm-first); both copy gates green on the final state of all 5 gated pages.</done>
</task>

</tasks>

<verification>
- Nav → pages → nav round-trips work everywhere, including from the mirrored pages back into concept-d.
- Link integrity zero non-allowlisted failures; allowlist documented (3 legal + 6 insights articles).
- copy-diff substring: 0 failures on index + 4 generated pages; script-diff gate green (the two together = COND-06's mechanical coverage, per the locked Phase 5 compensation).
- Grep suite clean at phase scope with the documented exemptions (ported deployed files + mirrored pages).
- Captures reviewed honestly at 390/768/1440 + modal-open + reduced-motion + no-JS.
</verification>

<success_criteria>
- COND-05 complete: InterceptOS/Labs/Work/Contact nav items route to standalone pages built from the same ported modules; About/Insights route to the mirrored pages.
- COND-01 complete: the deployed header/footer shell is consistent across index + 4 pages + logo treatment animating as deployed.
- COND-06 complete: copy verbatim mechanically proven (substring gate + script-diff gate) and visual fidelity judged against the deployed design via reviewed captures.
- COND-07 complete: keyboard/Esc/focus paths proven on index (05-02) and drawers/accordions proven on pages; no scroll-jacking anywhere; no-JS path captured.
- Phase 5 ready for Phase 6's cross-concept QA and gallery packaging (Concept D card).
</success_criteria>

<output>
After completion, create `.planning/phases/05-concept-d-home-variant/05-03-SUMMARY.md` — include the full QA results table, the link allowlist as shipped, capture review verdicts, and the explicit Phase 6 handoff notes: (a) concept-d uses substring mode + qa/concept-d-script-diff.py instead of annotated data-copy mode (documented exception to shared/README's static-copy rule, per locked CONTEXT), (b) the Puppeteer-rendered copy-diff variant is the deferred Phase 6 upgrade, (c) mirrored pages are gate-exempt.
</output>
