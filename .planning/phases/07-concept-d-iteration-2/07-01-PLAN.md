---
phase: 07-concept-d-iteration-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - concept-d/index.html
  - concept-d/assets/css/concept-d.css
  - concept-d/assets/js/cards.js
  - concept-d/assets/js/reel-video.js
  - .planning/phases/07-concept-d-iteration-2/captures/
autonomous: true
requirements: [ITER-01, ITER-02, ITER-03, ITER-06, ITER-07, ITER-08]

must_haves:
  truths:
    - "At 1440x900 AND 1280x800 the first viewport contains the compact hero (kicker + h1 + sub) plus all 5 section cards, none clipped"
    - "The 5 section cards are uniform height, copy anchored to the bottom edge, each with a visible mono expand CTA"
    - "Cards are semi-opaque (~75% white) at rest and drop to ~50% on hover/focus so the video field shows through; reduced-motion gets instant state change, touch/keyboard get the same end state"
    - "A full-screen work section follows the client logo strip: the campaign reel plays only when scrolled into view, pauses out of view, has a poster, a pause control, and never autoplays under reduced-motion"
    - "FAQs, Start the Conversation, and footer render as plain below-fold page sections (no dialog wrapper); the client logo strip sits unchanged between the hero viewport and the work section"
    - "No element on index.html carries data-case (the unguarded openCase() listener can never fire); the hero's Explore more CTA is gone"
  artifacts:
    - path: "concept-d/index.html"
      provides: "Restructured homepage: compact hero, 5-card grid, work-reel section with 3 case cards + 3 case dialog shells, inline FAQs/convert, no dlg-work/dlg-faqs/dlg-convert/#casePanel"
      contains: "card-field--five"
    - path: "concept-d/assets/css/concept-d.css"
      provides: "Compact hero scale overrides, flex-column card system with bottom anchor + CTA row, 5-col grid modifier, translucency hover, work-reel section chrome"
      contains: "card-bottom"
    - path: "concept-d/assets/js/reel-video.js"
      provides: "IntersectionObserver-gated reel play/pause + toggle + reduced-motion/userPaused/visibility guards"
      contains: "IntersectionObserver"
  key_links:
    - from: "concept-d/index.html .card[data-modal]"
      to: "concept-d/assets/js/cards.js openModal()"
      via: "data-modal attribute (existing convention, includes 3 new dlg-case-* buttons)"
      pattern: "data-modal=\"dlg-case-"
    - from: "concept-d/assets/js/reel-video.js"
      to: ".work-reel <video class=\"reel-video\">"
      via: "IntersectionObserver on the work section, threshold 0.25"
      pattern: "reel-video"
    - from: "inline #convert convo-tile"
      to: "#convoDrawer (end-of-body scaffold)"
      via: "deployed.js [data-open] listener — works without reparenting once un-dialogued"
      pattern: "data-open=\"convoDrawer\""
---

<objective>
Restructure concept-d/index.html for above-the-fold density: compact hero (no CTA), a uniform 5-card section grid with semi-opaque fills and explicit expand CTAs, a new full-screen work-reel section with 3 case cards (+ static case-dialog shells), FAQs/convert/footer unwrapped into plain below-fold sections, logo strip untouched in place.

Purpose: ITER-01/02/03/07/08 complete, ITER-06's section shell in place, so 07-02 can layer the scaling transition and quiet module content onto a structurally final page.
Output: restructured index.html, extended concept-d.css, new reel-video.js, staged-hover verdict captures.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-concept-d-iteration-2/07-CONTEXT.md
@.planning/phases/07-concept-d-iteration-2/07-RESEARCH.md
@concept-d/index.html
@concept-d/assets/css/concept-d.css
@concept-d/assets/js/cards.js
@concept-d/assets/js/hero-video.js

<interfaces>
<!-- Key contracts. Use directly — no exploration needed. -->

From concept-d/assets/js/cards.js (DO NOT restructure in this plan beyond the one listed edit):
```javascript
var SCAFFOLD_IDS = ["scrim", "casePanel", "convoDrawer", "pitchLabs"]; // remove "casePanel" here
function openModal(dialog, invoker) { if (!dialog) return; ... }       // null-guarded: a data-modal
// pointing at a not-yet-built dialog no-ops safely
var cardButtons = document.querySelectorAll("button.card[data-modal]"); // new case cards ride this
```

From concept-d/assets/js/deployed.js (NEVER edited — landmines to route around):
```javascript
// Line 392 — page-load global listener; openCase() has NO null guards and #casePanel
// is removed by this plan. ANY data-case attribute on index.html = runtime throw on click.
document.querySelectorAll('[data-case]').forEach(b => b.addEventListener('click', () => openCase(b.dataset.case)));
// [data-open] listener wires convo-tile/pitchLabs buttons anywhere in the page — inline
// #convert needs NO reparenting once it is plain page flow (scaffold is an end-of-body sibling).
```

From concept-d/assets/css/concept-d.css (rules this plan extends):
```css
.hero-viewport { min-height:100dvh; display:flex; flex-direction:column; justify-content:center; gap:56px; padding:132px 0 64px; }
.card-field { grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap:16px; }  /* resolves to 4 cols at --maxw:1200 — 5 cards would wrap 4+1 */
.card { display:block; background: rgba(255,255,255,0.94); transition: transform 1.1s cubic-bezier(0.37,0,0.63,1), box-shadow 1.1s cubic-bezier(0.37,0,0.63,1); }
.clients, footer { position:relative; z-index:1; background:var(--page); }  /* fixed-video occlusion — new below-fold sections need this too */
```

Sine easing everywhere: cubic-bezier(0.37, 0, 0.63, 1). Assets ready (do NOT re-encode):
concept-d/assets/video/work-reel-1080.webm (~3.3MB) / .mp4 (~4.0MB) / work-reel-poster.jpg (144KB).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact hero + uniform 5-card grid with translucency system</name>
  <files>concept-d/index.html, concept-d/assets/css/concept-d.css</files>
  <action>
**index.html hero (`section.hero.hero-d`):** delete the entire `.hero-ctas` div (the "Explore more" button — locked decision: DROPPED; cards are the explore affordance; omitting copy is allowed, rewriting is not). Kicker, h1 (including `<em>`/`.dot` spans), and `.hero-sub` stay byte-identical — do not touch their text.

**index.html `#modules`:** remove the Work, FAQs, and Convert card triples entirely (the `data-modal="dlg-work"` button + its `pages/work.html` fallback; `dlg-faqs` button + `pages/contact.html#faqs` fallback; `dlg-convert` button + `pages/contact.html` fallback). 5 cards remain: Problems, InterceptOS, Agents, Labs, Insights. Add class `card-field--five` to the section (keep `card-field`).

Restructure ALL remaining card elements (5 buttons + 4 `.card-fallback` anchors + 1 `.card-static` div — 10 elements) to the bottom-anchored architecture: `.card-eyebrow` stays first; wrap heading + teaser + a new CTA row in `<div class="card-bottom">` as the last child. CTA row: `<span class="card-cta">Open&nbsp;+</span>` — uniform on every card (neutral affordance per CONTEXT, NOT invented marketing copy; uniform label delivers the identical tile-to-tile baseline rhythm). All existing heading/teaser text nodes are MOVED, never re-typed — cut and paste exact bytes.

**concept-d.css — card system (extend, keep existing selectors where possible):**
- `.card { display:flex; flex-direction:column; min-height:150px; }` and `.card-bottom { margin-top:auto; }`
- `.card-teaser { display:-webkit-box; -webkit-line-clamp:2; -webkit-box-orient:vertical; overflow:hidden; }` (text stays intact in DOM — visual clamp only)
- `.card-cta`: mono/uppercase/12px/letter-spacing 0.08em like `.card-eyebrow`, color var(--fg), margin-top ~10px
- Resting fill: change `.card` background to `rgba(255,255,255,0.75)` (research contrast math: >=AA even at 0 fill over this poster). Add `background-color` to the existing 1.1s sine transition list.
- Hover/focus translucency drop: inside `@media (hover:hover) and (pointer:fine) { .card:hover { background-color: rgba(255,255,255,0.5); } }` plus an UNSCOPED `.card:focus-visible { background-color: rgba(255,255,255,0.5); }` and `.card:active { background-color: rgba(255,255,255,0.5); }` for touch parity. Keep the existing translateY/box-shadow hover.
- Grid: `@media (min-width:960px) { .card-field--five { grid-template-columns: repeat(5, 1fr); } }` and below 960px let `.card-field--five` fall back to `repeat(auto-fit, minmax(200px,1fr))`. Do NOT change the base `.card-field` rule (the 3-card work row in Task 2 uses it as-is).
- Compact hero overrides (new-chrome additive rules scoped `.hero-d` / `.hero-viewport`; deployed.css never edited) — research-calculated starting points, tune only via Task 3's capture review: `.hero-viewport { padding:32px 0 28px; gap:24px; }`; `.hero-d .kick { margin-bottom:8px; }`; `.hero-d .hero-h1 { font-size:clamp(26px,3.2vw,40px); max-width:32ch; line-height:1.12; }`; `.hero-d .hero-sub { margin-top:12px; font-size:clamp(14px,1vw,15px); max-width:63ch; }`
- Extend the no-JS ink override block to cover `.card-cta` (same `--nojs-ink` treatment as `.card-eyebrow`).
- `@media (prefers-reduced-motion: reduce)` already sets `.card { transition:none; }` — the state VALUES still apply (silent means instant, not absent).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && [ "$(grep -c 'data-modal=' concept-d/index.html)" = "5" ] && ! grep -q 'Explore more' concept-d/index.html && ! grep -q 'data-modal="dlg-work"\|data-modal="dlg-faqs"\|data-modal="dlg-convert"' concept-d/index.html && grep -q 'card-field--five' concept-d/index.html && [ "$(grep -o 'card-cta' concept-d/index.html | wc -l | tr -d ' ')" -ge 10 ] && grep -q 'card-bottom' concept-d/assets/css/concept-d.css && grep -q 'repeat(5, 1fr)' concept-d/assets/css/concept-d.css && grep -q 'rgba(255, 255, 255, 0.75)\|rgba(255,255,255,0.75)\|rgba(255, 255, 255, .75)\|rgba(255,255,255,.75)' concept-d/assets/css/concept-d.css && ! grep -IE 'linear-gradient|radial-gradient|conic-gradient' concept-d/assets/css/concept-d.css && ! grep -IE '(^|[^-])color:[^;}]*#' concept-d/assets/css/concept-d.css && python3 qa/copy-diff.py --mode substring concept-d/index.html && python3 qa/concept-d-script-diff.py</automated>
  </verify>
  <done>Hero CTA gone, 5 restructured cards with bottom-anchored copy + uniform Open + CTA, explicit 5-col grid, 75%/50% translucency states with sine transition and touch/keyboard/reduced-motion parity, compact hero clamp values in place, both copy gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Full-screen work-reel section + 3 case cards + case dialog shells; retire dlg-work/#casePanel</name>
  <files>concept-d/index.html, concept-d/assets/css/concept-d.css, concept-d/assets/js/reel-video.js, concept-d/assets/js/cards.js</files>
  <action>
**Insert `<section class="work-reel" id="work-reel" aria-label="The proof">` immediately AFTER the closing tag of `section.clients` (inside `<main>`)** — the logo strip itself moves ZERO bytes (ITER-08; research confirmed it already sits exactly where the locked order needs it).

Section contents:
1. Reel video, absolutely positioned cover inside the position:relative section (`min-height:100dvh`), research-verified markup — `preload="none"` + poster + NO autoplay attribute (unlike the hero; the IO gate owns playback):
```html
<video class="reel-video" muted playsinline loop preload="none" poster="assets/video/work-reel-poster.jpg" aria-hidden="true">
  <source src="assets/video/work-reel-1080.webm" type="video/webm">
  <source src="assets/video/work-reel-1080.mp4" type="video/mp4">
</video>
```
2. A pause/play control styled like the hero's `.video-toggle` (own class `.reel-toggle`, hidden under `html:not(.has-js)`).
3. Verbatim work heading MOVED from the `dlg-work` markup being deleted below (cut exact bytes, never re-type): `<span class="idx">the proof</span>`, the h2 "Three engagements that delivered verified outcomes.", the `.section-lead` paragraph. Composition (Claude's discretion): heading block on a flat translucent light panel (same translucency family as the cards — flat fill, NEVER a gradient scrim) so dark ink stays legible over the montage.
4. 3 case cards using the SAME `.card` composition as the section cards, in a `<div class="card-field">` row (base auto-fit rule resolves 3-across cleanly): for each of hp-abx / intel-abm / sap-video, a `<button type="button" class="card" data-modal="dlg-case-{key}" aria-haspopup="dialog">` with `.card-eyebrow` = the case client + tag (moved from the old markup's `.c-client`/`.c-tag` text), `.card-heading` = case name (moved), `.card-teaser` = the `.c-summary` paragraph (moved verbatim — these are canonical homepage.json chunks the substring gate checks), `.card-bottom` wrapper + `Open&nbsp;+` CTA row, plus a `.card-fallback` anchor to `pages/work.html` mirroring the same content for no-JS. CRITICAL: use `data-modal`, NEVER `data-case` — deployed.js's unguarded global `openCase()` listener throws once #casePanel is gone (research Pitfall 1).

**3 case dialog shells** appended after `dlg-insights` (before the footer scaffold), one per case — same pattern as existing dialogs: `<dialog class="module-modal" id="dlg-case-hp-abx" aria-label="Case study: HP">` + `.modal-close` button with `data-modal-close`, then a static shell: metric stat anchor (`398%` / `Incremental deal-size lift` etc. — MOVED from the old `.c-metric`/`.c-metric-lbl` markup), client, tag, name (h2), summary paragraph (same verbatim source), case image `<img src="assets/img/case-{key}.png" alt="{client} case study">`, and an empty `<div class="q-case-detail" data-case-key="{key}"></div>` container that 07-02's quiet render fills from CASES. Shells make the case cards functional immediately (cards.js `button.card[data-modal]` selector picks them up at load).

**Delete** the entire `<dialog id="dlg-work">` block AND the `<aside class="drawer" id="casePanel">` block from index.html (pages/work.html keeps its own copies — do not touch pages/). In cards.js, remove `"casePanel"` from SCAFFOLD_IDS (update the header comment's scaffold list too).

**reel-video.js (new file, IIFE, referenced by a script tag after hero-video.js):** IntersectionObserver on `#work-reel` at threshold 0.25 — `.play()` on intersect (muted video, no gesture needed), `.pause()` on leave. Port hero-video.js's guards: `prefersReducedMotion` (never auto-play; explicit toggle click may play), `userPaused` (WCAG 2.2.2 — IO/visibility resume paths never override an explicit pause), toggle state driven off the video's own play/pause events, play() rejection caught silently, `visibilitychange` pause. No IntersectionObserver support = leave video paused on poster (preload="none" means zero bytes fetched).

**concept-d.css:** `.work-reel` (position:relative; z-index:1; min-height:100dvh; overflow:hidden; background:var(--page); flex column layout for heading + card row), `.reel-video` (absolute inset 0, object-fit cover), `.reel-toggle`, the heading panel (flat translucent surface, dark ink, no gradients, no border rule lines). Card row width capped at var(--maxw) with the same 28px side padding.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && ! grep -q 'id="dlg-work"' concept-d/index.html && ! grep -q 'id="casePanel"' concept-d/index.html && ! grep -q 'data-case' concept-d/index.html && grep -q 'data-case' concept-d/pages/work.html && [ "$(grep -c 'data-modal="dlg-case-' concept-d/index.html)" = "3" ] && grep -q 'id="dlg-case-hp-abx"' concept-d/index.html && grep -q 'id="dlg-case-intel-abm"' concept-d/index.html && grep -q 'id="dlg-case-sap-video"' concept-d/index.html && grep -q 'preload="none"' concept-d/index.html && ! grep -E '<video class="reel-video"[^>]*autoplay' concept-d/index.html && grep -q 'reel-video.js' concept-d/index.html && grep -q 'IntersectionObserver' concept-d/assets/js/reel-video.js && node --check concept-d/assets/js/reel-video.js && ! grep -q 'casePanel' concept-d/assets/js/cards.js && node --check concept-d/assets/js/cards.js && python3 -c "h=open('concept-d/index.html').read(); assert h.index('work-reel-1080.webm') < h.index('work-reel-1080.mp4'); assert h.index('class=\"clients\"') < h.index('work-reel-1080.webm'); print('order ok')" && [ "$(stat -f%z concept-d/assets/video/work-reel-1080.webm)" -le 4000000 ] && [ "$(stat -f%z concept-d/assets/video/work-reel-poster.jpg)" -le 200000 ] && python3 qa/copy-diff.py --mode substring concept-d/index.html && python3 qa/concept-d-script-diff.py</automated>
  </verify>
  <done>Work-reel section sits after the untouched logo strip with IO-gated reel + poster + toggle; 3 case cards (data-modal, not data-case) open functional static case shells; dlg-work and #casePanel gone from index.html only; both copy gates green.</done>
</task>

<task type="auto">
  <name>Task 3: Unwrap FAQs/convert below-fold + fold-proof assertion + staged hover review</name>
  <files>concept-d/index.html, concept-d/assets/css/concept-d.css, .planning/phases/07-concept-d-iteration-2/captures/</files>
  <action>
**Unwrap (ITER-07):** delete the `<dialog id="dlg-faqs">` and `<dialog id="dlg-convert">` wrappers and their `.modal-close` buttons; MOVE the inner `<section id="faqs">` and `<section id="convert">` blocks byte-identical into page flow inside `<main>`, after `#work-reel`, in that order (faqs, then convert), so the final order is: hero-viewport → clients → work-reel → faqs → convert → footer. The `#convert` fritz-bg canvas now measures at load (visible page flow) and the convo-tile's `data-open="convoDrawer"` works with zero reparenting — plain page flow reaches the end-of-body scaffold exactly like pages/*.html do. Footer stays where it is. The FAQ JSON-LD script is untouched.

**Occlusion CSS (04-02 lesson, already documented in concept-d.css):** extend the `.clients, footer` rule to `.clients, .work-reel, #faqs, #convert, footer` so every below-fold section opaquely occludes the fixed hero video. (These two ported sections render with deployed.css's own section treatment — ported-module chrome, exempt from new-chrome greps, "inline as they render today" per locked decision.)

**Fold-proof mechanical check (ITER-01):** write a scratchpad Puppeteer script (real installed Chrome executablePath — Puppeteer's bundled binary hangs in this sandbox, 02-03 lesson — plus page.setViewport, launch arg `--autoplay-policy=no-user-gesture-required`) against `http://localhost:4340/concept-d/` (start `./serve.sh` in background if not running). At BOTH 1440x900 and 1280x800: assert `document.querySelector('#modules').getBoundingClientRect().bottom <= innerHeight` and `document.querySelector('.hero-h1').getBoundingClientRect().top >= 0`, and capture `index-fold-1440x900.png`, `index-fold-1280x800.png` (viewport screenshots, not fullPage) into .planning/phases/07-concept-d-iteration-2/captures/. If the assert fails, tune ONLY the compact-hero/gap/padding/card min-height values from Task 1 (research values are calculated starting points, not gates) and re-run.

**Staged hover review (locked call):** capture `card-hover-1440.png` — page.hover() over the InterceptOS card, wait 1.3s (past the 1.1s transition), viewport screenshot. Also assert mechanically that the hovered card's computed background-color alpha dropped below its resting alpha. READ the resting + hover captures with the Read tool and judge honestly: does the plain translucency shift already read as "the particle-wave field passes through the card" and calm? If YES → ship as-is, record the verdict for the SUMMARY. If NO → add the secondary CSS wave accent, then re-capture and re-judge. Wave accent constraints (hard): a `::after` flat translucent white band that sweeps across the card via transform translate animation, sine timing (cubic-bezier(0.37,0,0.63,1)), long duration (>=1.5s), scoped inside `@media (hover:hover) and (pointer:fine)` on `:hover`/`:focus-visible` only, disabled under prefers-reduced-motion. It must be a FLAT fill — NO linear/radial/conic gradients (the research's repeating-linear-gradient suggestion would fail the new-chrome gradient grep and the Fritz no-smooth-grades rule; do not use it), no backdrop-filter.

**Full-page state capture:** `index-full-1440.png` (fullPage) — READ it and confirm: logo strip between hero viewport and work reel, work heading legible over the montage poster, FAQs/convert/footer plain sections with no video bleed-through, no horizontal overflow. Fix what fails (new chrome only), re-run Task 1/2 gate commands on touched files.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && ! grep -q 'id="dlg-faqs"' concept-d/index.html && ! grep -q 'id="dlg-convert"' concept-d/index.html && grep -q 'id="faqs"' concept-d/index.html && grep -q 'id="convert"' concept-d/index.html && [ "$(grep -c '<dialog' concept-d/index.html)" = "8" ] && python3 -c "h=open('concept-d/index.html').read(); order=[h.index(x) for x in ['class=\"hero-viewport\"','class=\"clients\"','id=\"work-reel\"','id=\"faqs\"','id=\"convert\"','<footer']]; assert order==sorted(order), order; print('page order ok')" && grep -q '.work-reel, #faqs, #convert' concept-d/assets/css/concept-d.css && [ "$(ls .planning/phases/07-concept-d-iteration-2/captures/*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 4 ] && ! find .planning/phases/07-concept-d-iteration-2/captures -name '*.png' -size -8k | grep -q . && python3 qa/copy-diff.py --mode substring concept-d/index.html && python3 qa/concept-d-script-diff.py</automated>
  </verify>
  <done>FAQs/convert/footer are plain below-fold sections in the locked order with video occlusion; fold assertion passes at both breakpoints with captures READ and judged; hover verdict staged, captured, and recorded; 8 dialogs remain (5 section + 3 case); both copy gates green.</done>
</task>

</tasks>

<verification>
- `python3 qa/copy-diff.py --mode substring concept-d/index.html` green (all moved copy verbatim)
- `python3 qa/concept-d-script-diff.py` green (gated deployed.js regions untouched)
- No `data-case` anywhere in index.html; `grep -c '<dialog'` = 8; page order hero → cards → clients → work-reel → faqs → convert → footer
- Fold assertion holds at 1440x900 and 1280x800; captures READ, verdicts recorded
- New-chrome greps clean on concept-d.css: no gradients, no `color:#hex`, no border-top/bottom rule lines
</verification>

<success_criteria>
ITER-01 (fold), ITER-02 (uniform bottom-anchored cards + explicit CTA), ITER-03 (semi-opaque + staged video-echo hover), ITER-07 (plain below-fold sections), ITER-08 (logos untouched) fully met; ITER-06's section shell (reel + 3 functional case cards/shells) in place for 07-02's transition + quiet-content pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-concept-d-iteration-2/07-01-SUMMARY.md` — include the hover staging verdict (translucency-only vs + wave accent, with capture evidence), the final compact-hero values if tuned away from the research starting points, and the dialog inventory handed to 07-02 (5 section dialogs to reskin + 3 case shells to fill).
</output>
