---
phase: 07-concept-d-iteration-2
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - .planning/phases/07-concept-d-iteration-2/captures/
  - assets/gallery/concept-d.png
  - REVIEW.md
autonomous: true
requirements: [ITER-01, ITER-02, ITER-03, ITER-04, ITER-05, ITER-06, ITER-07, ITER-08]

must_haves:
  truths:
    - "Both copy gates green on the final page state; every canonical chunk on index.html verbatim"
    - "New-chrome brand greps clean (no rule lines, no gradients, no non-token colors, no banned tagline/deprecated hex, sine-only view-transition timing); deployed.css/js and ported inline modules exempt"
    - "Fold-proof at 1440x900 AND 1280x800: hero + full 5-card grid inside the first viewport, captures READ and judged"
    - "Both videos behave: hero plays at load, reel is byte-idle until scrolled into view, plays in view, pauses out of view; reduced-motion shows posters"
    - "Card scales into a full-viewport window; Esc returns focus to the invoking card — asserted mechanically, morph frame captured"
    - "Root gallery thumbnail and REVIEW.md's Concept D section describe iteration 2"
  artifacts:
    - path: ".planning/phases/07-concept-d-iteration-2/captures"
      provides: ">=12 reviewed QA captures (fold x2, hover, morph, windows, reel, reduced-motion, no-JS, below-fold)"
    - path: "assets/gallery/concept-d.png"
      provides: "Refreshed 1440x900 (16:10) gallery thumbnail of the restructured homepage"
    - path: "REVIEW.md"
      provides: "Updated Concept D walkthrough paragraph (above-the-fold grid, work reel, quiet windows)"
      contains: "work reel"
  key_links:
    - from: "qa/copy-diff.py + qa/concept-d-script-diff.py"
      to: "concept-d/index.html + deployed.js data objects"
      via: "substring gate + byte-compare gate (Phase 5's documented compensating pair, unchanged)"
      pattern: "concept-d-script-diff"
    - from: "Puppeteer behavior suite (scratchpad)"
      to: "http://localhost:4340/concept-d/"
      via: "real installed Chrome executablePath + page.setViewport + autoplay policy flag"
      pattern: "executablePath"
---

<objective>
Phase-closing QA: mechanical copy/brand/link/budget gates, a Puppeteer behavior suite (fold, hover, both videos' IO gating, transition + focus semantics, reduced-motion, no-JS), honest capture review with fixes, then refresh the root gallery thumbnail and REVIEW.md's Concept D paragraph.

Purpose: prove ITER-01..08 mechanically and leave Jon a current review surface. LOCAL ONLY — no deploy anywhere (Jon's explicit instruction; interceptgroup.com deploys only via intercept-deploy MCP on Jon's go).
Output: green gate suite, >=12 reviewed captures, refreshed assets/gallery/concept-d.png, updated REVIEW.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-concept-d-iteration-2/07-01-SUMMARY.md
@.planning/phases/07-concept-d-iteration-2/07-02-SUMMARY.md
@.planning/phases/07-concept-d-iteration-2/07-RESEARCH.md
@qa/copy-diff.py
@qa/concept-d-script-diff.py

<interfaces>
<!-- QA scope contract for this phase -->

Grep scope (matches Phase 5/6 precedent + this phase's delta):
- EXEMPT: concept-d/assets/css/deployed.css, concept-d/assets/js/deployed.js (verbatim ports),
  about.html/chatb2b.html/insights-hub.html (mirrored), pages/*.html (deployed design kept, untouched this
  phase), the inline #faqs/#convert module markup (ported modules rendering "as they render today").
- BOUND (new chrome): concept-d/assets/css/concept-d.css, concept-d/assets/js/{cards,hero-video,reel-video,quiet-modules}.js,
  all new/reskinned markup in index.html (card system, work-reel section, dialog interiors).
- CHANGED vs Phase 5: the `! grep view-transition-name` check is RETIRED (07-02 legitimately introduced it);
  its replacement is a sine-timing assertion on the ::view-transition rules.

Capture rig (established): Puppeteer + REAL installed Chrome executablePath (bundled Chrome-for-Testing
hangs in this sandbox — 02-03 lesson), page.setViewport (never bare --window-size), launch arg
--autoplay-policy=no-user-gesture-required, server via ./serve.sh on :4340,
page.setJavaScriptEnabled(false) for no-JS, page.emulateMediaFeatures([{name:'prefers-reduced-motion', value:'reduce'}]) for reduced-motion.

Gallery thumbnail recipe (06-01 precedent): 1440-wide capture, PIL crop to the top 900px = exact 16:10,
overwrite assets/gallery/concept-d.png.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical gate suite — copy, brand, links, budgets</name>
  <files>.planning/phases/07-concept-d-iteration-2/captures/</files>
  <action>
Run the full mechanical suite from the repo root; fix any failure in NEW chrome only (never deployed.css/js, never mirrored/ported markup), then re-run to green:

1. **Copy gates:** `python3 qa/copy-diff.py --mode substring concept-d/index.html concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html` and `python3 qa/concept-d-script-diff.py`. (Case-window copy renders from the untouched CASES object, so script-diff staying green IS the case-copy verification.)
2. **Brand greps (new chrome):**
   - `! grep -rIiE 'fresh thinking starts here' concept-d` (banned tagline)
   - `! grep -rIiE 'a855f7|6366f1|22d3ee' concept-d` (deprecated mark hex)
   - `! grep -Ii '<hr' concept-d/index.html` (no rule elements)
   - concept-d.css: `! grep -IE '(^|[^-])color:[^;}]*#'` (token-only colors), `! grep -IE 'linear-gradient|radial-gradient|conic-gradient'` (flat translucency, not scrims/grades), `! grep -IE 'border-(top|bottom)[[:space:]]*:[[:space:]]*[^0n]'` (no rule lines)
   - Sine-only morph timing (replaces the retired view-transition-name ban): python snippet — extract every `::view-transition` rule block in concept-d.css and assert each `animation-timing-function` is exactly `cubic-bezier(0.37, 0, 0.63, 1)` and each `animation-duration` is `600ms` (the reduced-motion `animation: none` block is exempt).
   - `! grep -q 'data-case' concept-d/index.html` (openCase null-ref trap stays dead) and `! grep -rI 'data-copy' concept-d` (substring-mode exception maintained).
3. **Link integrity:** write a scratchpad stdlib-Python scanner (06-01 pattern) over concept-d/*.html + concept-d/pages/*.html: every internal href/src resolves on disk; every in-page `#fragment` href has a matching id in its target document; allowlist: `insights-hub.html#episodes` (documented JS hash-route, 06-01 allowlist), `about.html`'s `#main` skip-link (pre-existing staging bug, deferred-items.md), external http(s)/mailto. Zero non-allowlisted failures.
4. **Video budgets:** hero unchanged (webm <=4MB, poster <=200KB); reel: `work-reel-1080.webm` <=4MB, `work-reel-1080.mp4` <=4.5MB, `work-reel-poster.jpg` <=200KB; reel markup carries `preload="none"` + poster + muted playsinline loop and NO autoplay attribute; webm listed before mp4 for both videos.

Record every command + count in a QA results table for the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 qa/copy-diff.py --mode substring concept-d/index.html concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html && python3 qa/concept-d-script-diff.py && ! grep -rIiE 'fresh thinking starts here' concept-d && ! grep -rIiE 'a855f7|6366f1|22d3ee' concept-d && ! grep -Ii '<hr' concept-d/index.html && ! grep -IE '(^|[^-])color:[^;}]*#' concept-d/assets/css/concept-d.css && ! grep -IE 'linear-gradient|radial-gradient|conic-gradient' concept-d/assets/css/concept-d.css && ! grep -IE 'border-(top|bottom)[[:space:]]*:[[:space:]]*[^0n]' concept-d/assets/css/concept-d.css && ! grep -q 'data-case' concept-d/index.html && ! grep -rI 'data-copy' concept-d && python3 /private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad/vt-sine-check.py && python3 /private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad/concept-d-linkcheck.py && [ "$(stat -f%z concept-d/assets/video/work-reel-1080.webm)" -le 4000000 ] && [ "$(stat -f%z concept-d/assets/video/work-reel-1080.mp4)" -le 4500000 ] && [ "$(stat -f%z concept-d/assets/video/work-reel-poster.jpg)" -le 200000 ]</automated>
  </verify>
  <done>Both copy gates green across index + 4 section pages; full brand grep suite clean on new chrome; sine-timing assertion passes; link integrity zero non-allowlisted failures; both videos inside budget with correct attributes and source order.</done>
</task>

<task type="auto">
  <name>Task 2: Behavior suite + full capture set + honest review</name>
  <files>.planning/phases/07-concept-d-iteration-2/captures/</files>
  <action>
Write one scratchpad Puppeteer suite (rig per interfaces block) against http://localhost:4340/concept-d/ and run it; every assertion mechanical, every capture READ with the Read tool and judged honestly. Fixes go in new chrome only; re-run Task 1's gates on any touched file.

**Fold (ITER-01):** at 1440x900 AND 1280x800 assert `#modules` bottom <= innerHeight and `.hero-h1` top >= 0; capture `index-fold-1440x900.png`, `index-fold-1280x800.png` (viewport shots). READ both: compact hero + all 5 cards fully visible, uniform tile heights, bottom-anchored copy rhythm, CTAs legible.

**Hover (ITER-03):** page.hover a card, wait 1.3s, assert computed background-color alpha < resting alpha; capture `card-hover-1440.png` and READ (video field passing through, still AA-legible).

**Videos (ITER-06 + hero regression):** at load (top of page) assert hero video `paused === false` and reel `paused === true` AND `currentSrc` either empty or no bytes fetched (`readyState === 0` with preload="none"); scroll `#work-reel` into view, wait, assert reel `paused === false`; scroll back to top, wait, assert reel `paused === true`. Click the reel toggle twice, assert pause→play and aria-pressed sync. Capture `work-reel-1440.png` in view and READ (heading legible over montage, 3 case cards composed like section cards).

**Transition (ITER-04):** click the InterceptOS card; ~250ms later capture `morph-midflight-1440.png` (best-effort mid-morph frame); after settle assert `dialog.open === true` and dialog rect covers >=85% of both viewport axes; capture `window-os-1440.png` and READ (one stage visible, stepper rail, quiet chrome). Press Escape; assert dialog closed AND `document.activeElement` is the invoking card. Open a case window; capture `window-case-1440.png`; READ (stat anchor, one C/A/R section). Open Agents → agent detail → contact; assert #convoDrawer rect covers the full viewport (invariant regression); capture `window-agents-drawer-1440.png`.

**Reduced-motion:** emulateMediaFeatures reduce → reload → assert BOTH videos paused at load and card click opens the dialog instantly (no morph — `dialog.open` true immediately after click resolves); capture `index-reduced-1440.png`.

**No-JS:** setJavaScriptEnabled(false) → reload → capture `index-nojs-1440.png` (fullPage). READ: `.card-fallback`/`.card-static` tiles visible with dark-ink text, video toggles hidden, FAQs/convert/footer readable inline, no dead-looking UI.

**Mobile sanity:** capture `index-390.png` (fullPage, 390x844) and `window-problems-390.png` (open Problems at 390). READ: cards stack cleanly, window fits with reachable close button, no horizontal overflow.

Record every assertion result + capture verdict for the SUMMARY. Minimum 12 captures, all READ.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && node /private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad/behavior-suite.mjs && [ "$(ls .planning/phases/07-concept-d-iteration-2/captures/*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 12 ] && ! find .planning/phases/07-concept-d-iteration-2/captures -name '*.png' -size -8k | grep -q . && python3 qa/copy-diff.py --mode substring concept-d/index.html && python3 qa/concept-d-script-diff.py</automated>
  </verify>
  <done>Behavior suite passes end-to-end (fold both breakpoints, hover alpha shift, both videos' IO gating, morph coverage + Esc focus return, drawer invariant, reduced-motion instant, no-JS fallbacks); >=12 non-trivial captures exist and every one was READ and judged with issues fixed; copy gates green on final state.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh gallery thumbnail + REVIEW.md Concept D paragraph</name>
  <files>assets/gallery/concept-d.png, REVIEW.md</files>
  <action>
**Thumbnail:** with the behavior suite's rig, capture the restructured homepage at 1440 wide (fresh load, top of page, video poster visible) and PIL-crop the top 900px (exact 16:10, 06-01 recipe) over `assets/gallery/concept-d.png`. READ the result — it must show the compact hero + card grid, not the stale Phase-5 layout.

**REVIEW.md:** update ONLY the Concept D walkthrough section: one refreshed paragraph describing iteration 2 (compact hero with the full 5-card grid above the fold, the full-screen work-reel section with three case cards, cards scaling into quiet full-viewport module windows with progressive disclosure, FAQs/conversation/footer as plain below-fold sections) plus its what-to-try hints (hover a card; click a card and watch it grow; Esc to return; scroll to the work reel; try a case card; reduced-motion + no-JS paths). Point its evidence line at `.planning/phases/07-concept-d-iteration-2/captures/`. Do not touch the other concepts' sections or the known-notes entries (the about.html skip-link note stays valid). Reuse the existing doc's tone — plain, honest, no invented marketing copy, no rule lines/numbered lists where order isn't load-bearing.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 -c "from PIL import Image; im=Image.open('assets/gallery/concept-d.png'); w,h=im.size; assert abs(w/h - 1.6) < 0.01, (w,h); print('16:10 ok', w, h)" && [ "$(find assets/gallery/concept-d.png -newer .planning/phases/07-concept-d-iteration-2/07-02-PLAN.md | wc -l | tr -d ' ')" = "1" ] && grep -qi 'work reel' REVIEW.md && grep -qi 'quiet' REVIEW.md && grep -q '07-concept-d-iteration-2/captures' REVIEW.md</automated>
  </verify>
  <done>Gallery thumbnail shows the iteration-2 homepage at exact 16:10; REVIEW.md's Concept D section describes the new structure with what-to-try hints and points at this phase's captures; nothing else in REVIEW.md changed.</done>
</task>

</tasks>

<verification>
- Full mechanical suite green: 2 copy gates, brand greps (new-chrome scope), sine-timing assertion, link integrity, video budgets
- Behavior suite green: fold x2, hover, both-videos IO, morph + Esc/focus, drawer invariant, reduced-motion, no-JS
- >=12 captures READ and judged; all found issues fixed in new chrome and gates re-run
- assets/gallery/concept-d.png + REVIEW.md current
- Nothing deployed anywhere — all evidence local (:4340)
</verification>

<success_criteria>
ITER-01 through ITER-08 each backed by at least one passing mechanical assertion or reviewed capture; the phase's QA evidence table complete in the SUMMARY; Jon can open http://localhost:4340/ and see the current Concept D represented in the gallery and REVIEW.md.
</success_criteria>

<output>
After completion, create `.planning/phases/07-concept-d-iteration-2/07-03-SUMMARY.md` — full QA results table (gate counts, each grep, link counts, behavior assertions, capture list with verdicts, budget numbers), the retired-grep note (view-transition-name ban → sine-timing assertion), and any deferred items.
</output>
