---
phase: 01-content-foundation-shared-brand-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/tokens.css
  - shared/fonts.css
  - shared/motion.css
  - shared/logo/lockup.svg
  - shared/README.md
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - "shared/tokens.css begins as a byte-identical mirror of the brand-kit SSoT and ends with an alias block mapping Variant-A shorthand names onto canonical ramp tokens"
    - "shared/fonts.css loads the exact Google Fonts CSS2 URL the live site uses (Instrument Sans, Inter, Geist Mono) — no fabricated self-hosted font files"
    - "shared/logo/lockup.svg is the canonical 8-path mark (base #FF00E5 + 6 named accents) statically baked into the wordmark lockup — no JS, no hover wiring, no deprecated 12-path hexes"
    - "shared/README.md encodes the brand rules that bind all three concepts, the copy-in-markup constraint, and the data-copy annotation convention"
  artifacts:
    - path: "shared/tokens.css"
      provides: "Mirrored Fritz design tokens + Variant-A shorthand alias layer"
      contains: "--flarepop: var(--flarepop-100)"
    - path: "shared/fonts.css"
      provides: "Google Fonts CDN typography delivery (mirrors live behavior)"
      contains: "fonts.googleapis.com/css2"
    - path: "shared/motion.css"
      provides: "Sine ease-in-out easing + long-duration custom properties"
      contains: "--ease-inout-sine"
    - path: "shared/logo/lockup.svg"
      provides: "Static canonical 8-path lockup, centered/at-rest state"
      contains: "FF00E5"
    - path: "shared/README.md"
      provides: "Binding brand rules + authoring conventions for Phases 2-4"
      contains: "data-copy"
  key_links:
    - from: "shared/tokens.css alias block"
      to: "canonical ramp tokens in the same file"
      via: "var() references (aliases never hold raw hex)"
      pattern: "var\\(--(flarepop|coolsweep|wiretree)-100\\)"
    - from: "shared/logo/lockup.svg"
      to: "canonical 8-path mark"
      via: "static paths inside the mark group (no runtime JS fill)"
      pattern: "FF00E5"
---

<objective>
Mirror the shared Fritz brand layer from its sources of truth into `shared/`: design tokens (with the alias layer), fonts CSS, motion easing tokens, the canonical static 8-path logo lockup, and a README encoding the brand rules that bind all three concepts.

Purpose: Concepts A/B/C never re-declare brand values locally — they link `shared/`. Mirroring by file copy (not retyping) is how token drift is prevented; the README is how Phase 2-4 executors inherit the non-negotiable brand rules.
Output: `shared/tokens.css`, `shared/fonts.css`, `shared/motion.css`, `shared/logo/lockup.svg`, `shared/README.md`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-content-foundation-shared-brand-layer/01-RESEARCH.md

Source files:
- `~/Creative-Projects/intercept-brand-kit/tokens.css` — SSoT for tokens (5,450 bytes; read it in full before writing aliases)
- `reference/live-homepage/index.html` — logo markup + fonts link (read ONLY the line ranges below; 2MB file)

<interfaces>
Canonical token names in intercept-brand-kit/tokens.css (verified 2026-07-23):
- Color ramps (100-500 each): --flarepop-*, --bitvoid-*, --bitbloom-*, --wiretree-*, --nightwire-*, --coolsweep-*, --laserwave-*, --siren-* (+ carbon/halo neutrals)
- Semantic: --bg-page (carbon-500 #0a0a0f), --bg-surface (carbon-300 #1f1f2a), --bg-surface-hi, --bg-surface-max, --fg-primary (#ffffff), --fg-secondary, --fg-muted
- Fonts: --font-display 'Instrument Sans', --font-body 'Inter', --font-mono 'Geist Mono'

Live snapshot anchors (reference/live-homepage/index.html):
- Google Fonts link, line 32 (preconnects lines 30-31): https://fonts.googleapis.com/css2?family=Instrument+Sans:ital,wght@0,400;0,500;0,600;0,700;1,500;1,600;1,700&family=Inter:wght@400;500;600&family=Geist+Mono:wght@400;500&display=swap
- `<symbol id="intercept-lockup" viewBox="0 -9 324.005 76.2147">` — wordmark paths, lines 2158-2214
- `<svg id="fritz-glitch-source" data-accent-palette="#8846C9 #1DABC1 #5154C4 #FF44F9 #FFA9F8 #FF52F9" data-channels="#FF00E5 #1A7AFF #00D862">` — line 2215
- `<g class="fritz-layout fritz-canon" data-fill="#FF00E5">` — the canonical 8-path mark (1 path.base + 7 path.accent), lines 2299-2318
- Header lockup usage with empty `<g id="mark-slot" transform="translate(0, -8.78)">` — line 2320 (JS fills it at runtime; that JS is the SUSPENDED web_animated behavior — do NOT port it)
- Variant A's own shorthand custom properties live at the top of its inline <style> block (approx lines 33-60): --flarepop, --coolsweep, --wiretree, --page, --surface, --fg, --line, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror tokens.css + alias block; create fonts.css and motion.css</name>
  <files>shared/tokens.css, shared/fonts.css, shared/motion.css</files>
  <action>
1. `shared/tokens.css`: copy `~/Creative-Projects/intercept-brand-kit/tokens.css` byte-for-byte (`cp`, never retype), then APPEND an alias block. Read the mirrored file first to confirm canonical names, then append:

```css

/* ── Variant-A shorthand aliases ─────────────────────────────
   Maps the shorthand custom-property names Variant A's own CSS
   uses onto the brand-kit canonical ramp. Aliases NEVER hold raw
   hex — every alias points at a canonical token above. */
:root {
  --flarepop:  var(--flarepop-100);
  --coolsweep: var(--coolsweep-100);
  --wiretree:  var(--wiretree-100);
  --page:      var(--bg-page);
  --surface:   var(--bg-surface);
  --fg:        var(--fg-primary);
}
```

Read Variant A's :root shorthand block (reference/live-homepage/index.html, approx lines 33-60) and add aliases for any OTHER shorthand names it defines that map cleanly to a canonical token (e.g. --line → var(--bg-surface-hi), --muted → var(--fg-muted)); each added alias must point at a canonical var(), never a raw hex, with a comment if the mapping is a judgment call. Skip shorthands with no sane canonical equivalent rather than inventing tokens. (Note: the comment block above uses a decorative dash run inside a CSS comment — that is fine in code comments; the no-rule-lines rule governs rendered design, not source comments. If in doubt, use a plain comment.)

2. `shared/fonts.css`: mirror the CDN delivery the live site actually uses (locked decision — there are no self-hosted font files anywhere in the source; do NOT fabricate .woff2 files):

```css
/* Mirrors the exact Google Fonts delivery the live homepage uses (line 32 of the snapshot). */
/* HTML heads should also add the two preconnect hints:
   <link rel="preconnect" href="https://fonts.googleapis.com">
   <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> */
@import url("https://fonts.googleapis.com/css2?family=Instrument+Sans:ital,wght@0,400;0,500;0,600;0,700;1,500;1,600;1,700&family=Inter:wght@400;500;600&family=Geist+Mono:wght@400;500&display=swap");
```

Verify the URL against line 32 of the snapshot before writing — copy it exactly.

3. `shared/motion.css`: motion tokens per the standing rule (sine ease-in-out, long durations, no jarring landings):

```css
:root {
  --ease-inout-sine: cubic-bezier(0.37, 0, 0.63, 1);
  --dur-short: 400ms;
  --dur-med:   800ms;
  --dur-long:  1400ms;
}
```
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && SRC="$HOME/Creative-Projects/intercept-brand-kit/tokens.css" && head -c "$(wc -c < "$SRC")" shared/tokens.css | cmp - "$SRC" && grep -q -- '--flarepop:  var(--flarepop-100)' shared/tokens.css && grep -q -- '--page:' shared/tokens.css && ! grep -Eq -- '--(flarepop|coolsweep|wiretree|page|surface|fg): *#' shared/tokens.css && grep -q 'fonts.googleapis.com/css2' shared/fonts.css && grep -q 'Instrument+Sans' shared/fonts.css && grep -q 'Geist+Mono' shared/fonts.css && grep -q -- '--ease-inout-sine' shared/motion.css && echo OK</automated>
  </verify>
  <done>tokens.css = SSoT byte-prefix + alias block (aliases all var()-based); fonts.css carries the exact live CDN URL; motion.css defines sine easing + duration tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Build the static canonical 8-path logo lockup</name>
  <files>shared/logo/lockup.svg</files>
  <action>
Read lines 2158-2330 of `reference/live-homepage/index.html`. Build `shared/logo/lockup.svg` as a standalone, static SVG:

1. Take the wordmark paths from `<symbol id="intercept-lockup" viewBox="0 -9 324.005 76.2147">` (lines 2158-2214). The wordmark fills use `var(--logo-ink)` in the source — replace with `currentColor` so the lockup inherits text color (set `color` on the consumer; defaults render white on dark via --fg).
2. Take the 8 paths from `<g class="fritz-layout fritz-canon">` (lines 2299-2318): exactly 1 `path.base` filled `#FF00E5` + 7 `path.accent` using the six accent hexes `#8846C9 #1DABC1 #5154C4 #FF44F9 #FFA9F8 #FF52F9`. Bake explicit `fill` attributes on each path (the source may set fills via class/data attributes + JS — resolve them to static fill attrs: base = #FF00E5; each accent keeps whatever accent hex the live at-rest state gives it, using the data-accent-palette order if per-path fills aren't explicit).
3. Place the 8-path mark statically where the live header's `<g id="mark-slot" transform="translate(0, -8.78)">` sits (line 2320) — i.e. bake the centered/at-rest state into a `<g id="mark" transform="translate(0, -8.78)">` group inside the lockup. This is the static `centered` variant; web_animated hover behavior is SUSPENDED — no `<script>`, no `data-fritz-hover-lockup`, no JS file.
4. Root element: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 -9 324.005 76.2147" role="img" aria-label="Intercept">` (adjust viewBox only if the baked mark demonstrably overflows — check path coordinate extents; the live symbol's viewBox already accounts for the mark slot).

Sanity gates (both must hold):
- Deprecated 12-path mark colors `#A855F7`, `#6366F1`, `#22D3EE` appear NOWHERE in the file.
- The `<g id="mark">` group contains exactly 8 `<path>` elements.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 -c "
import re, xml.etree.ElementTree as ET
svg = open('shared/logo/lockup.svg').read()
assert not re.search(r'(?i)A855F7|6366F1|22D3EE', svg), 'deprecated 12-path mark colors present'
assert '<script' not in svg and 'data-fritz-hover' not in svg, 'lockup must be static'
low = svg.lower()
assert '#ff00e5' in low
for h in ('#8846c9','#1dabc1','#5154c4','#ff44f9','#ffa9f8','#ff52f9'): assert h in low, h
tree = ET.parse('shared/logo/lockup.svg')
ns = {'s':'http://www.w3.org/2000/svg'}
mark = tree.getroot().find(\".//s:g[@id='mark']\", ns)
assert mark is not None, 'no <g id=mark>'
assert len(mark.findall('.//s:path', ns)) == 8, 'mark must be exactly 8 paths'
print('OK')"</automated>
  </verify>
  <done>lockup.svg is well-formed, static (no JS/hover), contains the full wordmark + exactly-8-path canon mark with base #FF00E5 and all six accent hexes, and zero deprecated colors.</done>
</task>

<task type="auto">
  <name>Task 3: Write shared/README.md — binding brand rules + authoring conventions</name>
  <files>shared/README.md</files>
  <action>
Write `shared/README.md` — the contract Phases 2-4 executors inherit. Sections (use headings/space/weight, no decorative rule lines, no gratuitous numbering):

**How to consume this layer** — link order in every concept page `<head>`: `shared/tokens.css` → `shared/fonts.css` → `shared/motion.css`; add the two Google Fonts preconnect hints; embed the logo via `<img src="/shared/logo/lockup.svg" alt="Intercept">` or inline SVG; never re-declare brand hex values locally — always `var()` (canonical ramp names or the shorthand aliases, both defined in tokens.css).

**Brand rules (binding on all three concepts):**
- Flarepop (`--flarepop`) is the ONLY colored text; everything else is ink/neutrals
- Triangles: apex-up with the right angle at the base; lean left/right only — never inverted, never rotated
- The mark is NEVER decoration or an icon (patterning or CD approval are the only exceptions)
- "No gradients" means 3-9 hard-edged equal steps — never a smooth grade
- NO decorative rule lines / underlines / divider accents — use space, weight, and alignment
- NO invented decorative marks
- The tagline "Fresh thinking starts here." is BANNED — it must not appear anywhere
- Motion: sine ease-in-out (`--ease-inout-sine`), long durations (`--dur-med`/`--dur-long`), no jarring landings
- The web_animated glitch lockup is SUSPENDED — use the static lockup.svg only
- Deprecated 12-path mark colors `#A855F7 #6366F1 #22D3EE` must appear nowhere; gate: `grep -rEi 'A855F7|6366F1|22D3EE' <files>` must return nothing

**Copy rules (binding on all three concepts):**
- Copy is immutable: transcribe verbatim from `content/homepage.json` — no paraphrasing, no case changes, no "improvements"; never fabricate extension copy
- Canonical copy MUST live as literal HTML text nodes in the page markup. JS may drive interaction/visibility, but must NEVER be the sole storage location for copy (a static-file gate verifies pages; JS-injected copy is invisible to it and will fail review)
- Annotate every copy-bearing element with `data-copy="<dot-path>"` into homepage.json (e.g. `data-copy="hero.sub"`, `data-copy="work.cases.0.summary"`); paths must resolve to string leaves. Truncation at a natural boundary is permitted ONLY with `data-copy-truncated="true"` and must be an exact prefix of the canonical string
- Gate: `python3 qa/copy-diff.py <page.html>` (or `--all`) must exit 0 before anything is shown to Jon

**Architecture rules:** concepts consume ONLY `content/` + `shared/` — never each other; each concept is fully self-contained in its own directory with its own assets.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && grep -q 'Fresh thinking starts here' shared/README.md && grep -q 'data-copy' shared/README.md && grep -qi 'literal HTML text nodes' shared/README.md && grep -qi 'apex-up' shared/README.md && grep -q 'A855F7' shared/README.md && grep -qi 'verbatim' shared/README.md && echo OK</automated>
  </verify>
  <done>README encodes every binding brand rule from the phase context, the copy-in-markup constraint, the data-copy convention (mandated for Phases 2-4), and consumption instructions.</done>
</task>

</tasks>

<verification>
- `head -c $(wc -c < ~/Creative-Projects/intercept-brand-kit/tokens.css) shared/tokens.css | cmp - ~/Creative-Projects/intercept-brand-kit/tokens.css` — SSoT mirror is byte-identical
- `grep -rEi 'A855F7|6366F1|22D3EE' shared/` returns nothing
- All five shared/ files exist and are non-empty
</verification>

<success_criteria>
- FOUND-03: shared Fritz brand layer (tokens + aliases, fonts, motion, canonical static 8-path logo, binding rules README) mirrored from the SSoT and ready for import by all three concepts
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-foundation-shared-brand-layer/01-02-SUMMARY.md`
</output>
