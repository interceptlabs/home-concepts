---
phase: 01-content-foundation-shared-brand-layer
plan: 04
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - index.html
  - serve.sh
  - concept-a/index.html
  - concept-b/index.html
  - concept-c/index.html
autonomous: true
requirements: [FOUND-05]

must_haves:
  truths:
    - "./serve.sh starts a static server on port 4340 serving the repo root"
    - "http://localhost:4340/ renders the Fritz-branded gallery with three concept cards linking to concept-a/, concept-b/, concept-c/"
    - "Each concept placeholder page loads and links shared/tokens.css + shared/fonts.css (proving the shared-layer import path)"
    - "The gallery contains no banned tagline, no decorative rule lines, no deprecated mark hexes, and colored text only in Flarepop"
  artifacts:
    - path: "serve.sh"
      provides: "One-command preview server on port 4340"
      contains: "http.server 4340"
    - path: "index.html"
      provides: "Review gallery — Jon's side-by-side entry point (placeholder states until concepts land)"
      contains: "concept-a/"
    - path: "concept-a/index.html"
      provides: "Concept A placeholder proving shared-layer consumption"
      contains: "shared/tokens.css"
    - path: "concept-b/index.html"
      provides: "Concept B placeholder proving shared-layer consumption"
      contains: "shared/tokens.css"
    - path: "concept-c/index.html"
      provides: "Concept C placeholder proving shared-layer consumption"
      contains: "shared/tokens.css"
  key_links:
    - from: "index.html"
      to: "shared/tokens.css"
      via: "<link rel=stylesheet> (tokens → fonts → motion order)"
      pattern: "shared/tokens\\.css"
    - from: "index.html"
      to: "concept-a/ concept-b/ concept-c/"
      via: "card link hrefs"
      pattern: "href=\"concept-[abc]/\""
    - from: "serve.sh"
      to: "repo root on port 4340"
      via: "python3 -m http.server"
      pattern: "http\\.server 4340"
---

<objective>
Stand up the repo skeleton (concept-a/b/c directories with shared-layer-consuming placeholders), the one-command preview server on port 4340, and the Fritz-branded root gallery — the side-by-side review surface Jon will use.

Purpose: FOUND-05 — one port serves everything. The gallery is a real Phase-1 deliverable (it graduates into the Phase-5 review surface), and the placeholders prove the shared-layer import path works before any concept is built against it.
Output: `index.html`, `serve.sh`, `concept-a/index.html`, `concept-b/index.html`, `concept-c/index.html`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@shared/README.md
@shared/tokens.css

<interfaces>
From plan 01-02 (on disk before this plan runs):
- `shared/tokens.css` — canonical ramp (--flarepop-100 … ) + shorthand aliases (--flarepop, --page, --surface, --fg)
- `shared/fonts.css` — Google Fonts CDN @import (Instrument Sans / Inter / Geist Mono); pages should add preconnect hints for fonts.googleapis.com + fonts.gstatic.com
- `shared/motion.css` — --ease-inout-sine, --dur-short/med/long
- `shared/logo/lockup.svg` — static lockup; wordmark inherits currentColor
- Binding rules in shared/README.md: Flarepop-only colored text; NO decorative rule lines; no gradients (hard steps only if any); tagline "Fresh thinking starts here." BANNED; mark never decoration; sine easing, long durations

Port fact (verified): 4340 is free; nearest listener is 4330 (meredith site).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo skeleton + serve.sh</name>
  <files>serve.sh, concept-a/index.html, concept-b/index.html, concept-c/index.html</files>
  <action>
1. Create the concept skeleton: `concept-a/`, `concept-b/`, `concept-c/`, each containing:
   - `index.html` — a minimal placeholder page that consumes the shared layer for real: `<head>` links `/shared/tokens.css`, `/shared/fonts.css`, `/shared/motion.css` (root-relative, in that order) + the two Google Fonts preconnect hints. Body: dark page (`background: var(--page); color: var(--fg); font-family: var(--font-display)`), the concept name as a heading ("Concept A — Editorial" / "Concept B — Full-Screen Video" / "Concept C — Experimental WebGL/3D"), one line "Landing in Phase 2" (/3/4 respectively), and a link back to the gallery (`href="/"`). Placeholder text is chrome, not canonical copy — do NOT use strings from content/homepage.json here (keeps the copy-diff fallback scan quiet until real builds land).
   - `pages/.gitkeep` and `assets/.gitkeep` so the structure is committed.
2. Create `serve.sh` at the repo root:
```bash
#!/usr/bin/env bash
# Intercept homepage concepts — local review server
cd "$(dirname "$0")"
echo "Serving http://localhost:4340 (gallery + concepts)"
exec python3 -m http.server 4340
```
`chmod +x serve.sh`.
3. Quick serve check: start the server in the background, curl each placeholder, kill the server (see verify).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && bash -n serve.sh && test -x serve.sh && (python3 -m http.server 4340 >/dev/null 2>&1 & echo $! > /tmp/ihc-srv.pid) && sleep 1 && curl -sf http://localhost:4340/concept-a/ | grep -q 'shared/tokens.css' && curl -sf http://localhost:4340/concept-b/ | grep -q 'shared/tokens.css' && curl -sf http://localhost:4340/concept-c/ | grep -q 'shared/tokens.css' && curl -sf http://localhost:4340/shared/tokens.css >/dev/null && kill "$(cat /tmp/ihc-srv.pid)" && echo OK</automated>
  </verify>
  <done>Three concept placeholders (each with pages/ + assets/ dirs) load over the server and link the shared layer; serve.sh is executable and serves the repo root on 4340.</done>
</task>

<task type="auto">
  <name>Task 2: Fritz-branded review gallery (root index.html)</name>
  <files>index.html</files>
  <action>
Create the root `index.html` — the side-by-side review gallery. Single self-contained page (inline `<style>` is fine) consuming the shared layer.

Head: title "Intercept — Homepage Concepts"; preconnect hints; `/shared/tokens.css` → `/shared/fonts.css` → `/shared/motion.css`.

Design (dark, minimal, Fritz — obey every rule in shared/README.md):
- Page: `background: var(--page); color: var(--fg)`; headings in `var(--font-display)`; small labels in `var(--font-mono)`
- Header: the lockup (`<img src="/shared/logo/lockup.svg" alt="Intercept" ...>` sized ~160-200px wide, or inline SVG) as site identification — this is identity use, not decoration
- A short page heading (e.g. "Homepage concepts." + a one-line sub: "Three directions, one content source. Side-by-side review.") — this is gallery chrome authored fresh, NOT canonical copy; do not reuse content/homepage.json strings
- Three cards in a responsive grid (3-up desktop, 1-up mobile): each card = concept letter + name, one-line description, status chip, and the whole card links to its directory:
  - Concept A — Editorial · "An editorial card grid with one strong hero statement. Accenture, but better." · status "In build — Phase 2" · href `concept-a/`
  - Concept B — Full-Screen Video · "A full-bleed ambient video hero with labeled hotspots and progressive reveal." · status "In build — Phase 3" · href `concept-b/`
  - Concept C — Experimental WebGL/3D · "A three.js spatial navigation metaphor with an accessible DOM mirror." · status "In build — Phase 4" · href `concept-c/`
- Cards use `var(--surface)` panels, separation by SPACE and surface contrast — NO border-bottom accents, NO `<hr>`, NO decorative rule lines anywhere
- Colored text: Flarepop (`var(--flarepop)`) ONLY, used sparingly (e.g. the status chip or card hover accent); every other text is fg/neutrals
- Hover: subtle transform/opacity transition using `var(--ease-inout-sine)` and `var(--dur-med)` — no jarring motion
- No triangles, no invented marks, no gradients, no numbered section headings; the banned tagline must not appear
- Placeholder state is honest: cards are fully clickable and land on the Phase-1 placeholder pages; no fake screenshots (representative captures come in Phase 5)
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && (python3 -m http.server 4340 >/dev/null 2>&1 & echo $! > /tmp/ihc-srv.pid) && sleep 1 && G=$(curl -sf http://localhost:4340/) && kill "$(cat /tmp/ihc-srv.pid)" && echo "$G" | grep -q 'href="concept-a/"' && echo "$G" | grep -q 'href="concept-b/"' && echo "$G" | grep -q 'href="concept-c/"' && echo "$G" | grep -q 'shared/tokens.css' && echo "$G" | grep -q 'lockup.svg' && ! echo "$G" | grep -qi 'Fresh thinking starts here' && ! echo "$G" | grep -q '<hr' && ! echo "$G" | grep -Eqi 'A855F7|6366F1|22D3EE' && echo OK</automated>
  </verify>
  <done>Gallery serves at :4340/, shows three linked concept cards with the lockup and shared-layer styling, and passes the mechanical brand greps (no banned tagline, no hr/rule lines, no deprecated hexes).</done>
</task>

</tasks>

<verification>
- `./serve.sh` (foreground) then browse http://localhost:4340/ — gallery renders; all three cards click through to placeholders; placeholders link back to the gallery
- `curl -sf http://localhost:4340/shared/logo/lockup.svg | head -1` — shared assets serve over the same port
- Both task verify blocks pass
</verification>

<success_criteria>
- FOUND-05: one local static server on port 4340 serves the repo skeleton (concept-a/, concept-b/, concept-c/ with pages/ + assets/ directories) plus a Fritz-branded placeholder gallery index — the surface Jon reviews side-by-side in Phase 5
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-foundation-shared-brand-layer/01-04-SUMMARY.md`
</output>
