---
phase: 06-cross-concept-qa-review-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - assets/gallery/concept-a.png
  - assets/gallery/concept-b.png
  - assets/gallery/concept-c.png
  - assets/gallery/concept-d.png
  - .planning/phases/06-cross-concept-qa-review-packaging/captures/gallery-390.png
  - .planning/phases/06-cross-concept-qa-review-packaging/captures/gallery-1440.png
autonomous: true
requirements: [QA-02, QA-03, QA-04]

must_haves:
  truths:
    - "Running the full mechanical suite from the repo root exits 0 on every gate: copy-diff annotated for A/B/C (expected 430/340/282 chunks), copy-diff substring for D (expected 71 chunks over 5 pages), concept-d-script-diff (13 checks), camera-framing-check, cross-concept link integrity (all four concepts + root), all four per-concept brand grep suites, the token-drift grep, and both video LCP/budget checks"
    - "http://localhost:4340/ renders a dark Fritz gallery with FOUR concept cards (A editorial / B video / C WebGL / D home variant), each showing a real capture thumbnail, a factual one-line description, 2-3 what-to-try hints, and a link that resolves"
    - "The rebuilt gallery itself passes the Fritz mechanical greps (no rule lines, Flarepop-only colored text, no banned tagline, no smooth gradients) and shows the canonical 8-path lockup visibly — inlined SVG, never an <img src> reference to shared/logo/lockup.svg"
  artifacts:
    - path: "index.html"
      provides: "Final Fritz review gallery — 4 concept cards with thumbnails, descriptions, hints, links, serve-instructions footer"
      contains: "href=\"concept-d/\""
    - path: "assets/gallery/concept-a.png"
      provides: "Concept A thumbnail copied from the best existing 1440 capture"
    - path: "assets/gallery/concept-d.png"
      provides: "Concept D thumbnail copied from the best existing 1440 capture"
    - path: ".planning/phases/06-cross-concept-qa-review-packaging/captures/gallery-1440.png"
      provides: "Reviewed desktop capture of the final gallery"
  key_links:
    - from: "index.html"
      to: "concept-d/"
      via: "fourth concept card href"
      pattern: "href=\"concept-d/\""
    - from: "index.html"
      to: "assets/gallery/*.png"
      via: "card thumbnail <img> elements"
      pattern: "assets/gallery/concept-"
    - from: "index.html"
      to: "/shared/tokens.css"
      via: "stylesheet link (dark Fritz page consistent with Phase 1 gallery)"
      pattern: "shared/tokens.css"
---

<objective>
Run the full mechanical cross-concept QA suite across ALL FOUR concepts (A/B/C/D) and record every result verbatim, then rebuild the root `index.html` as the final Fritz review gallery with four concept cards carrying real capture thumbnails.

Purpose: QA-02 (copy verbatim mechanically proven across all concepts), QA-03 (responsive/sub-page/LCP evidence consolidated + fresh spot-checks), QA-04 (the side-by-side review surface Jon actually opens). This plan is the mechanical layer of QA-01 — the Fritz brand agent review happens BETWEEN waves, run by the orchestrator, never by this executor.
Output: a QA results table for the SUMMARY (every gate, every count), the rebuilt gallery, 4 thumbnails in assets/gallery/, 2 reviewed gallery captures.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cross-concept-qa-review-packaging/06-CONTEXT.md
@qa/README.md
@index.html

<interfaces>
<!-- Everything the executor needs — no codebase exploration required. -->

REPO ROOT: /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts — run everything from there.
SCRATCHPAD (proven writable, used by prior plans): /private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad

EXPECTED GATE RESULTS (from the phase-closing SUMMARYs — any drift from these counts means the tree changed since phase close and must be investigated, not shrugged off):
| Gate | Command | Expected |
|---|---|---|
| Copy A | `python3 qa/copy-diff.py --all concept-a` | 6 pages, 430 chunks, 0 failures |
| Copy B | `python3 qa/copy-diff.py --all concept-b` | 4 pages, 340 chunks, 0 failures |
| Copy C | `python3 qa/copy-diff.py --all concept-c` | 4 pages, 282 chunks, 0 failures |
| Copy D | `python3 qa/copy-diff.py --mode substring concept-d/index.html concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html` | 5 pages, 71 chunks, 0 failures |
| Script D | `python3 qa/concept-d-script-diff.py` | 13 checks, 0 failures |
| Camera C | `node qa/camera-framing-check.mjs` | exit 0 |

PER-CONCEPT BRAND GREP SUITES (verbatim from each concept's own proven phase-closing plan — do NOT invent a stricter uniform suite; concept-d's suite is new-chrome-only with ported deployed.css/deployed.js and the 3 mirrored pages exempt, while tagline/deprecated-hex/view-transition greps run everywhere):
- concept-a (02-03): `! grep -riE 'fresh thinking starts here' concept-a && ! grep -riE 'a855f7|6366f1|22d3ee' concept-a && ! grep -ri '<hr' concept-a && ! grep -rE '(^|[^-])color:[^;}]*#' concept-a/assets/css && ! grep -rE '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)' concept-a/assets/css && ! grep -rE 'radial-gradient|conic-gradient' concept-a/assets/css && ! grep -r 'view-transition-name' concept-a`
- concept-b (03-03): `! grep -rIiE 'fresh thinking starts here' concept-b && ! grep -rIiE 'a855f7|6366f1|22d3ee' concept-b && ! grep -rIi '<hr' concept-b && ! grep -rIE 'border-(top|bottom)[[:space:]]*:' concept-b && ! grep -rIE '(^|[^-])color:[^;}]*#' concept-b && ! grep -rIE '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' concept-b && ! grep -rIE 'gradient\(' concept-b && ! grep -rI 'view-transition-name' concept-b`
- concept-c (04-03, vendor-excluded; the sole licensed `gradient(` hit is `.field-backdrop`'s repeating-linear-gradient stepped utility, which the `(^|[^-])linear-gradient\(` guard already skips): `! grep -rIiE --exclude-dir=vendor 'fresh thinking starts here' concept-c && ! grep -rIiE --exclude-dir=vendor 'a855f7|6366f1|22d3ee' concept-c && ! grep -rIi --exclude-dir=vendor '<hr' concept-c && ! grep -rIE --exclude-dir=vendor 'border-(top|bottom)[[:space:]]*:' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*#' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])linear-gradient\(|radial-gradient|conic-gradient' concept-c && ! grep -rI --exclude-dir=vendor 'view-transition-name' concept-c`
- concept-d (05-03, new-chrome-only): `! grep -rIiE 'fresh thinking starts here' concept-d && ! grep -rIiE 'a855f7|6366f1|22d3ee' concept-d && ! grep -rI 'view-transition-name' concept-d && ! grep -rI 'data-copy' concept-d && ! grep -Ii '<hr' concept-d/index.html concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html && ! grep -IE '(^|[^-])color:[^;}]*#' concept-d/assets/css/concept-d.css && ! grep -IE 'linear-gradient|radial-gradient|conic-gradient' concept-d/assets/css/concept-d.css && ! grep -IE 'border-(top|bottom)[[:space:]]*:[[:space:]]*[^0n]' concept-d/assets/css/concept-d.css`

TOKEN-DRIFT CHECK (CONTEXT's mechanical layer — no concept CSS may redefine a shared brand-hue custom property; concepts defining their OWN local non-brand properties is fine; concept-d's ported deployed.css is exempt): `! grep -rIE '^[[:space:]]*--(flarepop|coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire)(-[0-9]+)?[[:space:]]*:' concept-a/assets/css concept-b/assets/css concept-c/assets/css concept-d/assets/css/concept-d.css`

VIDEO LCP/BUDGET RE-CHECKS (proven idioms; this is QA-03's lightweight LCP sanity — full Lighthouse is out of scope):
- concept-b: `[ $(( $(stat -f%z concept-b/assets/video/hero-loop-1080.webm) + $(stat -f%z concept-b/assets/video/hero-poster.jpg) )) -le 7340032 ]` plus `grep -q 'preload="metadata"' concept-b/index.html && grep -q 'fetchpriority="high"' concept-b/index.html`
- concept-d: `[ "$(stat -f%z concept-d/assets/video/hero-light-loop.webm)" -le 4000000 ] && [ "$(stat -f%z concept-d/assets/video/hero-light-poster.jpg)" -le 200000 ]` plus `grep -q 'autoplay muted playsinline loop' concept-d/index.html` and the webm-before-mp4 source-order assert: `python3 -c "h=open('concept-d/index.html').read(); assert h.index('hero-light-loop.webm') < h.index('hero-light-loop.mp4'); print('webm-first ok')"`

LINK-INTEGRITY ALLOWLIST (documented in 05-03-SUMMARY + 05 deferred-items.md — reproduce exactly, no new entries without documenting them in the SUMMARY):
- concept-d legal-page links (3), un-mirrored insights article hrefs from insights-hub.html, and `about.html`'s `#main` skip-link (verified pre-existing dead anchor in the staging source itself — see .planning/phases/05-concept-d-home-variant/deferred-items.md)

THUMBNAIL SOURCES (best existing 1440 captures; all are real reviewed QA artifacts):
- A: .planning/phases/02-concept-a-editorial-accenture-but-better/captures/index-1440.png
- B: .planning/phases/03-concept-b-full-screen-video/captures/index-1440.png
- C: .planning/phases/04-concept-c-experimental-webgl-3d/captures/index-scene-1440.png (JS-on scene — the fallback JS-off capture misrepresents the concept)
- D: .planning/phases/05-concept-d-home-variant/captures/index-1440.png

LOCKUP LANDMINE (03-03 discovery): `shared/logo/lockup.svg`'s wordmark is `<g fill="currentColor">` — referenced via `<img src>` it renders BLACK (invisible on the dark page) because an externally-referenced SVG can't inherit host CSS. The current placeholder gallery has this bug. The rebuilt gallery MUST inline the SVG markup directly in index.html (concept-b's proven scoped fix). NEVER edit shared/logo/lockup.svg itself.

CAPTURE RIG (proven 02-03/03-03/04-03/05-03): Puppeteer in the scratchpad only (never the repo), `executablePath` pointing at the real installed Google Chrome (the bundled Chrome-for-Testing hangs on captureScreenshot in this sandbox), `page.setViewport()` (never bare `--window-size` below ~500px), fullPage. The gallery uses absolute `/shared/...` paths so captures MUST load via http://localhost:4340/, not file://.
</interfaces>
</context>

<constraints>
- Do NOT spawn any agents (no Task tool, no subagents). All work in this plan is direct execution. The Fritz brand agent review is the ORCHESTRATOR's job between waves.
- Do NOT modify any concept file (concept-a/, concept-b/, concept-c/, concept-d/, shared/, content/, qa/). A failing gate on concept internals is a finding to report in the SUMMARY (and, if genuinely blocking, a stop condition) — not a license to edit concepts in this plan.
- Gallery text is meta-UI wayfinding (CONTEXT locked): minimal and factual, never site copy. The copy-immutability rule governs site content; do not paste canonical homepage.json copy into the gallery.
- Fritz rules bind the gallery page itself: NO rule lines (`<hr>`, border-top/bottom hairlines), Flarepop-only colored text, static inlined lockup, no banned tagline ("Fresh thinking starts here." in any casing), hard-step-only if any gradient is used (none is needed), sine easing for the one hover transition.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Full mechanical QA suite across all four concepts</name>
  <files>(scratchpad script + SUMMARY evidence only — zero repo files modified)</files>
  <action>
Run every gate from the repo root and record each result VERBATIM (exact counts, exact pass/fail lines) for the SUMMARY's QA results table:

1. **Copy gates (QA-02):** the four copy-diff invocations plus `qa/concept-d-script-diff.py` from the interfaces table. Assert the totals match the expected counts (430/340/282 annotated, 71 substring over 5 pages, 13 script-diff checks). A pass with DIFFERENT counts is a red flag — diff `git log --oneline -5 -- <concept>` to explain before proceeding.
2. **Brand grep suites:** run all four per-concept chains verbatim from the interfaces block. For concept-c additionally audit `grep -n 'gradient(' concept-c/assets/css/concept-c.css` and confirm every hit is the licensed duplicated-stop stepped utility.
3. **Token drift:** the cross-concept token-drift grep from the interfaces block.
4. **Camera gate (QA-03/CONC regression):** `node qa/camera-framing-check.mjs` — exit 0.
5. **Cross-concept link integrity (QA-03):** write `/private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad/cross-concept-linkcheck.py` (Python, stdlib only) that: scans root `index.html` plus every `*.html` under concept-a/, concept-b/, concept-c/, concept-d/; extracts all href/src values; skips external schemes (http/https/mailto/tel/data:); resolves relative paths against each file's own directory and `/...` absolute paths against the repo root; treats directory links (`concept-a/`) as `<dir>/index.html`; asserts every local target exists on disk; for fragment links (`page.html#x` or bare `#x`) asserts the target file contains `id="x"`; applies EXACTLY the documented allowlist from the interfaces block; prints counts (files scanned, links checked, allowlisted, failures) and exits 0 only on zero non-allowlisted failures. Run it.
6. **Video LCP/budget re-checks (QA-03):** both concept-b and concept-d check sets from the interfaces block.
7. **Sub-page inventory (QA-03 evidence):** record in the SUMMARY the derived sub-page count per concept (A: 5 — interceptos/insights/3 work cases; B: 3 — problems/interceptos/work; C: 3 — interceptos/work/insights; D: 4 section pages + 3 mirrored pages) and cite the existing click-through/back-button evidence (02-03/03-03/04-03 functional checks, 05-03's 34-assertion nav suite) — do NOT rebuild those suites.

If ANY gate fails: stop, record the exact failure output in the SUMMARY, and report the failure per the deviation rules — concept internals are out of scope to edit here.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 qa/copy-diff.py --all concept-a && python3 qa/copy-diff.py --all concept-b && python3 qa/copy-diff.py --all concept-c && python3 qa/copy-diff.py --mode substring concept-d/index.html concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html && python3 qa/concept-d-script-diff.py && node qa/camera-framing-check.mjs && python3 /private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad/cross-concept-linkcheck.py && ! grep -riE 'fresh thinking starts here' concept-a && ! grep -riE 'a855f7|6366f1|22d3ee' concept-a && ! grep -ri '<hr' concept-a && ! grep -rE '(^|[^-])color:[^;}]*#' concept-a/assets/css && ! grep -rE '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)' concept-a/assets/css && ! grep -rE 'radial-gradient|conic-gradient' concept-a/assets/css && ! grep -r 'view-transition-name' concept-a && ! grep -rIiE 'fresh thinking starts here' concept-b && ! grep -rIiE 'a855f7|6366f1|22d3ee' concept-b && ! grep -rIi '<hr' concept-b && ! grep -rIE 'border-(top|bottom)[[:space:]]*:' concept-b && ! grep -rIE '(^|[^-])color:[^;}]*#' concept-b && ! grep -rIE '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' concept-b && ! grep -rIE 'gradient\(' concept-b && ! grep -rI 'view-transition-name' concept-b && ! grep -rIiE --exclude-dir=vendor 'fresh thinking starts here' concept-c && ! grep -rIiE --exclude-dir=vendor 'a855f7|6366f1|22d3ee' concept-c && ! grep -rIi --exclude-dir=vendor '<hr' concept-c && ! grep -rIE --exclude-dir=vendor 'border-(top|bottom)[[:space:]]*:' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*#' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])linear-gradient\(|radial-gradient|conic-gradient' concept-c && ! grep -rI --exclude-dir=vendor 'view-transition-name' concept-c && ! grep -rIiE 'fresh thinking starts here' concept-d && ! grep -rIiE 'a855f7|6366f1|22d3ee' concept-d && ! grep -rI 'view-transition-name' concept-d && ! grep -rI 'data-copy' concept-d && ! grep -Ii '<hr' concept-d/index.html concept-d/pages/os.html concept-d/pages/labs.html concept-d/pages/work.html concept-d/pages/contact.html && ! grep -IE '(^|[^-])color:[^;}]*#' concept-d/assets/css/concept-d.css && ! grep -IE 'linear-gradient|radial-gradient|conic-gradient' concept-d/assets/css/concept-d.css && ! grep -IE 'border-(top|bottom)[[:space:]]*:[[:space:]]*[^0n]' concept-d/assets/css/concept-d.css && ! grep -rIE '^[[:space:]]*--(flarepop|coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire)(-[0-9]+)?[[:space:]]*:' concept-a/assets/css concept-b/assets/css concept-c/assets/css concept-d/assets/css/concept-d.css && [ $(( $(stat -f%z concept-b/assets/video/hero-loop-1080.webm) + $(stat -f%z concept-b/assets/video/hero-poster.jpg) )) -le 7340032 ] && grep -q 'preload="metadata"' concept-b/index.html && grep -q 'fetchpriority="high"' concept-b/index.html && [ "$(stat -f%z concept-d/assets/video/hero-light-loop.webm)" -le 4000000 ] && [ "$(stat -f%z concept-d/assets/video/hero-light-poster.jpg)" -le 200000 ] && grep -q 'autoplay muted playsinline loop' concept-d/index.html && python3 -c "h=open('concept-d/index.html').read(); assert h.index('hero-light-loop.webm') < h.index('hero-light-loop.mp4'); print('webm-first ok')"</automated>
  </verify>
  <done>Every gate exits 0 with counts matching the expected table (430/340/282 annotated chunks, 71 substring chunks over 5 pages, 13 script-diff checks, camera gate green, zero non-allowlisted link failures with counts printed, all four grep suites + token-drift clean, both video budgets inside limits); all results recorded verbatim for the SUMMARY's QA table.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild root index.html as the final Fritz review gallery</name>
  <files>index.html, assets/gallery/concept-a.png, assets/gallery/concept-b.png, assets/gallery/concept-c.png, assets/gallery/concept-d.png, .planning/phases/06-cross-concept-qa-review-packaging/captures/gallery-390.png, .planning/phases/06-cross-concept-qa-review-packaging/captures/gallery-1440.png</files>
  <action>
1. **Thumbnails:** create `assets/gallery/` and copy the four source captures from the interfaces block to `assets/gallery/concept-{a,b,c,d}.png`. Source captures are fullPage (very tall) — display them cropped via CSS (`aspect-ratio: 16 / 10; width: 100%; object-fit: cover; object-position: top center;`). If the top-crop of any thumbnail reads badly in the reviewed gallery capture (e.g. shows nothing recognizable), EITHER pre-crop the PNG top region before copying (`sips --cropOffset 0 0 -c 900 1440 in.png --out out.png` or Python/PIL in the scratchpad) OR pick a different existing capture from that concept's phase captures/ dir — capture choice is discretion; document the choice in the SUMMARY.

2. **Rebuild `index.html`** keeping the Phase 1 gallery's dark Fritz foundation (same `/shared/tokens.css` + `/shared/fonts.css` + `/shared/motion.css` links, `var(--page)` background, `var(--ease-inout-sine)` for the one card hover transition — sine easing, long-ish duration, no jarring landings):
   - **Header:** the canonical 8-path lockup INLINED as SVG markup copied from `shared/logo/lockup.svg` (per the lockup landmine in interfaces — the current `<img src>` reference renders the wordmark invisible; never edit the shared file). Static, centered-left, ~180px wide, no animation.
   - **Intro:** h1 "Homepage concepts." and a short factual sub line, e.g. "Four directions, one content source. Side-by-side review." (meta-UI wayfinding, not site copy).
   - **FOUR cards** in order A, B, C, D — grid 2×2 at ≥900px, 1 column below. Each card: thumbnail `<img>` (with alt text naming the concept), mono kicker ("Concept A" … "Concept D"), concept name h2, one-line factual description, a 2-3 item "what to try" list (plain `<ul>`, no numbering — order isn't load-bearing), and the whole card is the link. Suggested content (factual defaults — tighten wording at discretion, never marketing-ify):
     - A — "Editorial": "An editorial card grid with one hero statement and click-through sub-pages." Try: click a work card to open its case page; scroll — sections reveal sparingly; open InterceptOS from the card grid. → `href="concept-a/"`
     - B — "Full-Screen Video": "A full-bleed ambient video hero with labeled hotspots and chapter panels." Try: click a hotspot label to open its chapter panel; continue from a panel to its full page; use the hero pause control. → `href="concept-b/"`
     - C — "Experimental WebGL/3D": "A three.js topic field — scroll moves the camera through six topics." Try: scroll through the field; Tab to a topic label and press Enter; the topic index works without WebGL. → `href="concept-c/"`
     - D — "Home Variant": "A speculative variant of the deployed homepage — light video field with expanding module cards." Try: click a card to expand its full module; Esc closes a modal; the nav routes to full section pages. → `href="concept-d/"`
   - **Status line per card** (the sole colored text on the page, `var(--flarepop-100)`, mono, exactly like the Phase 1 gallery's `.status` idiom): "Ready for review" on A/B/C and "Start here" on D (Concept D is the Jon-directed variant to look at first).
   - **Footer note** (plain, quiet): serve instructions — `./serve.sh` then `http://localhost:4340/`.
   - **Absolute bans:** no `<hr>`, no border-top/bottom hairline dividers (use space/weight/alignment for separation), no colored text other than the Flarepop status, no gradients, no banned tagline, no numbered lists.

3. **Serve + curl checks:** start `./serve.sh` (:4340) if not already up (kill after only if this task started it). `curl -sf` must return 200-class for `/`, all four `/assets/gallery/concept-*.png`, and the fresh sub-page direct-load spot checks: `/concept-a/pages/interceptos.html`, `/concept-b/pages/interceptos.html`, `/concept-c/pages/interceptos.html`, `/concept-d/pages/os.html`.

4. **Captures:** with the proven Puppeteer scratchpad rig (real Chrome executablePath, `page.setViewport`, load via http://localhost:4340/ — the absolute `/shared/` paths break under file://), capture the gallery fullPage at 390 and 1440 into `.planning/phases/06-cross-concept-qa-review-packaging/captures/gallery-{390,1440}.png`.

5. **Honest review (mandatory):** READ both captures with the Read tool and judge: lockup wordmark visibly white (not missing/black); all four thumbnails show a recognizable, representative crop; text legible; no horizontal overflow at 390; cards read as one obvious click target each; the page reads as a calm dark Fritz surface, not a dashboard. Fix any issue found, re-run the greps in the verify block, re-capture.

6. **Re-run the cross-concept link-integrity script** (now covering the NEW root index.html + its assets/gallery references).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && test -s index.html && [ "$(ls assets/gallery/*.png 2>/dev/null | wc -l | tr -d ' ')" -eq 4 ] && ! find assets/gallery -name '*.png' -size -8k | grep -q . && grep -q 'href="concept-a/"' index.html && grep -q 'href="concept-b/"' index.html && grep -q 'href="concept-c/"' index.html && grep -q 'href="concept-d/"' index.html && grep -q 'assets/gallery/concept-' index.html && grep -q 'id="mark"' index.html && ! grep -q 'logo/lockup.svg' index.html && ! grep -Ii '<hr' index.html && ! grep -IiE 'fresh thinking starts here' index.html && ! grep -IiE 'a855f7|6366f1|22d3ee' index.html && ! grep -IE 'border-(top|bottom)[[:space:]]*:' index.html && ! grep -IE '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)' index.html && ! grep -IE 'radial-gradient|conic-gradient|linear-gradient' index.html && ! grep -IE '<ol' index.html && python3 /private/tmp/claude-501/-Users-jontoewsinterceptgroup-com/f14fc7b0-c28f-475d-a98e-6a9ad1bf4ab4/scratchpad/cross-concept-linkcheck.py && (curl -sf -o /dev/null http://localhost:4340/ || { ./serve.sh >/dev/null 2>&1 & sleep 1; }) && curl -sf -o /dev/null http://localhost:4340/ && curl -sf -o /dev/null http://localhost:4340/assets/gallery/concept-a.png && curl -sf -o /dev/null http://localhost:4340/assets/gallery/concept-d.png && curl -sf -o /dev/null http://localhost:4340/concept-a/pages/interceptos.html && curl -sf -o /dev/null http://localhost:4340/concept-b/pages/interceptos.html && curl -sf -o /dev/null http://localhost:4340/concept-c/pages/interceptos.html && curl -sf -o /dev/null http://localhost:4340/concept-d/pages/os.html && [ "$(ls .planning/phases/06-cross-concept-qa-review-packaging/captures/gallery-*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 2 ] && ! find .planning/phases/06-cross-concept-qa-review-packaging/captures -name '*.png' -size -8k | grep -q .</automated>
  </verify>
  <done>The root gallery shows four cards (A/B/C/D) each with a real thumbnail, factual description, what-to-try hints, Flarepop-only status, and a working link; the inlined lockup renders visibly (confirmed by reading the 1440 capture); gallery brand greps all clean; curl direct-loads pass for the gallery, all four thumbnails, and one sub-page per concept; link integrity re-passes including the new root; both gallery captures reviewed honestly with any issues fixed.</done>
</task>

</tasks>

<verification>
- All copy gates green with expected counts: A 430 (6 pages), B 340 (4), C 282 (4), D 71 substring (5 pages) + 13 script-diff checks
- camera-framing-check exits 0; cross-concept link integrity zero non-allowlisted failures (counts recorded)
- All four per-concept brand grep suites + the token-drift grep clean; concept-c gradient audit shows only the licensed stepped utility
- Video budgets hold: B webm+poster ≤ 7,340,032 B with preload/fetchpriority intact; D webm ≤ 4 MB, poster ≤ 200 KB, webm-first
- http://localhost:4340/ serves the rebuilt four-card gallery; gallery passes its own Fritz greps; both gallery captures read and judged
</verification>

<success_criteria>
- QA-02: verbatim copy mechanically proven across all four concepts in one consolidated run, results recorded verbatim in the SUMMARY
- QA-03: responsive/sub-page/LCP evidence consolidated (existing phase evidence cited + fresh direct-load spot checks + budget re-checks + link integrity across the whole repo)
- QA-04: the final review gallery exists with four cards, real thumbnails, and working links — the surface the orchestrator's Fritz agent gate (QA-01) reviews between waves
</success_criteria>

<output>
After completion, create `.planning/phases/06-cross-concept-qa-review-packaging/06-01-SUMMARY.md` — include the full QA results table (every gate, every count, verbatim), the link-integrity counts + allowlist as run, the thumbnail source choices, and the gallery capture review verdicts. Note explicitly for the orchestrator: the gallery + all four concepts are now ready for the Fritz brand agent gate (06-FRITZ-QA.md) that must run BEFORE 06-02.
</output>
