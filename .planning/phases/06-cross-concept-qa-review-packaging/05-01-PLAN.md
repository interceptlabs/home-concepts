---
phase: 05-cross-concept-qa-review-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qa/link-integrity.mjs
  - index.html
  - assets/gallery/concept-a.png
  - assets/gallery/concept-b.png
  - assets/gallery/concept-c.png
  - .planning/phases/05-cross-concept-qa-review-packaging/captures/gallery-390.png
  - .planning/phases/05-cross-concept-qa-review-packaging/captures/gallery-1440.png
autonomous: true
requirements: [QA-01, QA-02, QA-03, QA-04]

must_haves:
  truths:
    - "copy-diff exits 0 across all 14 concept pages: 430 chunks (concept-a, 6 pages), 340 (concept-b, 4 pages), 282 (concept-c, 4 pages), 0 failures"
    - "The 8-grep brand suite is clean for all three concept dirs (vendor excluded), with the border grep's only tolerated hits being value `none` (border removal, not a rule line)"
    - "No brand-hue hex from shared/tokens.css appears anywhere in concept CSS or HTML except inside the inlined canonical lockup SVG (aria-label=\"Intercept\")"
    - "node qa/camera-framing-check.mjs exits 0 (concept-c dolly invariants hold)"
    - "Link integrity passes across all 14 concept pages plus the root gallery: every local href/src target exists, every #anchor resolves, external URLs are well-formed"
    - "The root gallery shows three concept cards side by side with a real 1440 screenshot thumbnail each, factual one-line descriptions, what-to-try hints, and links to /concept-a/, /concept-b/, /concept-c/"
    - "The gallery renders with no horizontal overflow at 390 and 1440, and passes the same brand greps it enforces on the concepts"
  artifacts:
    - path: "qa/link-integrity.mjs"
      provides: "Permanent cross-concept link/anchor integrity gate (stdlib node, exit 0/1)"
    - path: "index.html"
      provides: "Final Fritz review gallery: 3 cards, thumbnails, what-to-try hints, serve note, inline canonical lockup"
      contains: "aria-label=\"Intercept\""
    - path: "assets/gallery/concept-a.png"
      provides: "Real 1440 homepage capture thumbnail for Concept A"
    - path: "assets/gallery/concept-b.png"
      provides: "Real 1440 homepage capture thumbnail for Concept B"
    - path: "assets/gallery/concept-c.png"
      provides: "Real 1440 homepage capture thumbnail for Concept C (JS-on 3D scene framing)"
  key_links:
    - from: "index.html"
      to: "concept-a/, concept-b/, concept-c/"
      via: "card hrefs"
      pattern: "href=\"concept-[abc]/\""
    - from: "index.html"
      to: "assets/gallery/*.png"
      via: "card thumbnail img src"
      pattern: "assets/gallery/concept-[abc]"
    - from: "index.html"
      to: "shared/tokens.css"
      via: "stylesheet link (all color via var() tokens)"
      pattern: "shared/tokens.css"
---

<objective>
Run the full mechanical cross-concept QA suite over all 14 pages (copy gate, brand greps, token drift, link integrity, camera-framing gate), then replace the Phase 1 placeholder gallery with the final Fritz review gallery Jon will actually use.

Purpose: This is the mechanical half of QA-01 plus all of QA-02/QA-03's evidence, and the QA-04 review surface. Everything here must be green BEFORE the orchestrator runs the Fritz brand-agent review (which happens between this plan and 05-02) — the Fritz agent reviews the finished gallery too.
Output: Recorded pass results for every mechanical gate, a permanent `qa/link-integrity.mjs` gate, the rebuilt root `index.html` gallery, three thumbnails in `assets/gallery/`, and reviewed gallery captures.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-concept-qa-review-packaging/05-CONTEXT.md
@index.html
@serve.sh
@qa/README.md

<interfaces>
<!-- Verified during planning (2026-07-24) by dry-running every suite. Use these directly — no rediscovery needed. -->

**Page inventory (14 concept pages + root gallery):**
- concept-a: index.html + pages/{interceptos,insights,work-hp-abx,work-intel-abm,work-sap-video}.html — 6 pages, 430 chunks
- concept-b: index.html + pages/{interceptos,problems,work}.html — 4 pages, 340 chunks
- concept-c: index.html + pages/{interceptos,work,insights}.html — 4 pages, 282 chunks (vendor/ dir holds three.js — always --exclude-dir=vendor)

**Copy gate (all three currently PASS at exactly these totals):**
`python3 qa/copy-diff.py --all concept-a` → 6 pages, 430 chunks, 0 failures
`python3 qa/copy-diff.py --all concept-b` → 4 pages, 340 chunks, 0 failures
`python3 qa/copy-diff.py --all concept-c` → 4 pages, 282 chunks, 0 failures

**The 8-grep brand suite (run per concept dir; all clean today except noted):**
1. `grep -rIiE --exclude-dir=vendor 'fresh thinking starts here' $C` — banned tagline
2. `grep -rIiE --exclude-dir=vendor 'a855f7|6366f1|22d3ee' $C` — deprecated 12-path mark hexes
3. `grep -rIi --exclude-dir=vendor '<hr' $C` — no rule lines
4. `grep -rIhE --exclude-dir=vendor 'border-(top|bottom)[[:space:]]*:' $C | grep -vE ':[[:space:]]*none'` — no hairline rules. KNOWN BENIGN: concept-a.css has exactly 3 hits of `border-top: none` on `:first-child` overrides (they REMOVE borders — the none-filter keeps the gate honest without touching concept internals)
5. `grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*#' $C` — no raw-hex colored text
6. `grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' $C` — Flarepop-only colored text
7. `grep -rIE --exclude-dir=vendor '(^|[^-])linear-gradient\(|radial-gradient|conic-gradient' $C` — hard-step fields only (the `[^-]` guard licenses `repeating-linear-gradient` stepped utilities)
8. `grep -rI --exclude-dir=vendor 'view-transition-name' $C` — plain cross-fade locked (concepts use `@view-transition { navigation: auto }`, never named transitions)

**Token-drift check (cross-concept, verified clean today):**
- Concept CSS carries ZERO raw hex of any kind: `grep -rIiE '#[0-9a-f]{3}' concept-a/assets/css concept-b/assets/css concept-c/assets/css` returns nothing (all color is var() tokens)
- Brand-hue hexes in HTML: build the hue list from the SSoT at runtime — `HEXES=$(grep -oiE '#[0-9a-f]{6}' shared/tokens.css | tr -d '#' | sort -u | paste -sd'|' -)` — then any `#(${HEXES})` hit in concept HTML must be on a line containing `aria-label="Intercept"` (the inlined canonical lockup SVG in concept-b's 4 pages and concept-c's 4 pages carries its own licensed mark fills, incl. #FF00E5). Filter: `| grep -v 'aria-label="Intercept"'` → must be empty.

**Capture rig (02-03/03-03/04-03 proven):** puppeteer installed in the session scratchpad dir ONLY (never repo — repo has no package.json), `executablePath` pointing at the real installed `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` (the bundled Chrome-for-Testing binary hangs on captureScreenshot in this sandbox), `page.setViewport()` for widths (the bare `--window-size` CLI flag clamps below ~500px). For concept-c JS-on scene shots: `waitForFunction` on `document.documentElement.dataset.sceneReady`.

**Known deferred item (do NOT fix, just carry forward):** `GET /favicon.ico` → 404, site-wide, pre-existing (04 deferred-items.md).

**Lockup rule:** `fill="currentColor"` cannot inherit page color through `<img src>` (03-03 finding). The rebuilt gallery must INLINE the canonical lockup SVG (copy the exact single-line `<svg … aria-label="Intercept">…</svg>` block from concept-b/index.html) instead of the placeholder's `<img src="/shared/logo/lockup.svg">`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full mechanical QA suite across all 14 pages + permanent link-integrity gate</name>
  <files>qa/link-integrity.mjs</files>
  <action>
Run every mechanical gate across all three concepts and record per-concept results for the SUMMARY's QA table (the Fritz agent and 05-02's REVIEW.md both consume that table).

**1. Copy gate (QA-02):** `python3 qa/copy-diff.py --all concept-a`, then `--all concept-b`, then `--all concept-c`. Each must exit 0. Record exact page/chunk totals — expected 6/430, 4/340, 4/282. Any other total means pages changed since planning: investigate before proceeding.

**2. 8-grep brand suite per concept (QA-01, mechanical layer):** run the 8 greps from the interfaces block against each of concept-a, concept-b, concept-c (always `--exclude-dir=vendor`; it only matters for concept-c but is harmless elsewhere). All must return nothing, with one audited exception: grep 4 on concept-a returns exactly 3 `border-top: none` lines — confirm each hit's value is literally `none` (border removal). Any hit with a visible border value is a violation. Record the per-concept result of each grep.

**3. Cross-concept token-drift check (QA-01):** two parts per the interfaces block — (a) zero raw hex in any concept CSS file; (b) brand-hue hexes (runtime-derived from shared/tokens.css) in concept HTML appear only on lines containing `aria-label="Intercept"` (the inlined canonical lockup). Record hit counts and confirm the lockup-only pattern.

**4. Camera-framing gate:** `node qa/camera-framing-check.mjs` — must exit 0.

**5. Link integrity (QA-03) — write the permanent gate `qa/link-integrity.mjs`:** stdlib node only (fs, path, url — no deps, matching qa/'s zero-dependency rule). Behavior:
- CLI: `node qa/link-integrity.mjs [root ...]` — roots default to `concept-a concept-b concept-c index.html`. A root that is a directory is walked recursively for `*.html` (skip any `vendor/` dir); a root that is a file is checked directly.
- For each page, extract every `href="…"` and `src="…"` value (regex extraction is fine; also catch `<source src>` and `poster="…"`).
- Local paths (site-absolute `/…` resolved against repo root; relative resolved against the page's dir): assert the target file exists on disk. For paths ending in `/`, assert `<dir>/index.html` exists.
- Fragment links: `#id` must match an `id="…"` in the same page; `page.html#id` must match an id in the target page (this covers concept-c's `#t-*` waypoint/topic-index anchors).
- External `http(s)://` URLs: well-formedness only via `new URL()` — never fetched.
- Skip `mailto:`, `tel:`, `data:`, and `javascript:void` values.
- Print one PASS/FAIL line per check plus a final `N pages, M refs checked, K failures` summary; exit 0 iff K=0.
Sanity-prove the script the same way qa/copy-diff.py was fixture-proven: temporarily point it at a scratchpad HTML file containing one broken local href, one broken anchor, and one good link — confirm it reports exactly the two failures and exits 1 — then run it for real against the default roots (the Phase 1 placeholder index.html is still in place during this task; its links are expected to pass).

**6. LCP/serve sanity (QA-03, lightweight):** start `./serve.sh` (or `python3 -m http.server 4340`) in the background; `curl -s -o /dev/null -w '%{http_code}'` each of `/`, `/concept-a/`, `/concept-b/`, `/concept-c/` → all 200. Grep-confirm concept-b's hero `<video` still declares `poster=` and `autoplay muted playsinline loop` (poster-frame LCP protection intact). Kill the server if this task started it.

Fix-scope rule: every suite above was dry-run during planning and passes today. A failure here means a regression introduced during this phase — investigate the cause; do NOT silently patch concept internals (concept fixes belong to the Fritz-findings loop, per CONTEXT).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && python3 qa/copy-diff.py --all concept-a && python3 qa/copy-diff.py --all concept-b && python3 qa/copy-diff.py --all concept-c && node qa/camera-framing-check.mjs && node qa/link-integrity.mjs && ! grep -rIiE --exclude-dir=vendor 'fresh thinking starts here' concept-a concept-b concept-c && ! grep -rIiE --exclude-dir=vendor 'a855f7|6366f1|22d3ee' concept-a concept-b concept-c && ! grep -rIi --exclude-dir=vendor '<hr' concept-a concept-b concept-c && ! (grep -rIhE --exclude-dir=vendor 'border-(top|bottom)[[:space:]]*:' concept-a concept-b concept-c | grep -vE ':[[:space:]]*none' | grep -q .) && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*#' concept-a concept-b concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' concept-a concept-b concept-c && ! grep -rIE --exclude-dir=vendor '(^|[^-])linear-gradient\(|radial-gradient|conic-gradient' concept-a concept-b concept-c && ! grep -rI --exclude-dir=vendor 'view-transition-name' concept-a concept-b concept-c && ! grep -rIiE '#[0-9a-f]{3}' concept-a/assets/css concept-b/assets/css concept-c/assets/css && HEXES=$(grep -oiE '#[0-9a-f]{6}' shared/tokens.css | tr -d '#' | sort -u | paste -sd'|' -) && ! (grep -rIiE --include='*.html' --exclude-dir=vendor "#(${HEXES})" concept-a concept-b concept-c | grep -v 'aria-label="Intercept"' | grep -q .)</automated>
  </verify>
  <done>All three copy-diff runs exit 0 at exactly 430/340/282 chunks; all 8 brand greps clean per concept (grep 4's only hits are `border-top: none` removals); zero raw hex in concept CSS; brand-hue hexes in HTML confined to the inlined canonical lockup; camera-framing gate exits 0; `qa/link-integrity.mjs` exists, was fixture-proven to catch a broken href and a broken anchor, and exits 0 against all 14 pages + root; all 4 homepages served 200 with concept-b's poster path intact; every result recorded for the SUMMARY QA table.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild root index.html as the final Fritz review gallery + verified thumbnails</name>
  <files>index.html, assets/gallery/concept-a.png, assets/gallery/concept-b.png, assets/gallery/concept-c.png, .planning/phases/05-cross-concept-qa-review-packaging/captures/gallery-390.png, .planning/phases/05-cross-concept-qa-review-packaging/captures/gallery-1440.png</files>
  <action>
**Thumbnails first — create `assets/gallery/` with one real 1440 capture per concept:**
- Source captures (locked decision: real captures, best 1440 per concept — exact pick is discretion):
  - Concept A: `.planning/phases/02-concept-a-editorial-accenture-but-better/captures/index-1440.png` (fullPage — crop to the top ~900px so it shows the above-the-fold hero)
  - Concept B: `.planning/phases/03-concept-b-full-screen-video/captures/index-1440.png` (fullPage — crop to top ~900px)
  - Concept C: `.planning/phases/04-concept-c-experimental-webgl-3d/captures/index-scene-1440.png` (already a 1440x~900 JS-on scene shot at top framing — the most representative view of the 3D concept; use as-is)
- Crop with `sips` (macOS builtin; `--cropOffset 0 0 -c 900 1440` crops from the top-left) or, if sips cropping proves unreliable, take fresh 1440x900 viewport captures of each homepage with the scratchpad puppeteer rig (JS-on; `dataset.sceneReady` wait for concept-c) — either path satisfies the "real screenshot" decision. Optionally `sips --resampleWidth 720` afterward to keep the gallery light. All three thumbnails must show a consistent above-the-fold view.
- Save as `assets/gallery/concept-a.png`, `concept-b.png`, `concept-c.png`.

**Rebuild `index.html` (full replacement of the Phase 1 placeholder):**
- Keep the existing head pattern: tokens.css + fonts.css + motion.css links, page-scoped `<style>` using ONLY var() tokens (no raw hex anywhere in the file outside the lockup SVG).
- Replace the `<img src="/shared/logo/lockup.svg">` header with the INLINED canonical lockup SVG — copy the exact single-line `<svg class="lockup-svg" … aria-label="Intercept">…</svg>` block from `concept-b/index.html` (the img/currentColor route renders the wordmark invisible on the dark page — known 03-03 bug). Static, centered-left in the header, no animation.
- Dark Fritz page (var(--page) / var(--fg)), intro headline + one short factual line (this is meta-UI wayfinding chrome, NOT site copy — keep it minimal and factual, never paraphrase canonical homepage copy; run no canonical sentences at all so the substring copy-diff stays quiet).
- Three concept cards side by side at 1440 (grid 3-col collapsing to 1-col ≤900px, same breakpoint idiom as the placeholder). Each card, top to bottom:
  - Thumbnail `<img src="assets/gallery/concept-X.png" alt="…" width="…" height="…">` — explicit dimensions or a CSS `aspect-ratio` + `object-fit: cover; object-position: top` so there is zero CLS
  - Mono kicker (Concept A / B / C), concept name (Editorial / Full-Screen Video / Experimental WebGL/3D), one-line factual description
  - "What to try" hint list — 3 bullets max, plain `<ul>` (no numbering — order isn't load-bearing). Suggested: A: click a work card → its case page; watch the hero stagger on load; open InterceptOS. B: hover/focus a hotspot label; click one — chapter panel first, then the full page; try the pause control. C: scroll through the topic field; click a floating topic label; Tab through the labels with the keyboard. (Exact wording is discretion — factual, imperative, short.)
  - The whole card is the link to `concept-a/` / `concept-b/` / `concept-c/` (same anchor-card pattern as the placeholder)
- Remove the stale "In build — Phase N" status lines. Colored text stays Flarepop-only and minimal — at most one accent (e.g. the kicker or a "What to try" label); zero colored text is also acceptable.
- Footer note with serve instructions: `./serve.sh` → http://localhost:4340/ (plain text, mono font is fine).
- Fritz rules apply to this page itself (it is in the Fritz agent's review scope): NO rule lines/borders-as-dividers, no `<hr>`, hard-step fields only if any background texture is used (none required), sine ease-in-out on the card hover transition (reuse the placeholder's `var(--ease-inout-sine)` idiom), no banned tagline, mark never used as decoration (the one inline lockup in the header only).

**Verify the finished gallery:**
- Serve on :4340; `curl` `/`, `/assets/gallery/concept-a.png` (and b/c) → all 200.
- Scratchpad puppeteer rig (real Chrome executablePath, setViewport): capture the gallery at 390x844 and 1440x900 into `.planning/phases/05-cross-concept-qa-review-packaging/captures/gallery-390.png` and `gallery-1440.png`. In the same run, assert `document.documentElement.scrollWidth <= window.innerWidth` at BOTH widths (no horizontal overflow) — fail the run otherwise.
- READ both captures (visual review): three cards render side by side at 1440 and stacked at 390, thumbnails show real recognizable homepage tops, lockup wordmark is visible (not black-on-dark), hint lists legible. Fix anything off and re-capture.
- Run the gallery's own brand greps + `python3 qa/copy-diff.py index.html` (substring mode — expect "no canonical copy detected" style PASS) + `node qa/link-integrity.mjs` (now picks up the new gallery links + thumbnails).
- Kill the server if this task started it.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && test -f assets/gallery/concept-a.png && test -f assets/gallery/concept-b.png && test -f assets/gallery/concept-c.png && grep -q 'href="concept-a/"' index.html && grep -q 'href="concept-b/"' index.html && grep -q 'href="concept-c/"' index.html && grep -q 'assets/gallery/concept-a' index.html && grep -q 'aria-label="Intercept"' index.html && grep -q 'serve.sh' index.html && grep -qi 'what to try' index.html && ! grep -q 'In build' index.html && python3 qa/copy-diff.py index.html && node qa/link-integrity.mjs && ! grep -iE 'fresh thinking starts here' index.html && ! grep -iE 'a855f7|6366f1|22d3ee' index.html && ! grep -i '<hr' index.html && ! (grep -hE 'border-(top|bottom)[[:space:]]*:' index.html | grep -vE ':[[:space:]]*none' | grep -q .) && ! grep -E '(^|[^-])color:[^;}]*#' index.html && ! grep -E '(^|[^-])color:[^;}]*var\(--(coolsweep|wiretree|bitvoid|bitbloom|laserwave|siren|volts|nightwire|success|info|warning|danger)\)' index.html && ! grep -E '(^|[^-])linear-gradient\(|radial-gradient|conic-gradient' index.html && HEXES=$(grep -oiE '#[0-9a-f]{6}' shared/tokens.css | tr -d '#' | sort -u | paste -sd'|' -) && ! (grep -iE "#(${HEXES})" index.html | grep -v 'aria-label="Intercept"' | grep -q .) && [ "$(ls .planning/phases/05-cross-concept-qa-review-packaging/captures/gallery-*.png 2>/dev/null | wc -l | tr -d ' ')" -ge 2 ] && ! find .planning/phases/05-cross-concept-qa-review-packaging/captures -name '*.png' -size -8k | grep -q .</automated>
  </verify>
  <done>Root index.html is the final review gallery: inlined canonical lockup, three concept cards with real capture thumbnails, factual descriptions, ≤3 what-to-try hints each, whole-card links, serve-note footer; all gallery brand greps clean; copy-diff and link-integrity pass with the gallery included; both gallery captures exist, are non-trivial, were visually reviewed, and the rig asserted no horizontal overflow at 390 and 1440.</done>
</task>

</tasks>

<verification>
- `python3 qa/copy-diff.py --all concept-a && --all concept-b && --all concept-c` — 0 failures at 430/340/282 chunks (QA-02)
- 8-grep brand suite clean across all three concept dirs + the gallery (QA-01 mechanical layer)
- Token drift: zero raw hex in concept CSS; brand-hue hexes only inside the canonical inline lockup (QA-01)
- `node qa/camera-framing-check.mjs` exits 0
- `node qa/link-integrity.mjs` exits 0 across 14 concept pages + gallery (QA-03)
- Gallery: 3 cards, real thumbnails, working links, no overflow at 390/1440, reviewed captures on disk (QA-04)
</verification>

<success_criteria>
Every mechanical gate green with results recorded in the SUMMARY QA table, and the final gallery live at / — the repo is ready for the orchestrator to run the Fritz brand-agent review (which covers the three concepts AND this gallery) before 05-02 packages the review.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-concept-qa-review-packaging/05-01-SUMMARY.md` including the full QA results table (per-concept copy-diff totals, per-grep results, token-drift result, camera gate, link-integrity totals, capture list). The Fritz agent and 05-02's REVIEW.md both read this table.
</output>
