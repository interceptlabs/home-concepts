---
phase: 05-cross-concept-qa-review-packaging
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - REVIEW.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [QA-01, QA-04]

must_haves:
  truths:
    - "Jon can open REVIEW.md at the repo root and know exactly how to run the review (./serve.sh → http://localhost:4340/), what each concept is, and what to try in each"
    - "REVIEW.md points at every piece of QA evidence: the 05-01 QA table, the phase captures dirs, the qa/ gates, and 05-FRITZ-QA.md"
    - "The Fritz brand-agent findings outcome (resolved and/or accepted-as-noted-for-Jon) is reflected verbatim-in-spirit in REVIEW.md's known-notes section and in the plan SUMMARY"
    - "REQUIREMENTS.md shows QA-02 and QA-03 complete; QA-01 and QA-04 remain explicitly pending orchestrator sign-off after Fritz findings resolution — the executor never marks them"
    - "STATE.md and ROADMAP.md reflect Phase 5 execution state accurately (both plans done, phase completion itself left to the orchestrator)"
  artifacts:
    - path: "REVIEW.md"
      provides: "Jon's single entry point: run instructions, per-concept guide, what-to-try lists, QA evidence map, known notes"
      contains: "serve.sh"
    - path: ".planning/REQUIREMENTS.md"
      provides: "QA-02/QA-03 checked; QA-01/QA-04 pending with Fritz-gate note"
    - path: ".planning/STATE.md"
      provides: "Position/metrics/decisions updated for 05-02"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 plan checkboxes updated"
  key_links:
    - from: "REVIEW.md"
      to: "serve.sh + http://localhost:4340/"
      via: "run instructions"
      pattern: "serve\\.sh"
    - from: "REVIEW.md"
      to: ".planning/phases/05-cross-concept-qa-review-packaging/05-FRITZ-QA.md"
      via: "Fritz findings reference"
      pattern: "05-FRITZ-QA"
    - from: "REVIEW.md"
      to: ".planning/phases/05-cross-concept-qa-review-packaging/05-01-SUMMARY.md"
      via: "QA evidence reference"
      pattern: "05-01-SUMMARY"
---

<objective>
Package the review for Jon: write REVIEW.md as the single human entry point to the three-concept review, then close out the phase's planning documents — honoring the Fritz gate the orchestrator ran between 05-01 and this plan.

Purpose: QA-04's "ready for Jon" state isn't just the gallery — it's Jon knowing how to run it, what he's looking at, and what the QA evidence says. This plan also folds the Fritz brand-agent findings (05-FRITZ-QA.md, committed by the orchestrator before this plan executes) into the record.
Output: REVIEW.md at repo root; REQUIREMENTS/STATE/ROADMAP updated with the correct split of what the executor closes vs. what the orchestrator signs off.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-cross-concept-qa-review-packaging/05-CONTEXT.md
@.planning/phases/05-cross-concept-qa-review-packaging/05-01-SUMMARY.md
@.planning/phases/05-cross-concept-qa-review-packaging/05-FRITZ-QA.md

<interfaces>
**Sequencing contract (set by the phase orchestrator — do not work around it):**
- The ORCHESTRATOR runs the Fritz brand-agent review between 05-01 and this plan and commits its findings report to `.planning/phases/05-cross-concept-qa-review-packaging/05-FRITZ-QA.md`. Executors cannot spawn agents — if that file does not exist when this plan starts, STOP and report the missing precondition instead of inventing findings or proceeding without them.
- The executor must NOT mark QA-01 or QA-04 complete in REQUIREMENTS.md. The orchestrator marks them after confirming Fritz findings are resolved (or explicitly accepted as noted-for-Jon). QA-02 and QA-03 are purely mechanical and fully evidenced by 05-01 — the executor marks those two.

**Known notes REVIEW.md must carry (from prior phase records):**
- `GET /favicon.ico` → 404 site-wide — pre-existing, deliberately deferred (04 deferred-items.md)
- Concept C low device tier: degradation path (antialias off, DPR 1, 3-step gradient map, no parallax) verified by code review, not on physical low-tier hardware — a human check on a real integrated-GPU/mobile device remains open (04-03-SUMMARY)
- Anything 05-FRITZ-QA.md records as "accepted as noted-for-Jon"

**Doc style rules (global creative rules apply to this doc too):**
- No decorative horizontal rules/dividers in REVIEW.md body (no `---` section separators — use headings and whitespace)
- No numbered lists unless order is load-bearing (run steps may be numbered; concept lists and hint lists must not be)
- Never quote canonical homepage copy at length — describe, don't transcribe

**Known gsd-tools quirks (recur every plan — hand-correct after running):** `state advance-plan` may mangle `status`/`current_plan`; `add-decision` silently resets frontmatter `status` to `planning`; `record-metric` appends below the table footer instead of inside the table. Verify STATE.md by eye after any gsd-tools state write.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write REVIEW.md — Jon's entry point to the review</name>
  <files>REVIEW.md</files>
  <action>
First, confirm the precondition: `.planning/phases/05-cross-concept-qa-review-packaging/05-FRITZ-QA.md` exists (orchestrator committed it after the Fritz gate). Read it fully. If it lists unresolved BLOCKING findings, stop and report — this plan only executes after resolution/acceptance.

Write `REVIEW.md` at the repo root, addressed to Jon, covering (headings + whitespace, no divider lines, no gratuitous numbering):

- **How to run** — `./serve.sh` from the repo root, then open http://localhost:4340/ (the gallery). Note the gallery links into each concept and each concept's pages link back. Numbered steps are fine here (order is load-bearing).
- **The three concepts** — one short paragraph each (A: editorial card grid, one hero statement + one primary CTA, 5 sub-pages, cross-document view transitions; B: full-bleed ambient video hero with 6 labeled hotspots, chapter panels then full sub-pages, reduced-motion poster fallback; C: three.js topic field with scroll-driven dolly, DOM mirror + no-WebGL/reduced-motion fallbacks, device tiering, 3 sub-pages). Factual descriptions — never paraphrased site copy.
- **What to try** — per concept, mirror the gallery's hint lists (keep the two consistent; pull the finalized hints from the rebuilt index.html rather than re-inventing).
- **QA evidence** — where everything lives: `qa/copy-diff.py` (verbatim gate; 430/340/282 chunks green — pull exact totals from 05-01-SUMMARY), `qa/link-integrity.mjs`, `qa/camera-framing-check.mjs`, the 8-grep brand suite results in `05-01-SUMMARY.md`, responsive captures under each phase dir's `captures/`, and the Fritz brand review findings in `05-FRITZ-QA.md` with a one-line statement of its outcome (all findings resolved / accepted items listed below).
- **Known notes** — the three items from the interfaces block (favicon 404 deferred; device-tier human check on real low-tier hardware still open; any Fritz accepted-as-noted-for-Jon items, listed individually).

Tone: honest WIP-doc tone — plain statements of what was verified and what wasn't. Keep the whole doc scannable (aim ~60-100 lines).
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && test -f .planning/phases/05-cross-concept-qa-review-packaging/05-FRITZ-QA.md && test -f REVIEW.md && grep -q 'serve.sh' REVIEW.md && grep -q '4340' REVIEW.md && grep -qi 'concept a' REVIEW.md && grep -qi 'concept b' REVIEW.md && grep -qi 'concept c' REVIEW.md && grep -q '05-FRITZ-QA' REVIEW.md && grep -q '05-01-SUMMARY' REVIEW.md && grep -qi 'favicon' REVIEW.md && grep -qi 'device' REVIEW.md && grep -q 'copy-diff' REVIEW.md && ! grep -iE 'fresh thinking starts here' REVIEW.md && ! grep -E '^---$' REVIEW.md</automated>
  </verify>
  <done>REVIEW.md exists at the repo root with run instructions, per-concept descriptions, what-to-try lists consistent with the gallery, a complete QA-evidence map including the Fritz findings outcome, and the known-notes list — no divider lines, no banned tagline, no transcribed site copy.</done>
</task>

<task type="auto">
  <name>Task 2: Phase closeout — REQUIREMENTS/STATE/ROADMAP with the Fritz-gate split honored</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
**REQUIREMENTS.md:**
- Mark `QA-02` and `QA-03` complete (checkbox `[x]` in the v1 list AND their Traceability rows → `Complete`) — both are fully evidenced by 05-01's mechanical results (copy-diff 430/340/282; responsive captures + link integrity + sub-page proof).
- Leave `QA-01` and `QA-04` UNCHECKED and their traceability rows `Pending`. Add a single italic note directly under the Cross-cutting QA section: *QA-01/QA-04 sign-off is the orchestrator's, made after Fritz findings in `.planning/phases/05-cross-concept-qa-review-packaging/05-FRITZ-QA.md` are confirmed resolved or accepted — mechanical evidence lives in 05-01-SUMMARY.* Do NOT mark them yourself even if 05-FRITZ-QA.md reads clean.

**STATE.md:**
- Update position to Phase 5, plan 2 of 2 complete; update `current_plan`, progress counts (re-baseline: 16 total plans, 16 complete), `last_activity`, and Current focus (Phase 5 executed; awaiting orchestrator QA-01/QA-04 sign-off + milestone close).
- Record this plan's metric row and any decisions via gsd-tools if the workflow calls for it, then HAND-VERIFY the frontmatter `status`, `current_plan`, and metric-table placement — the known quirks (see interfaces) have recurred on every plan since 04-01.
- Carry forward one pending todo: "Human check: Concept C low device tier on real integrated-GPU/mobile hardware" plus any Fritz accepted-as-noted items as todos if 05-FRITZ-QA.md flagged them for follow-up.

**ROADMAP.md:**
- Check off both Phase 5 plan lines (`05-01-PLAN.md`, `05-02-PLAN.md`) with completion dates.
- Update the Progress table row for Phase 5 to `2/2` — leave its Status for the orchestrator's phase-completion pass (do not mark the phase `Complete` or check the top-level Phase 5 box; that happens with QA-01/QA-04 sign-off).

Do not modify REVIEW.md, index.html, or any concept file in this task.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && grep -q -- '- \[x\] \*\*QA-02\*\*' .planning/REQUIREMENTS.md && grep -q -- '- \[x\] \*\*QA-03\*\*' .planning/REQUIREMENTS.md && grep -q -- '- \[ \] \*\*QA-01\*\*' .planning/REQUIREMENTS.md && grep -q -- '- \[ \] \*\*QA-04\*\*' .planning/REQUIREMENTS.md && grep -q '| QA-02 | Phase 5 | Complete' .planning/REQUIREMENTS.md && grep -q '| QA-03 | Phase 5 | Complete' .planning/REQUIREMENTS.md && grep -q '| QA-01 | Phase 5 | Pending' .planning/REQUIREMENTS.md && grep -q '| QA-04 | Phase 5 | Pending' .planning/REQUIREMENTS.md && grep -qi '05-FRITZ-QA' .planning/REQUIREMENTS.md && grep -q '\[x\] 05-01-PLAN.md' .planning/ROADMAP.md && grep -q '\[x\] 05-02-PLAN.md' .planning/ROADMAP.md && grep -q '2/2' .planning/ROADMAP.md && grep -q 'Phase: 5 of 5' .planning/STATE.md</automated>
  </verify>
  <done>QA-02/QA-03 marked complete with traceability rows updated; QA-01/QA-04 explicitly left pending with the Fritz sign-off note; STATE.md re-baselined to 16/16 plans with hand-verified frontmatter and metrics; ROADMAP shows both Phase 5 plans done with phase-level completion left to the orchestrator.</done>
</task>

</tasks>

<verification>
- REVIEW.md exists, is complete (run / concepts / what-to-try / evidence / known notes), and references 05-FRITZ-QA.md and 05-01-SUMMARY.md
- 05-FRITZ-QA.md precondition confirmed and its outcome incorporated into REVIEW.md + this plan's SUMMARY
- REQUIREMENTS.md: QA-02/QA-03 complete; QA-01/QA-04 pending orchestrator sign-off (never executor-marked)
- STATE.md and ROADMAP.md accurate for both Phase 5 plans
</verification>

<success_criteria>
Jon has one file to open (REVIEW.md) and one command to run (./serve.sh); every claim in it traces to on-disk evidence; the planning record is closed out exactly as far as the executor's authority extends, leaving QA-01/QA-04 for the orchestrator's post-Fritz sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-concept-qa-review-packaging/05-02-SUMMARY.md`. Include a short "Fritz gate outcome" section summarizing 05-FRITZ-QA.md's findings and their resolution/acceptance status, so the orchestrator's QA-01/QA-04 sign-off has everything in one place.
</output>
