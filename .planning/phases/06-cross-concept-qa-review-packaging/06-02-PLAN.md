---
phase: 06-cross-concept-qa-review-packaging
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - REVIEW.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [QA-01, QA-04]

must_haves:
  truths:
    - "Jon can open REVIEW.md and know how to run the review, what each of the four concepts is (with Concept D flagged as the Jon-directed variant to look at first), what to try in each, where the QA evidence lives, and the three known notes"
    - "QA-01 is marked complete ONLY because 06-FRITZ-QA.md exists with a pass/resolved outcome — if that file is missing or carries unresolved MUST-FIX findings, this plan STOPS and reports blocked instead of closing"
    - "REQUIREMENTS.md shows QA-01 through QA-04 checked, with the traceability table rows flipped to Complete"
  artifacts:
    - path: "REVIEW.md"
      provides: "Jon's review guide — run instructions, four concept walkthroughs, what-to-try lists, QA evidence map, known notes"
      contains: "http://localhost:4340/"
    - path: ".planning/REQUIREMENTS.md"
      provides: "QA-01..QA-04 checkboxes flipped and traceability rows marked Complete"
      contains: "- [x] **QA-04**"
  key_links:
    - from: "REVIEW.md"
      to: "serve.sh / http://localhost:4340/"
      via: "how-to-run section"
      pattern: "serve\\.sh"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/06-cross-concept-qa-review-packaging/06-FRITZ-QA.md"
      via: "QA-01 may only flip to [x] on a pass/resolved Fritz verdict"
      pattern: "\\[x\\] \\*\\*QA-01\\*\\*"
    - from: "REVIEW.md"
      to: ".planning/phases/*/captures/ and *-VERIFICATION.md"
      via: "QA evidence map section"
      pattern: "\\.planning/phases/"
---

<objective>
Package the review for Jon (REVIEW.md at the repo root) and close out the phase: incorporate the orchestrator-run Fritz brand gate's outcome, and mark QA-01 through QA-04 in REQUIREMENTS.md strictly per the evidence.

Purpose: QA-04's "ready for Jon" packaging and QA-01's sign-off bookkeeping. The Fritz brand agent ran BETWEEN waves (orchestrator's job — its findings live in 06-FRITZ-QA.md, already committed before this plan starts). This plan consumes that outcome; it never produces it.
Output: REVIEW.md, updated .planning/REQUIREMENTS.md, and a SUMMARY that records the Fritz gate outcome.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cross-concept-qa-review-packaging/06-CONTEXT.md
@.planning/phases/06-cross-concept-qa-review-packaging/06-01-SUMMARY.md
@.planning/phases/05-concept-d-home-variant/deferred-items.md
@.planning/REQUIREMENTS.md

<interfaces>
REPO ROOT: /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts

FRITZ GATE FILE (produced by the ORCHESTRATOR between waves, never by this executor): `.planning/phases/06-cross-concept-qa-review-packaging/06-FRITZ-QA.md`. Contract: it records the Fritz brand agent's findings across all four concepts + the gallery and ends with an overall outcome. "Pass/resolved" means it states a passing verdict, or every MUST-FIX finding it raised is explicitly marked resolved/accepted-as-noted-for-Jon.

QA EVIDENCE MAP (for REVIEW.md's evidence section — these all exist on disk):
- Concept A: .planning/phases/02-concept-a-editorial-accenture-but-better/captures/ (9 captures) + 02-03-SUMMARY.md QA table
- Concept B: .planning/phases/03-concept-b-full-screen-video/captures/ (11) + 03-03-SUMMARY.md QA table
- Concept C: .planning/phases/04-concept-c-experimental-webgl-3d/captures/ (33) + 04-VERIFICATION.md + 04-03/04-04 SUMMARYs
- Concept D: .planning/phases/05-concept-d-home-variant/captures/ (10) + 05-VERIFICATION.md + 05-03-SUMMARY.md QA table
- Cross-concept: .planning/phases/06-cross-concept-qa-review-packaging/06-01-SUMMARY.md (consolidated QA table) + captures/gallery-{390,1440}.png + 06-FRITZ-QA.md

KNOWN NOTES for REVIEW.md (all three, factually):
1. Device-tier degradation (Concept C) is verified by code heuristic + captures; real-hardware behavior on integrated GPU / mobile is the one human check left for Jon on-device.
2. `concept-d/about.html`'s skip-link `#main` anchor is a pre-existing dead anchor in the staging source itself (verified identical there) — documented in .planning/phases/05-concept-d-home-variant/deferred-items.md, deliberately not fixed in the mirrored page.
3. Concept D's JS-templated copy is gated by `qa/concept-d-script-diff.py` (byte-compare vs staging) + substring copy-diff; the Puppeteer-rendered-DOM copy-diff variant remains deferred (05-RESEARCH's own recommendation) — noted, not built.

REQUIREMENTS.md EDIT SHAPE (exact current strings to flip):
- `- [ ] **QA-01**` → `- [x] **QA-01**` (same for QA-02, QA-03, QA-04 — checkbox character only, never reword the requirement text)
- Traceability rows `| QA-01 | Phase 6 | Pending |` → `| QA-01 | Phase 6 | Complete |` (same for 02/03/04)
- Update the trailing `*Last updated: ...*` line
</interfaces>
</context>

<constraints>
- Do NOT spawn any agents (no Task tool, no subagents). The Fritz review already happened between waves.
- HARD GATE on QA-01: if `06-FRITZ-QA.md` does not exist, or you READ it and it contains unresolved MUST-FIX findings, STOP — do not flip QA-01, do not write the closeout as complete; report blocked with the exact unresolved findings quoted. Never edit 06-FRITZ-QA.md, and never force the verify green.
- REVIEW.md style: it's for Jon — honest working-doc tone, no decorative rule lines (`---`/`***`/`___` divider lines — use headings and space), no numbered section lists (order isn't load-bearing except "look at D first", which is said in prose), no banned tagline, no marketing gloss. Do not paste canonical site copy into it; describe, don't quote at length.
- Do not modify concept files, the gallery, or QA scripts in this plan.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write REVIEW.md for Jon</name>
  <files>REVIEW.md</files>
  <action>
Create `REVIEW.md` at the repo root with these sections (headings + space for separation, never divider lines):

1. **How to run** — `./serve.sh` from the repo root, then open `http://localhost:4340/`. One line: the gallery links to all four concepts; everything is static, no build step.
2. **The four concepts** — one factual paragraph each (A editorial card grid, B full-screen video with hotspots + chapter panels, C three.js topic field with DOM mirror + device tiering, D light-video home variant with expanding module cards). State in prose that Concept D is the Jon-directed speculative variant of the deployed homepage and the one to look at first. Draw wording from ROADMAP phase goals and 06-01's gallery descriptions — describe mechanics and intent, never invent claims.
3. **What to try** — per concept, the same 2-3 hints as the gallery cards, plus the states worth checking: reduced-motion (B poster fallback, C frozen scene, D paused poster), no-JS (A/C/D fallbacks), keyboard paths (B hotspots, C labels, D modals with Esc/focus-return).
4. **Where the QA evidence lives** — the evidence map from the interfaces block, as paths: per-phase captures/ dirs, 04/05 VERIFICATION.md files, the consolidated 06-01-SUMMARY QA table, the gallery captures, and 06-FRITZ-QA.md for the brand sign-off.
5. **Known notes** — the three notes from the interfaces block, stated plainly (what it is, why it's left, where it's documented).

Keep it tight — roughly a page and a half of markdown. It's a wayfinding document, not a report.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && test -s REVIEW.md && grep -q 'serve.sh' REVIEW.md && grep -q 'http://localhost:4340/' REVIEW.md && grep -q 'Concept A' REVIEW.md && grep -q 'Concept B' REVIEW.md && grep -q 'Concept C' REVIEW.md && grep -q 'Concept D' REVIEW.md && grep -qi 'first' REVIEW.md && grep -qi 'about.html' REVIEW.md && grep -qi 'device' REVIEW.md && grep -qiE 'puppeteer|rendered' REVIEW.md && grep -q '.planning/phases/' REVIEW.md && grep -qi 'reduced-motion' REVIEW.md && ! grep -qiE 'fresh thinking starts here' REVIEW.md && ! grep -qE '^[[:space:]]*(-{3,}|_{3,}|\*{3,})[[:space:]]*$' REVIEW.md</automated>
  </verify>
  <done>REVIEW.md exists at the repo root with run instructions, four one-paragraph concept walkthroughs (D flagged first-look in prose), what-to-try lists including reduced-motion/no-JS/keyboard states, the QA evidence map as real paths, and all three known notes — no divider lines, no banned tagline, honest tone.</done>
</task>

<task type="auto">
  <name>Task 2: Closeout — consume the Fritz gate and mark QA-01..04</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
1. **READ `.planning/phases/06-cross-concept-qa-review-packaging/06-FRITZ-QA.md`** (the orchestrator ran the Fritz brand agent between waves and committed this file).
   - If the file DOES NOT EXIST: STOP. Report blocked — "Fritz gate has not run; QA-01 cannot be marked. Orchestrator must run the Fritz brand agent and commit 06-FRITZ-QA.md before 06-02 can close." Do not touch REQUIREMENTS.md at all in this case.
   - If it exists but contains unresolved MUST-FIX findings (findings marked must-fix that are not marked resolved or explicitly accepted-as-noted-for-Jon): STOP. Report blocked, quoting each unresolved finding verbatim. Do not flip any checkbox.
   - If it passes (passing verdict, or all MUST-FIX findings resolved/accepted): summarize its outcome — verdict, findings count, resolutions — for this plan's SUMMARY. The verify grep below is a tripwire, not the judgment: your READ of the file is the judgment; a failing verify means STOP and report, never massage files to force green.
2. **Mark requirements in `.planning/REQUIREMENTS.md`** using the exact edit shapes from the interfaces block:
   - QA-01 → `[x]` — justified solely by 06-FRITZ-QA.md's pass/resolved outcome (the mechanical grep layer was 06-01's evidence; the Fritz agent review is the sign-off).
   - QA-02 → `[x]` — per 06-01-SUMMARY's copy-gate table (430/340/282 annotated + 71 substring + 13 script-diff checks, all green).
   - QA-03 → `[x]` — per 06-01-SUMMARY's link-integrity/sub-page/budget evidence + the per-phase responsive capture record.
   - QA-04 → `[x]` — the gallery shipped in 06-01 (four cards, real thumbnails, reviewed captures) + REVIEW.md from Task 1.
   - Flip the four traceability rows to `Complete` and update the `*Last updated:*` line. Change checkbox characters and status cells ONLY — never reword requirement text.
3. **Record in this plan's SUMMARY:** the Fritz gate outcome (verdict + findings disposition), which evidence justified each of QA-02/03/04, and the final state: all 32 v1 requirements complete.
  </action>
  <verify>
    <automated>cd /Users/jontoewsinterceptgroup.com/Creative-Projects/intercept-home-concepts && test -s .planning/phases/06-cross-concept-qa-review-packaging/06-FRITZ-QA.md && grep -qiE '\b(pass|passed|resolved|accepted)\b' .planning/phases/06-cross-concept-qa-review-packaging/06-FRITZ-QA.md && grep -qE '^- \[x\] \*\*QA-01\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*QA-02\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*QA-03\*\*' .planning/REQUIREMENTS.md && grep -qE '^- \[x\] \*\*QA-04\*\*' .planning/REQUIREMENTS.md && grep -qE '^\| QA-01 \| Phase 6 \| Complete \|' .planning/REQUIREMENTS.md && grep -qE '^\| QA-02 \| Phase 6 \| Complete \|' .planning/REQUIREMENTS.md && grep -qE '^\| QA-03 \| Phase 6 \| Complete \|' .planning/REQUIREMENTS.md && grep -qE '^\| QA-04 \| Phase 6 \| Complete \|' .planning/REQUIREMENTS.md && ! grep -qE '^- \[ \]' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>06-FRITZ-QA.md was read and its outcome is pass/resolved (or the plan stopped as blocked); QA-01..QA-04 checkboxes and traceability rows are flipped to complete with zero requirement text reworded; the SUMMARY records the Fritz verdict and the evidence trail for each requirement.</done>
</task>

</tasks>

<verification>
- REVIEW.md exists with all five sections and passes its style greps (no divider lines, no banned tagline)
- 06-FRITZ-QA.md exists, was READ, and carries a pass/resolved outcome — or the plan exited blocked without touching REQUIREMENTS.md
- REQUIREMENTS.md: all four QA checkboxes `[x]`, all four traceability rows `Complete`, no unchecked v1 requirements remain
</verification>

<success_criteria>
- QA-01 complete: Fritz brand sign-off consumed and recorded — marked strictly on the gate file's pass/resolved outcome, never assumed
- QA-04 complete: Jon has one page to open (the gallery) and one document telling him how to review it (REVIEW.md)
- Milestone v1.0: all 32 v1 requirements complete; the project is ready for Jon's side-by-side review at http://localhost:4340/
</success_criteria>

<output>
After completion, create `.planning/phases/06-cross-concept-qa-review-packaging/06-02-SUMMARY.md` — include the Fritz gate outcome (verdict, findings, dispositions) verbatim enough that the SUMMARY stands alone, the evidence trail used for QA-02/03/04, and a final milestone note: what Jon reviews, where, and the one on-device human check left (Concept C on real low-tier hardware).
</output>
